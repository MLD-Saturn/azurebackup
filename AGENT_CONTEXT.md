# AGENT_CONTEXT.md — Handoff for Copilot Sessions

This file is the **persistent memory** for Copilot agent sessions on the AzureBackup repo. It is checked into source control so the agent can read it on a fresh chat (on **any machine, any OS-supported environment**) and immediately understand the project state, conventions, recent decisions, and open questions.

## How to use this file

**At the start of every fresh Copilot chat:** read this file before doing anything else. Treat the "Active workstreams" section as a continuation of the previous session.

### Pre-commit checklist (run this immediately before every `git commit`)

This section is the load-bearing rule of the file. **The agent has demonstrably skipped it before.** When that happens the file silently drifts and the next session inherits a lie. Treat it as non-optional.

Before running `git add .; git commit -m "..."`, walk through these questions in order. If the answer to any is "yes", the commit MUST also include an AGENT_CONTEXT.md edit (and frequently a `docs/USER_GUIDE.md` or `docs/SETUP.md` edit per the Documentation trust policy).

1. **Did the change introduce a new architectural fact?** New `public` API on a `Core` type, new cross-project constant, new env var read at startup, new build-time `DefineConstants`, new file-format magic number / version byte, new partial-class seam, new singleton, new background task, new on-disk file location, new lock or shared-state contract. → Add an entry to the "Architectural facts to load into working memory" list. Do not assume "it's just a refactor" — a public API / public constant / cross-boundary contract IS a fact.
2. **Did the change introduce or remove a hard-coded duplicate of a value used in more than one project?** → Add a fact recording the canonical location and a "do not introduce a new copy" warning. (See fact #9 for the template.)
3. **Did the change land a commit with a B-number?** → Add a row to the "Recent commit history" table. Add an entry to "Completed workstreams". If the commit consumed a B-number that was pre-allocated to a different planned task in "Active workstreams", renumber the planned task and add a one-line note explaining the renumbering.
4. **Did the change resolve, block, or pivot any workstream?** → Update the corresponding W-numbered section. If a workstream is now done, move it to "Completed workstreams".
5. **Did the change establish or change a user preference, coding convention, or workflow rule?** → Add or update the corresponding entry under "Hard rules".
6. **Did the change discover a non-obvious production behavior** (memory, concurrency, persistence, network, encryption)? → Add a fact.
7. **Did the change produce a benchmark number that affects a design decision?** → Update the relevant benchmark file's xmldoc table (the source of truth) AND add a one-line summary in the relevant W-numbered section. Do not paste the full numbers here.
8. **Did the change teach a "do not do this" lesson?** → Add a fact, with the word "NEVER" or "DO NOT" so a future session greps for it.
9. **Did this session touch AGENT_CONTEXT.md at all?** → Add a dated bullet to the "Maintenance log for THIS file" at the bottom summarizing what changed in the file. One bullet per session, not per edit.

If the answer to all nine is "no", commit without touching this file. That is the only acceptable reason to skip the update.

### Hidden failure mode that has happened before

The trap is reasoning "this commit only changes code, it doesn't change documentation, so AGENT_CONTEXT doesn't need an update." That logic is wrong because **AGENT_CONTEXT documents the code, not the documentation**. A code commit that adds a new public constant is exactly the trigger for question 1 above. Commit `b9d5744` (B26) hit this trap; the catch-up was commit `df0df53` and cost the user an extra prompt to ask "did you remember the file?". Do not repeat it.

### Implausibility-prompts-verification (added 2026-04-27 after B43)

When a user reports a measurement, log line, or behaviour whose magnitude looks *implausible* given the documented design (e.g. "throughput shows 6 Mbps on gigabit Azure", "memory used reads 41 GB on a 32 GB host", "auto-repair reset to zero after restart"), do NOT accept it at face value AND do NOT immediately blame the user's environment. Treat implausibility as a prompt to verify the LABEL against the FORMULA at every producer in the codebase before tuning anything. The B43 throughput rename was the textbook case: production logs showed values that looked like single-digit Mbps for a gigabit pipeline, but the on-disk number was actually MB/s -- a factor-of-8 label error that would have wasted a network-perf investigation. The check costs one `code_search` for the field name; the cost of skipping it is days of misdirected debugging.

### Edit policy (style)

- Keep entries dated (YYYY-MM-DD).
- Move completed workstreams to the "Completed workstreams" section near the bottom.
- Keep the file under ~600 lines so a fresh agent can read it in one tool call.
- **Never** record machine-specific values (free disk, CPU model, core count, terminal-tool timeouts, exact wall-clock times, OS-edition build numbers). Those mislead the next session if it is on a different PC. Record *relative* findings (deltas, percentages, comparisons) and *workload* identity (which benchmark / which workload name produced the number) instead.

**Anti-pattern:** do not duplicate code or full xmldoc into this file. Reference file paths, commit hashes, and benchmark file xmldoc tables instead.

---

## Project at a glance

- **Name:** AzureBackup — a cross-platform desktop application that backs up local files to Azure Blob Storage with client-side encryption and content-defined chunking for deduplication. Runs on Windows, macOS, and Linux.
- **Language/runtime:** C# on .NET 10 (`net10.0` TFM).
- **Solution:** `azurebackup.sln` at repo root.
- **Project layout:**
  - `src/AzureBackup.Core/` — all backup/restore/encryption/database logic. Cross-platform-clean C# library; no UI dependencies. **All non-trivial logic lives here.**
  - `src/AzureBackup/` — **Avalonia 11.3.12** desktop UI shell (`MainWindowViewModel` + views). Targets `net10.0` (no `-windows` suffix), so it builds and runs on Windows, macOS, and Linux. Verify with `Select-String Avalonia src/AzureBackup/AzureBackup.csproj` if in doubt.
  - `tests/AzureBackup.Tests/` — xUnit. As of B25-bench-2: 759 tests, all passing.
  - `benchmarks/AzureBackup.Benchmarks/` — BenchmarkDotNet 0.15.8. Release-only, never run in CI. See "Running tests and benchmarks" below.
- **Setup, build, publish, file locations:** `docs/SETUP.md` is current and authoritative as of the most recent commit that touched it. If you change build flags, target frameworks, runtime identifiers, the `portable.marker` mechanism, the `AppMode.DataDirectory` resolution, or the encryption/chunking parameters listed in its "Technical specifications" table, you MUST update `docs/SETUP.md` in the same commit.
- **End-user UX (views, settings, menus, dialogs):** `docs/USER_GUIDE.md` is current and authoritative as of the most recent commit that touched it. If you add or rename a view (`*.axaml` under `src/AzureBackup/Views/`), add or rename a button/toggle/setting, change a default value visible to the user, or change a user-visible workflow, you MUST update `docs/USER_GUIDE.md` in the same commit.
- **Database backend:** SQLCipher-encrypted SQLite (production default since commit `63103b1` = `C-5`). Legacy LiteDB code path still present for migration only.
- **Authentication:** Argon2id KDF (64 MB, 8 lanes, 3 iterations) for both database key derivation and the `EncryptionService` content key.
- **Encryption envelope:** AES-256-GCM with `[magic(4) | version(1) | nonce(12) | ciphertext(N) | tag(16) | crc32(4)]`. `EncryptionService.EncryptionOverhead = 37`.
- **Chunking:** content-defined (Rabin-style rolling hash, window=48, prime=31). Per-extension chunk-size config (`.mkv` uses 1-64 MB, `.txt` uses 16-128 KB, etc.). Files >500 MB use 16-128 MB chunks regardless of extension.

---

## Hard rules (USER PREFERENCES — never violate)

These come from `.github/copilot-instructions.md` and from explicit user instruction during session work. Reproduced here because they apply to every session.

### Documentation trust policy

**Treat every Markdown documentation file in this repo as stale until proven current** — with exceptions noted below.

**Verified-current docs (treat as authoritative, but maintain rigorously):**

- `docs/SETUP.md` — verified accurate against source on 2026-04-24. Covers Azure provisioning, build-from-source, single-file publish, portable mode (`portable.marker`), encryption envelope, per-extension chunking config, storage tiers, `AppMode.DataDirectory` paths, and a verified "Technical specifications" table.
- `docs/USER_GUIDE.md` — verified accurate against source on 2026-04-24. Covers every view (`SyncView`, `SettingsView`, `StorageHealthView`, `TierMigrationView`, `DataIntegrityView`, `LogsView`), the diagnostic-bundle export flow, the memory-limit slider, and all four storage tiers (Hot/Cool/Cold/Archive).
- `AGENT_CONTEXT.md` - verified accurate against source on 2026-04-27. Covers project layout, architectural facts, workstream state, and the conventions for commits, terminal commands, and tool usage that every future session MUST follow.

**Mandatory maintenance protocol for the verified-current docs:** every commit that touches one of the things they describe MUST update them in the same commit. Specifically:

- Adding/renaming a view file (`src/AzureBackup/Views/*.axaml`) → update `docs/USER_GUIDE.md` table of contents and the corresponding section.
- Adding/renaming a user-visible button, toggle, or setting → update `docs/USER_GUIDE.md`.
- Changing a default value the user can see (default storage tier, default container name, default exclusion patterns, default memory-limit state) → update `docs/USER_GUIDE.md`.
- Changing the encryption envelope, Argon2id parameters, chunk-size config, storage-tier set, or `AppMode.DataDirectory` resolution → update `docs/SETUP.md`.
- Changing build flags, target framework, runtime identifiers, the publish profile, or NuGet package versions called out in the technical-specifications table → update `docs/SETUP.md`.
- Adding or renaming a top-level menu, tab, or workflow step → update both docs as appropriate.
- **Renaming a serialized field, schema column, JSON key, log key, or any other on-disk / on-wire identifier** → update every doc that quotes the old name in the same commit. The grep target is the literal old token, not the prose around it; doc table cells that reference the old key are silent landmines until found.

**Label/formula consistency rule (added 2026-04-27 after B43).** Any persisted numeric field whose NAME implies a unit (e.g. `*_mbps`, `*_kib`, `*_us`, `*_per_sec`) MUST match the formula at every producer. The agent shipped `ThroughputMbps` for months while every producer divided by `1024 * 1024` (i.e. produced megabytes per second, MB/s) -- the label was wrong by a factor of 8 and silently corrupted every downstream throughput comparison. Do not trust the existing field name as evidence of the unit; trace the formula at the producer and the consumer separately and confirm they agree. When renaming the field, pin the JSON key explicitly via `[JsonPropertyName(...)]` so the configured naming policy cannot silently regress the key on a future rename.

When the doc and the code drift, the doc is wrong by definition; fix the doc, do not weaken the code to match. If a commit cannot reasonably update the doc in the same change (e.g. an emergency hotfix), record an observation in this file's maintenance log and open a follow-up.

For any other Markdown doc not on the verified-current list:

- Verify it against the actual code, project files, configuration, build output, or a fresh test run. Do not paraphrase a doc claim into a response without that verification.
- If the statement is wrong or out of date, update the doc file in the same commit as the code change that reveals the discrepancy. Do not leave a known-wrong doc file in place.

This file (`AGENT_CONTEXT.md`) is the one doc that future sessions are *required* to read first, so the bar for keeping it current is correspondingly higher: every commit that invalidates anything in it MUST update it in the same commit, and the maintenance log at the bottom must record the change.

### Terminal commands

- **Single physical line, always.** Chain with `;`. No here-strings, no backticks, no multi-line.
- The user's shell is **PowerShell 7+ (`pwsh.exe`)**. Other shells will be flagged immediately.
- Length is not a concern — correctness and single-line execution are.
- Occasionally a tooling display artifact mangles the leading characters of the command in the echoed output (e.g. `Set-Location` shown as `t-Location`). This indicates the command was not run through the terminal tool as intended. If you see this, stop and fix the command rather than proceeding with a broken one.

### Git commits

- Commit immediately at logical stopping points.
- Use `git add .; git commit -m "..."` chained on one line, with multiple `-m` flags (one per paragraph). **Never** write the message to a file. **Never** use `git commit -F`.
- **No emoji, no non-ASCII** in commit messages.
- Escape `"` and `\` properly so the command parses cleanly.
- **Never run `git push` under any circumstances.** Pushing is a manual step the user performs.
- **One logical change, one commit.** Do not split a single change across multiple commits to work around tool limitations (e.g. the placeholder-hash trailer pattern previously used for `35536b0`, `4364faf`, etc. is retired -- see hash policy below).

#### Commit message hygiene

Commit messages exist to explain the substance of the change to a reader who is browsing history months later. They are NOT a session-narration log, a process audit, or a checklist re-statement. Write them accordingly:

- **Describe what changed and why.** Code changes, the bug being fixed, the design decision being made, the new feature being introduced, performance numbers if relevant, the test count delta if it moved.
- **Do NOT mention things that did not change.** "docs/SETUP.md was checked, no update needed" is noise -- the absence of a docs/SETUP.md edit in the commit already conveys that. Same for any "I checked X and X was fine" line.
- **Do NOT restate rules that live in this file.** "Commit is local; per the never-push rule it will not be pushed" is implicit. "Pre-commit checklist items 1, 2, 4 did not fire" is process narration. "Per the verified-current-docs maintenance protocol" is a process reference. The reader already knows the rules; the commit message describes the change, not the process that produced it.
- **Do NOT refer to yourself as the agent.** Write in the impersonal third person about the change. "Added the guard" not "the agent added the guard". "Restructured the conditional allocation" not "I restructured". The author of the commit is recorded in the git metadata; it does not belong in the message body.
- **The same hygiene applies to AGENT_CONTEXT maintenance-log entries** -- they describe what changed in this file and the load-bearing reason, not the dialogue that led to the change. "User asked the agent to look at X" is the wrong shape; "X was changed because Y" is the right shape.

#### Commit hashes in `AGENT_CONTEXT.md`

The recent-commit-history table and Completed-workstreams entries reference commit hashes as a navigation aid. Because a commit cannot embed its own hash, follow this rule for new B-numbered commits:

- Add the new row to the table with a `<pending>` placeholder where the hash would go. Do the same for the matching Completed-workstreams entry.
- The next session that touches `AGENT_CONTEXT.md` for any other reason replaces any `<pending>` placeholders it finds with the actual hashes from `git log --oneline | Select-String 'B<n>:'`. This is a low-priority janitorial step, not a separate commit.
- **Never** commit a placeholder hash and then immediately follow up with a trailer commit whose only purpose is to fill in the hash. That pattern (used by `35536b0`, `4364faf`, `75ceaa9`, `d5fbc38`, `d3e60d4`) inflated the commit history with no substance and is retired.

### Tool usage

- Prefer `replace_string_in_file` / `multi_replace_string_in_file` over re-creating files. Re-create only when the file is small or being rewritten end-to-end.
- Read files before editing them.
- The `nuget_get-nuget-solver*` and `start_modernization` tools are NEVER appropriate for this project's typical work — do not invoke them unless the user explicitly asks for a package upgrade or .NET version change.
- Do not call `code_search` in parallel; do not call `run_command_in_terminal` in parallel.
- Long-running operations (benchmarks, large test sweeps) may exceed the terminal tool's timeout (whatever it is in the current environment). BenchmarkDotNet writes its result artifacts on each child-process completion, so even when the orchestrating call appears to time out you can usually find the artifacts under `BenchmarkDotNet.Artifacts/results/`. Always check there before assuming a benchmark didn't run.

---

## Architectural facts to load into working memory

These are non-obvious and have caused real bugs when forgotten.

1. **`SqliteBackend` is single-connection, single-threaded.** Post-B23 every read AND write goes through one `SQLiteConnection` serialized by a single `_writeLock`. Adding more file-level parallelism cannot help SqliteBackend throughput; it just adds contention. This is a known constraint of the chosen backend, not a bug.

2. **`FileOperationDiagnostics` (`.diag` files)** are written by 4 producers: `BackupOrchestrator.BackupFileAsync`, `RestoreService.RestoreFileAsync`, `RestoreService.Batch.cs` (CorruptedRecovery), `IntegrityCheckService.CheckOneFileAsync`. Post-B23 + B24, **all four only emit on real errors**. The `FlushAllLive` shutdown hook in `FileOperationDiagnostics` snapshots any diag still in the live `_live` registry at process exit — so the success/cancel paths in producers MUST call `Discard()` to deregister.

3. **`MemoryBudget` is a soft cap, configured via the WPF app's memory-limit slider** (`MemoryLimitEnabled` + `MemoryLimitMB` in the Configuration table). When `MemoryLimitEnabled=false` the budget is unlimited and the orchestrator can hold many GB of in-flight chunk buffers — confirmed by production telemetry showing multi-GB process working sets on large workloads with `memoryBudget=unlimited`. The dominant memory cost is `EffectiveMaxParallelChunkUploads (default 6) × per-chunk size (up to 64 MB for media) × EffectiveMaxParallelFileBackups (default 8)`.

4. **CPU is NOT the production bottleneck.** Production telemetry on a 7,313-file backup showed CPU usage well below 5%. The actual bottleneck is somewhere downstream: disk read throughput, the SqliteBackend write lock, or HttpClient/TLS state. Any "make this more parallel" hypothesis must be measured rather than reasoned about — the benchmarks under `benchmarks/AzureBackup.Benchmarks/` exist precisely for this.

5. **`InMemoryBlobService`** (`src/AzureBackup.Core/Services/InMemoryBlobService.cs`) is a production-quality fake of `IBlobStorageService`. Used by ~10 test files and by all 4 backup benchmarks. Constructor takes optional `simulatedLatencyMs` and `failureRate`. Honors deduplication. Tracks `TotalBytesUploaded` / `TotalOperations`. **This is the single biggest enabler of network-free testing in the project.** Use it instead of inventing a new mock.

6. **Backend selection is SQLite-only in production.** As of B58 the legacy `AZBK_USE_SQLITE` env-var feature flag and every code path it gated have been removed. `DatabaseBackendFactory.ShouldUseSqlite()` now returns `true` unconditionally except when an internal-only `AsyncLocal<bool?>` test override pins it to `false`; production callers cannot reach the LiteDB branch and there is no env var to set or unset. The override exists only so the `BackendOverrideScope` test helper and the `BackendContractTests` shared-contract suite can still exercise the retained `LiteDbBackend` adapter. Do NOT re-introduce an environment variable to flip backends; do NOT add new production callers of the AsyncLocal override; do NOT delete `LiteDbBackend` until the contract-test suite is also retired.

7. **`BackupOrchestrator` constructor** signature: `(LocalDatabaseService, EncryptionService, ChunkingService, IBlobStorageService, FileWatcherService)`. The `FileWatcherService` is required even when no folders are watched — it can be constructed without ever being started.

8. **`BackupOrchestrator` parallelism overrides (B25-bench-2 seam):** two optional `int?` properties — `MaxParallelChunkUploadsOverride` (default null → 6) and `MaxParallelFileBackupsOverride` (default null → 8). Read once per backup operation via internal `EffectiveMax*` getters so they cannot change mid-pipeline. Production behavior is unchanged when overrides are unset; the seam exists for the parallelism benchmarks. If you want to change the production *default*, change the constants in `BackupOrchestrator.cs`, not the override.

9. **`RestoreService.SmallFileThresholdBytes` (B26) is the single source of truth for what "small" means.** This `public const long` (currently `16L * 1024 * 1024`, i.e. 16 MB) lives on the `RestoreService` partial in `src/AzureBackup.Core/Services/RestoreService.Batch.cs` and is referenced by **both** the production restore pipeline (which routes files at or below the threshold through a 32-way parallel small-file lane) **and** the desktop UI (which groups the same set of files into the aggregate "Small files" progress row in `ProgressTabViewModel.SmallFileGroupText`, plus three call sites in `MainWindowViewModel.Commands.Backup.cs` / `MainWindowViewModel.Commands.TreeView.cs`). Before B26 the UI hard-coded 100 MB in three places while production used 16 MB, so the progress row mis-grouped a strictly larger set of files than the pipeline actually optimized for. **Do not introduce a new hard-coded copy of this value anywhere.** If you need the threshold in a new file, add `using AzureBackup.Core.Services;` and reference `RestoreService.SmallFileThresholdBytes` directly. The displayed user-facing label interpolates the constant so it tracks any future change automatically.

10. **B27 benchmark seam (`BackupBenchmarkBase`) — three new public surfaces.** `BackupBenchmarkBase.MemoryLimitMBOverride` is a `protected virtual int?` returning `null` by default; when non-null, `BaseIterationSetup` writes `MemoryLimitEnabled=true` + `MemoryLimitMB=<value>` via `LocalDatabaseService.SaveConfiguration` BEFORE the orchestrator runs, and `BackupOrchestrator.BackupFilesCoreAsync` picks it up via its `GetConfiguration()` call at the top of every backup. This is how the big-scale benchmarks simulate the recommended 16 GB production setting without touching production code. `BackupBenchmarkBase.BigWorkloads` is the public set of disk-heavy workloads (`media-library-500` ~260 GB, `production-scale-3000` ~70 GB, `huge-outlier-mixed` ~32 GB) gated by a new disk-space preflight at the top of `BaseGlobalSetup` that throws a clear error if free disk < 1.5x estimated workload size. The preflight reads `EstimateWorkloadBytes(Workload)` which returns 0 for the pre-B27 small workloads (no preflight) and a conservative overestimate for big ones. **Pre-B27 small workloads are unchanged** so the historical i7-9700K result tables in the four pre-B27 benchmark xmldocs remain directly comparable to a fresh small-workload run on the same hardware. Do not duplicate the `MemoryLimitMB=16384` value — if the recommended production setting changes, change it once in the relevant `*BigScaleBenchmark.cs` files (and `ProductionScaleBackupBenchmark.cs` and `MemoryBudgetBenchmark.cs`) and update this fact. **2026-04-27 correction (post-B27 hardware run):** the seam works as designed; the OOM observed at `[ChunkConcurrency=24, Workload=media-library-500]` on the first B27 hardware run was NOT caused by `MemoryLimitMB=16384` failing to bind. Root cause was the destination side -- see fact #11.

11. **`InMemoryBlobService` discard mode (B27).** New constructor parameter `bool retainPayloads = true` (default preserves all pre-B27 behaviour). When `false`, `UploadChunkAsync` / `UploadChunkDirectAsync` still record the blob name and tier in `_blobs` / `_blobTiers` (so dedup, listing, and `BlobExistsAsync` work) but the encrypted payload is replaced with `Array.Empty<byte>()`; the would-be storage cost is recorded in `_discardModeStorageBytes` so `TotalStorageUsed` stays accurate. Discard mode short-circuits the dedup-collision verification path because the original bytes are no longer present to compare against -- it trusts the hash, matching real Azure's optimistic dedup. `DownloadChunkAsync` and `VerifyChunkIntegrityAsync` are not supported against a discard-mode store, so restore-style benchmarks must NOT opt in. Production code never sets the flag; production tests never set the flag. The flag exists exclusively to remove the RAM ceiling on big-scale backup-throughput benchmarks: without it, `media-library-500` (~260 GB) cannot fit its encrypted ciphertext in process memory regardless of `MemoryBudget`, and the previously observed 58 GB peak working set was the `_blobs` dictionary growing unboundedly, not the orchestrator chunk-buffer pool. The benchmark opt-in is the new `protected virtual bool BackupBenchmarkBase.RetainBlobPayloads => true`; all five big-scale benchmark classes (`AdaptiveChunkConcurrencyBigScaleBenchmark`, `TwoTierFileSplitBigScaleBenchmark`, `LargestFirstSchedulingBigScaleBenchmark`, `ProductionScaleBackupBenchmark`, `MemoryBudgetBenchmark`) override it to `false`. **DO NOT add a new big-scale benchmark against `media-library-500` or larger without setting `RetainBlobPayloads => false`** -- it will OOM the host. **DO NOT use discard mode for any test that calls `DownloadChunkAsync` / `VerifyChunkIntegrityAsync` against the same store** -- they will fail because the bytes are gone.

12. **`BackupOrchestrator.BackupFilesAsync` reports unchanged-file bytes as completed (B31).** The orchestrator computes its own `totalFiles` and `totalBytes` from the input list it receives (`BackupFilesCoreAsync` lines 681-696). When a file is detected as unchanged via the metadata-skip path (`BackupOrchestrator.cs` ~line 648, `progress?.Report((fileInfo.Length, fileInfo.Length))`), the unchanged file's full size flows into the running `bytesProcessed` essentially instantly. This is INTENTIONAL and is the behaviour the file-watcher path and `MirrorSyncToAzureAsync` rely on. Consequence for UI callers that derive their progress denominators from a preview's `TotalBytesToTransfer` (which excludes unchanged files): if the UI passes the unfiltered selection list to `BackupFilesAsync`, the UI's preview-derived denominator (e.g. 1 TB of changed bytes) will be exceeded by the orchestrator's reported numerator (e.g. 2 TB including unchanged), producing 200%-style progress and a wildly wrong upload-speed estimate that briefly shows the unchanged files' bytes as transferred instantaneously. **DO NOT call `BackupOrchestrator.BackupFilesAsync` from a UI command path with the unfiltered user selection.** The B31 fix makes `MainWindowViewModel.ConfirmAndFilterBackupFilesAsync` rebuild the file list as `preview.FilesToCreate.Where(IsIncluded) UNION preview.FilesToOverwrite.Where(IsIncluded)` so unchanged files are stripped before the orchestrator ever sees them. Two regression tests in `BackupOrchestratorTests` (`BackupFilesAsync_UnchangedFileInInputList_OrchestratorReportsItsBytesAsCompleted` and `BackupFilesAsync_MixedChangedAndUnchanged_BytesProcessedSumsAllInputs`) pin the orchestrator-side behaviour the UI fix relies on. The Mirror Sync path on `MainWindowViewModel.Commands.TreeView.cs` is structured differently (it derives its denominator from the same unfiltered list and adds unchanged files instantly via an explicit "Unchanged" handler) and is not affected by this fix.

13. **Force-reupload + integrity auto-repair (B42).** `BackupOrchestrator.BackupFileAsync` gained a new optional parameter `bool forceReupload = false` on its deepest overload (the three shorter overloads default to `false` and behave exactly as before). When `true`, the metadata-skip fast path is bypassed (~line 654), `existingHashes` is treated as empty so the chunker emits every chunk as new (~line 681), and `isNewFile` is forced so the upload routes through `UploadChunkDirectAsync` instead of the dedup-aware `UploadChunkAsync` path (~line 686). `BackupOrchestrator.BackupFilesAsync` and `BackupFilesCoreAsync` both accept and propagate the same flag. **`PerformFullScanAsync` (in `BackupOrchestrator.Operations.cs`) was rewired in the same commit to actually re-upload** -- pre-B42 it only enqueued `FileChangeEvent`s which still hit the metadata-skip path for unchanged files, despite the UI button being labelled as a force-reupload action. It now invokes `BackupFilesAsync(allFiles, ..., forceReupload: true, ct)` directly. `IntegrityCheckService` gained a public `Func<string, CancellationToken, Task<bool>>? RepairCallback` and a private `RepairableReasons` whitelist (`missing-blob`, `wrong-size`, `md5-mismatch`, `crc-mismatch`, `decrypt-failed`, `byte-differ`, `download-error`); `IntegrityCheckOptions` gained `bool AutoRepairOnFailure { get; init; } = true`; `IntegrityCheckRun` gained `int FilesAutoRepaired` (B43: now persisted to the SQL `integrity_check_runs.files_auto_repaired` column; pre-B43 rows backfill to 0). When the first per-file pass produces at least one repairable failure and a callback is wired, the engine `Discard()`s the first-pass diag, invokes the callback (which is wired in `MainWindowViewModel` to `_orchestrator.BackupFileAsync(..., forceReupload: true, ...)`), re-reads the file's metadata via `_databaseService.GetBackedUpFile(path)` so the second pass uses the freshly-uploaded chunk hashes, and re-runs the chunk-loop. If the second pass is clean: no `.diag` is written, no row is persisted in `integrity_check_failures`, and `FilesAutoRepaired` is incremented. If the callback returns `false` or the second pass still fails: the original (or post-repair) failures are reported normally with a `.diag` flushed via the new `ReplayFailuresToDiag` / `FlushPassDiag` helpers. The retry is one-shot -- no recursion, no escalating budget. Three regression tests in `IntegrityCheckServiceTests` pin the contract (`AutoRepair_MissingBlob_ReuploadFixesIt_NoFailureRecordedNoDiag`, `AutoRepair_Disabled_FailureStillReported`, `AutoRepair_CallbackReturnsFalse_OriginalFailureReported`). **DO NOT call `BackupFileAsync(path, ct)` (the two-arg overload) from a repair callback** -- it hits the metadata-skip path and the missing/corrupt blob stays missing/corrupt; you MUST use the deepest overload with `forceReupload: true`. **DO NOT add a new failure reason that re-uploading cannot fix to `RepairableReasons`** (e.g. `local-file-missing`, `engine-error`) -- the callback would loop pointlessly and the user would see no failure.

14. **Throughput JSONL field is megabytes-per-second, key pinned (B43).** `ThroughputMetrics.FileMetrics` and `OperationMetrics` expose the C# property `ThroughputMBps` (note the capitalization: B-M-B-p-s, not B-Mbps). The on-disk JSON key is pinned via `[JsonPropertyName("throughput_mbytes_per_sec")]` so the configured `JsonNamingPolicy.SnakeCaseLower` cannot regress the key on a future rename. The value is computed at every producer as `bytes / elapsedSeconds / 1048576`, i.e. **megabytes per second (MB/s)**. Pre-B43 the property was named `ThroughputMbps` and the key was `throughput_mbps`, advertising megabits per second -- a factor-of-8 label error against the actual MB/s formula. Pre-B43 JSONL files contain the old key but the same MB/s values; consumers do a key-rename only, no value conversion. **DO NOT introduce a new throughput field with a `_mbps` suffix unless the producer literally multiplies by 8 to convert to megabits.** **DO NOT remove the `[JsonPropertyName]` attribute** -- the snake_case policy would silently emit `throughput_m_bps` and break every grep tool that expects the canonical key. The implausibility lesson is recorded in the new "Implausibility-prompts-verification" subsection under Hard Rules: when a user reports a number that looks off by a small-integer factor, suspect a label/formula mismatch BEFORE suspecting the environment.

15. **`integrity_check_runs.files_auto_repaired` schema column (B43).** Added in `SqliteBackend.Schema.cs` as `INTEGER NOT NULL DEFAULT 0`, with an idempotent `pragma_table_info` probe + `ALTER TABLE` backfill so existing user databases pick up the column on first open without a destructive migration. `InsertIntegrityCheckRun` and `UpdateIntegrityCheckRun` write the value, `GetRecentIntegrityCheckRuns` selects it, `ReadRun` maps it onto `IntegrityCheckRun.FilesAutoRepaired`. Pre-B43 historical rows backfill to 0 (the value was not recorded at the time, so 0 is the historical truth). **DO NOT change the column default to anything other than 0** -- the History expander would misrepresent legacy runs. **DO NOT remove the `pragma_table_info` probe** -- a fresh install does not need it but every existing user DB does, and a missing column would surface as a parameter-binding error on the next run insert.

16. **Catalog-file integrity diagnostic distinct from Data Integrity feature (B44).** `SqliteBackend.IntegrityFile.cs` adds `CheckDatabaseFileIntegrity()` which runs `PRAGMA cipher_integrity_check` followed by `PRAGMA integrity_check` under the same write lock the rest of the backend uses, and returns a `DatabaseFileIntegrityResult` record with `CipherIntegrityMessages`, `SqliteIntegrityMessages`, `CipherOk`, `SqliteOk`, and `IsHealthy`. **The two pragmas have OPPOSITE success shapes**: SQLCipher's `cipher_integrity_check` returns ZERO rows on success and one row per failing page; stock SQLite's `integrity_check` returns a single literal `"ok"` row on success and one row per problem. The result record encodes that asymmetry (`CipherOk => Count==0`, `SqliteOk => Count==1 && rows[0]=="ok"`); the StorageHealthViewModel report formatter spells `ok (no failing pages)` so the empty-list shape is not misread as "the pragma did not run". `LocalDatabaseService.CheckDatabaseFileIntegrity()` forwards on SQLite and throws `NotSupportedException` on the LiteDB legacy backend. `IntegrityCheckService.RunAsync` wraps the very first `InsertIntegrityCheckRun` call in a typed catch for SQLite error 11 (`SQLITE_CORRUPT`) and 26 (`SQLITE_NOTADB`) and rethrows as the new `DatabaseFileCorruptException` (carries `SqliteErrorCode`); `DataIntegrityViewModel.CheckNowAsync` and `ReCheckFailuresAsync` catch that type specifically and surface `Check could not start: …` / `Re-check could not start: …` with a pointer to the new Storage Health diagnostic. The Storage Health view exposes a `Verify Database File` button + scrolling `SelectableTextBlock` report bound to `VerifyDatabaseFileCommand` / `DatabaseFileReport`. **DO NOT collapse the cipher and SQLite pragmas into one bool** -- the two failure modes have different remediations (re-derive key vs restore from backup vs rebuild from Azure metadata) and the user needs to see which one fired. **DO NOT use `cipher_integrity_check` to distinguish "healthy" from "pragma did not run"** -- the empty-list shape is the success signal; check the connection state before calling instead. **DO NOT route `SQLITE_CORRUPT` errors from the integrity-check engine through the generic Data Integrity "check failed" path** -- a corrupt catalog cannot be repaired by re-running the Data Integrity check, and the auto-repair callback would loop pointlessly.

17. **In-place narrow REINDEX-based catalog repair (B45).** `SqliteBackend.IntegrityFile.cs` adds `ReindexCorruptIndexes(DatabaseFileIntegrityResult diagnosis)` plus the supporting `DatabaseRepairClassifier`, `DatabaseRepairClassification`, and `DatabaseRepairResult` types. The repair takes the diagnosis as input rather than recomputing it so the action operates on the EXACT findings the user saw. Three layered safety gates fire in order: (a) refuse if `!diagnosis.CipherOk` (cipher-pragma damage means page bytes do not decrypt; REINDEX cannot help and would race the same bad pages); (b) refuse if `diagnosis.IsHealthy` (no-op guard; without it a healthy DB would be REINDEXed for nothing); (c) refuse if the classifier reports any unrepairable finding. The classifier whitelist is intentionally narrow: only the three known index-only-damage shapes `wrong # of entries in index NAME`, `row N missing from index NAME`, and `non-unique entry in index NAME` (case-insensitive, whitespace-trimmed, deduplicated). Anything else (page-level damage, freelist damage, unfamiliar text) routes to the unrepairable bucket. After the gates pass, every classified index name is validated against `sqlite_master` BEFORE issuing REINDEX as defence-in-depth against an injected name in a poisoned message; the REINDEX statement quotes the identifier with embedded `""` escapes even though the name was confirmed to come from `sqlite_master`. Each REINDEX runs in its own implicit transaction so a failure on one index does not roll back successful repairs of others -- partial recovery is strictly better than no recovery, and the post-repair re-verification (both pragmas re-run inside the same write lock so no other writer can interleave) makes any remaining damage visible. `LocalDatabaseService.RepairDatabaseIndexes(DatabaseFileIntegrityResult diagnosis)` is the SQLite-only forwarder. `StorageHealthViewModel` caches the most recent diagnosis and classification in `_lastDiagnosis` / `_lastClassification`; `CanAttemptRepair` is true only when not busy, the diagnosis has `CipherOk == true`, the classification has `AllRepairable == true`, AND there is at least one extracted index name. The Attempt Repair button is bound to `CanAttemptRepair` so it stays disabled until a Verify Database File run produces a qualifying shape. **DO NOT widen the classifier whitelist** -- adding patterns the classifier doesn't already recognise risks running REINDEX against damage it cannot fix; if a future SQLite version emits a new index-only-damage shape, add it as its own regex with a regression test rather than relaxing existing patterns. **DO NOT skip the `sqlite_master` membership check** -- the integrity-check message is the only place the index name comes from, and that string flows into a constructed SQL statement; the membership check is the canonical safety against a poisoned message. **DO NOT call the public `CheckDatabaseFileIntegrity` from inside `ReindexCorruptIndexes`** -- the repair already holds the write lock and `InWriteLock` uses a non-recursive lock; re-run the pragma helpers directly. **DO NOT auto-clear the cached diagnosis between Attempt Repair and a fresh Verify** -- the post-repair diagnosis IS the new cached state and drives the next click of the same button (typically: classification empty, button disables).

19. **LiteDB compatibility surface fully retired in W4 (B47, B57, B58, B59, B60).** The historical LiteDB code path was removed across five coordinated commits. **B47** dropped the unlock-time `EnsureMigratedToSqliteAsync` probe. **B57** (Phase 1) removed the production migration bridges from the three viewmodel files. **B58** (Phase 2) deleted the orphaned migration helpers (`LocalDatabaseService.Migration.Sqlite.cs` in full, the legacy helpers in `LocalDatabaseService.Migration.cs`, the `.upgrade-pending` rename dance, the `.unencrypted.bak` / `.legacy.bak` / `.litedb-backup` / `.sqlite-tmp` artefacts, the matching test suites). **B59** (Phase 3 Commit 1) collapsed the SQLite-vs-LiteDB selection seam: deleted `DatabaseBackendFactory.ShouldUseSqlite` and the `AsyncLocal<bool?>` override; collapsed `LocalDatabaseService.InitializeCore` to unconditional SQLite; deleted `InitializeLiteDbCore`, `DeriveKeyFromPassword`, the four LiteDB-only Argon2id constants on `LocalDatabaseService`, the `BackendOverrideScope` test helper, `DatabaseBackendFactoryTests`, `LocalDatabaseServiceSqliteRoutingTests`, and the LiteDB-pinning cases in `IntegrityCheckServiceTests` / `LocalDatabaseServiceTests`. **B60 (Phase 3 Commit 2, this commit)** retired the residual surface: `LiteDbBackend.cs` deleted; `LocalDatabaseService.Locking.cs` deleted; `LocalDatabaseService.cs` and `LocalDatabaseService.ChunkIndex.cs` rewritten as pure SQLite forwarders (the LiteDB-typed collection fields `_database`, `_configCollection`, `_filesCollection`, `_pendingChangesCollection`, `_chunkIndexCollection`, `_indexMetadataCollection`, `_chunkFileRefsCollection`, the `ReaderWriterLockSlim _dbLock`, the `_checkpointTimer` + `CheckpointInterval` + `CheckpointTimerCallback`, the `OverwriteSensitiveData` helper, the `EnsureInitialized` helper, the two-branch `if (_sqliteBackend != null) ... else _xCollection!.X(...)` pattern across every method, the `using LiteDB`, and the `using System.Security.Cryptography` for `RandomNumberGenerator` are all gone; `EnsureInitialized` was replaced by a private `GetBackend()` helper that throws if `_sqliteBackend` is null); `LocalDatabaseService.GetChunkEntriesForFile_LegacyScan` (the LiteDB-only full-scan path) deleted; `BackendContractTests.cs` deleted; eight backend-comparison benchmarks deleted (`ConcurrentReadersBackendBenchmark.cs`, `GetChunkEntriesForFileBackendBenchmark.cs`, `OpenDatabaseBackendBenchmark.cs`, `RebuildReverseChunkIndexBackendBenchmark.cs`, `SaveBackedUpFileBackendBenchmark.cs` from the original list, plus `BulkRemoveFileReferencesBenchmark.cs`, `ConcurrentGetChunkEntriesBenchmark.cs`, `GetChunkEntriesForFileBenchmark.cs` discovered during the C2 build because they all called the deleted `GetChunkEntriesForFile_LegacyScan`); `BenchDataHelper` extracted into its own `benchmarks/AzureBackup.Benchmarks/BenchDataHelper.cs` because it had been embedded in one of the deleted files but is still consumed by `RebuildReverseChunkIndexBenchmark.cs`; `[LiteDB.BsonIgnore]` and `[LiteDB.BsonId]` attributes stripped from `ConfigurationModels.cs` (3 sites), `FileModels.cs` (1 site), and `ChunkIndexModels.cs` (1 site) -- they were dead annotations that no SQLite code path read but the LiteDB package reference was the only thing that supplied the namespace; `DiagnosticBundleExporter.cs` and `DiagnosticBundleExporterTests.cs` lost their `.upgrade-pending` and `.litedb-backup` filename-pattern awareness; the `<PackageReference Include="LiteDB" Version="5.0.21" />` removed from `src/AzureBackup.Core/AzureBackup.Core.csproj`. The `Konscious.Security.Cryptography.Argon2` package reference is INTENTIONALLY retained -- it is the production SQLCipher KDF used by `EncryptionService.cs`, `SqliteBackend.cs`, and `SqliteBackend.Schema.cs`. The `IDatabaseBackend` interface is INTENTIONALLY retained -- only one implementation remains (`SqliteBackend`), but the abstraction is cheap to keep and removing it is a separate refactor that does not belong in W4. **DO NOT re-introduce ANY LiteDB code path** -- there are no field users, no legitimate test scenario, and no production caller. **DO NOT re-introduce the `.upgrade-pending` sentinel or the four-step rename dance** -- they were specific to the deleted migration helpers. **DO NOT add a backend-selection probe back into the auth path** -- SQLite is the only backend by construction. **DO NOT remove the `Konscious.Security.Cryptography.Argon2` package reference** -- it is the production SQLCipher KDF, not a LiteDB-era leftover. **DO NOT add `[LiteDB.*]` attributes back to model classes** -- they would re-add the package dependency and serve no SQLite consumer.

20. **Wrong-password classifier in `SqliteBackend.OpenAndUnlockCore` covers `SQLITE_CORRUPT` (B48).** Pre-B48 the `catch (SqliteException ex)` block inside the post-`PRAGMA key` `sqlite_master` validation probe only mapped `SqliteErrorCode == 26` (`SQLITE_NOTADB`) and a couple of message substrings (`"not a database"`, `"file is encrypted"`) to `InvalidPasswordException`; everything else propagated unchanged. SQLCipher's wrong-key surface is non-deterministic across passwords because the page-1 decrypt produces uniform-random-looking bytes -- a wrong password can also surface as `SqliteErrorCode == 11` (`SQLITE_CORRUPT`, message `"database disk image is malformed"`) when the garbage decrypt happens to parse as a partially-valid header but with internally inconsistent b-tree pointers. The B47 LiteDB-probe removal stopped masking this leak, and the next user with a "garbage-decrypt looks corrupt" password received a raw `SQLite Error 11: 'database disk image is malformed'` on the unlock screen instead of the expected `Invalid password` message. B48 adds `ex.SqliteErrorCode == 11` and the literal `"database disk image is malformed"` message substring to the existing wrong-password classifier in `OpenAndUnlockCore`, alongside the `26` and existing substring cases. The classifier change is scoped strictly to the validate-key probe; SQLite error 11 surfaced from any LATER path (schema-creation pass, `ApplyPragmas`, steady-state queries) propagates unchanged so genuine on-disk corruption still routes through the typed `DatabaseFileCorruptException` path added in B44 and the Storage Health diagnostic added in the same commit. New regression test `Initialize_ManyDistinctWrongPasswords_AlwaysThrowInvalidPassword` runs 50 distinct wrong passwords against the same SQLCipher database and asserts every shape (`SqliteException(11)`, `SqliteException(26)`, `OverflowException`, `ArgumentOutOfRangeException`, `IndexOutOfRangeException`) is mapped to `InvalidPasswordException`; pre-fix this test failed with a `SqliteException(11)` leak in well under 50 iterations. **DO NOT widen the classifier to cover error codes 11 or 26 outside the `validateKey && dbExistedBeforeOpen` probe block** -- once that probe has succeeded the connection is known-good, and any later corruption surface is genuine and must reach the typed `DatabaseFileCorruptException` path that drives the Storage Health repair UI. **DO NOT collapse the password-error path and the corruption-error path into a single message** -- the two have completely different remediations (retry vs Storage Health -> Verify Database File -> Attempt Repair / Rebuild from Azure), and a generic message would route the user to the wrong tool every time.

18. **Rebuild from Azure repopulates the backed-up-file catalog (B46).**

21. **Shell-level async dispose on shutdown + lock-protected `Checkpoint` (B49).** Two paired changes that together preserve clean catalog teardown across every classic-desktop exit path. (a) `App.axaml.cs` subscribes to `IClassicDesktopStyleApplicationLifetime.ShutdownRequested` in `OnFrameworkInitializationCompleted` and runs a two-pass dance: first pass cancels the shutdown via `e.Cancel = true` and fires `ShutdownAfterDisposeAsync` (a fire-and-forget continuation that awaits `MainWindowViewModel.DisposeAsync()` and then re-issues `desktop.Shutdown()`); second pass observes the new `_shutdownDisposeCompleted` gate and lets the lifetime tear down. The async dispose flushes the hourly checkpoint timer, runs the final `_databaseService.Checkpoint()`, and disposes the `LocalDatabaseService` / SQLCipher connection. (b) `SqliteBackend.Checkpoint()` is now wrapped in `InWriteLock(...)` so the public checkpoint contract matches every other writer; pre-B49 the body called `_connection.CreateCommand()` directly on the shared `SqliteConnection`, racing the hourly checkpoint timer against in-flight writers and producing the failure shapes recorded under fact #1 plus the `SQLite Error 11: 'database disk image is malformed'` next-open failure when a torn `PRAGMA wal_checkpoint(TRUNCATE)` left the WAL unrecoverable. Regression test `Checkpoint_ConcurrentWithWriters_DoesNotProduceDriverOrTransactionErrors` in `SqliteBackendSmokeTests` fans out 8 writer tasks concurrently with 50 `Checkpoint()` calls. **DO NOT reintroduce a direct `_connection.CreateCommand()` path on any public maintenance method** -- every method that touches the shared connection MUST go through `InWriteLock` or `InReadLock`. **DO NOT remove the `ShutdownRequested` handler in `App.axaml.cs`** -- it is the only call site that invokes `MainWindowViewModel.DisposeAsync()` on tray Exit / `desktop.Shutdown()`. **DO NOT call any synchronous catalog-mutating API after the shutdown handler has fired the dispose continuation** -- the second pass through `OnShutdownRequested` short-circuits via `_shutdownDisposeCompleted` and the lifetime tears down immediately.

22. **Non-destructive catalog quarantine (B50).** New recovery path for an unreadable local catalog database that the user did NOT ask to delete. `LocalDatabaseService.Migration.cs` adds the static helper `QuarantineCorruptDatabase(string databasePath)` which generates a single UTC `yyyyMMdd-HHmmss` stamp, moves the main database file to `databasePath + ".quarantine-" + stamp` FIRST (so a failure surfaces immediately and the operation is not partially complete), then best-effort moves `-wal`, `-shm`, `-journal`, and the `.salt` sidecar via a local `TryMoveCompanion` helper, returning a `QuarantineResult` record with `QuarantinedDatabasePath`, `MovedFiles` (main DB always at index 0), and `SkippedFiles` (one line per companion whose move failed, typically antivirus-locked). The bytes are NEVER securely deleted; quarantine is the deliberate counterpart to `SqliteBackend.SecureReset` / `BackupOrchestrator.ResetApplicationAsync`, which destroy the catalog because the user explicitly asked to start over. `LocalDatabaseService.QuarantineAndClose(string databasePath)` is the instance forwarder: it `Close()`s the live SQLCipher connection BEFORE invoking the static helper because Windows holds an exclusive file handle on the open database file and `File.Move` would fail otherwise. `BackupOrchestrator.QuarantineCorruptCatalogAsync(string databasePath)` is the application-level entry point: it calls `StopAsync()` if a backup is running, clears `_encryptionService` (`ClearKey()`) and the Entra ID `_credential`, calls `_databaseService.QuarantineAndClose(databasePath)`, and emits a `StatusChanged` message instructing the user to set a new password and re-enter Azure details. The UI surface is in `Settings -> Danger Zone` (NOT Storage Health) because the startup flow in `MainWindow.HandleStartupFlowAsync` routes locked users to Settings -- a user who cannot unlock the catalog can never reach Storage Health. `MainWindowViewModel.IsQuarantinePending` + `RequestQuarantine` / `CancelQuarantine` / `ConfirmQuarantineAsync` mirror the existing reset confirmation pattern. **The encrypted connection string is treated as UNRECOVERABLE on this path by design** -- it lives inside the quarantined catalog and cannot be decrypted without the original key; the user is forced to re-enter it manually after the fresh catalog is created. Watched folders, exclusion patterns, storage account name, and container name are also catalog-resident and require manual re-entry; the recommended follow-up is `Storage Health -> Rebuild from Azure` (fact #18 / B46) to repopulate the chunk index and backed-up-file graph from Azure metadata. Three regression tests in `SqliteBackendSmokeTests` pin the contract: `QuarantineCorruptDatabase_MovesEveryArtefactUnderTimestampedSuffix_AndPreservesBytes` (originals gone, quarantined copies byte-identical to originals), `QuarantineCorruptDatabase_AllowsFreshInitializeAtSamePath_WithDifferentPassword` (fresh `Initialize` succeeds at the same path with a different password and does NOT disturb the quarantined bytes), and `QuarantineCorruptDatabase_OnMissingFile_ThrowsFileNotFoundException` (programming-error guard surfaces the offending path). **DO NOT add salvage/decrypt logic that tries to extract the encrypted connection string from a quarantined catalog** -- the user explicitly chose manual re-entry over salvage. **DO NOT route quarantine through Storage Health** -- locked users cannot reach that view; the entry point MUST remain on Settings or in the navigation/auth surface. **DO NOT change `QuarantineCorruptDatabase` to delete the original bytes** -- the recovery semantics depend on the bytes being preserved on disk under the timestamped suffix; deletion is `SecureReset`'s job, not quarantine's. **DO NOT skip the `Close()` step in `QuarantineAndClose`** -- on Windows the file handle on the open SQLCipher database prevents `File.Move`; reordering would silently succeed on Linux/macOS and fail loudly on Windows, masking the bug.

23. **Rebuild from quarantined catalog (B51).** Recovery extension on top of the B50 quarantine path: rebuild a brand-new local catalog at the active path using a previously quarantined catalog plus user-supplied Azure connection details, without forcing the user to first start over from a fresh password. The architectural insight is that **two distinct salt domains coexist** even though the user types one password: the plaintext `.salt` sidecar next to the SQLCipher database is the salt that derives the SQLCipher unlock key, while the in-database column `config.password_salt` (read by `SqliteBackend.GetConfiguration`) is the salt that derives the Azure blob-encryption key via `EncryptionService.DeriveKeyAsync`. The `.salt` sidecar alone is therefore NOT sufficient for Azure recovery; the Azure-side salt lives inside the encrypted catalog and can only be read after a successful read-only unlock. New public static helper `SqliteBackend.ReadPasswordSaltFromQuarantinedCatalog(string quarantinedDatabasePath, string quarantinedSaltPath, ReadOnlySpan<char> password)` opens the quarantined database with `SqliteOpenMode.ReadOnly` (the input bytes are never modified), uses the caller-supplied quarantined salt sidecar (NOT the on-disk `.salt` next to the file) for the SQLCipher key derivation, validates the salt sidecar is exactly `SaltSize` bytes, runs the existing validate-key probe inside `OpenAndUnlockCore` (the single password-correctness oracle), and returns the 16-byte `byte[]?` from `SELECT password_salt FROM config WHERE id = 1` (or `null` if the column was never populated). New application-level entry point `BackupOrchestrator.RebuildFromQuarantinedCatalogAsync(quarantinedDatabasePath, quarantinedSaltPath, ReadOnlyMemory<char> password, connectionString, containerName, freshDatabasePath, IProgress?, CancellationToken)` orchestrates the full recovery in seven phases: extract the recovered `PasswordSalt` from the quarantined DB; stop any running backup and clear in-memory secrets; quarantine any unexpected catalog already sitting at `freshDatabasePath` (NEVER overwrite or delete a catalog file silently); initialize a fresh catalog at the active path with the user's password; reseed the fresh `config` row by overwriting the throwaway `PasswordSalt` and `PasswordVerificationHash` with values derived from the recovered salt so the new catalog can decrypt blobs that were encrypted with the old key; arm the encryption service with the recovered key; persist the user-supplied connection string + container via `SaveConnectionStringAsync` (which re-encrypts the connection string with the recovered key and opens the live blob connection); then call `_chunkIndexService.RebuildIndexFromAzureAsync(progress, ct)` (fact #18 / B46) to repopulate `chunk_index`, `chunk_file_refs`, and the backed-up-file graph from Azure metadata. Cancellation is honoured during the Azure rebuild phase only; the read-only extraction phase is fast and not cancellable. UI surface lives next to the B50 quarantine block in `SettingsView.axaml -> Danger Zone`: a `Rebuild From Quarantined Catalog...` button toggles `MainWindowViewModel.IsRebuildFromQuarantinePending`, which expands a five-field form (`RebuildQuarantinedDbPath`, `RebuildQuarantinedSaltPath`, `RebuildQuarantinedPassword`, `RebuildConnectionString`, `RebuildContainerName`); the `ConfirmRebuildFromQuarantineAsync` command in `MainWindowViewModel.Commands.Navigation.cs` validates all five fields are non-blank, calls the orchestrator, and surfaces typed exceptions with user-readable log lines (`InvalidPasswordException` -> "the password does not match the quarantined catalog. The active catalog file at the rebuild target was NOT created."; `FileNotFoundException` -> "file not found -- {path}"; `InvalidOperationException` -> raw message). Six new regression tests pin the contract: four helper-level smoke tests in `SqliteBackendSmokeTests` (`ReadPasswordSaltFromQuarantinedCatalog_WithCorrectPassword_ReturnsStoredSalt`, `..._WithWrongPassword_ThrowsInvalidPasswordException`, `..._WithMissingSaltSidecar_ThrowsFileNotFoundException`, `..._WithMismatchedSaltSize_ThrowsInvalidOperationException`) plus two end-to-end orchestrator tests in the new `RebuildFromQuarantinedCatalogTests` file (`RebuildFromQuarantinedCatalog_RestoresFilesFromAzure_AndPreservesPasswordSalt` and `RebuildFromQuarantinedCatalog_WrongPassword_ThrowsInvalidPassword_LeavesActivePathFree`). **DO NOT collapse the SQLCipher unlock salt and the Azure encryption salt into one conceptual value** -- the `.salt` sidecar only unlocks the local DB; the Azure-side salt lives in the encrypted `config` row and is the only field the rebuild flow extracts from the quarantined catalog. **DO NOT bypass the validate-key probe by reading `config.password_salt` from a partially-decrypted page** -- the probe is the single oracle that distinguishes a wrong password from a successful unlock against garbage; without it a typo would derive an Azure key from random bytes and the user would silently rebuild a catalog whose blobs cannot be decrypted. **DO NOT add a fallback that opens the quarantined catalog read-write** -- the input bytes must remain forensically pristine for repeat recovery attempts. **DO NOT extend `RebuildFromQuarantinedCatalogAsync` to accept the encrypted connection string from the quarantined catalog as input** -- the user explicitly chose manual re-entry over salvage; widening the input shape recreates the salvage path B50 deliberately rejected. **DO NOT silently overwrite an existing catalog at the fresh-rebuild path** -- the orchestrator quarantines whatever is already there before initializing the fresh one, so a mistaken click does not destroy in-progress state.

24. **Budget-derived `LargeChunkBufferPool` global cache cap (B52). (B70 RENAME: the class itself was merged into `ChunkBufferPool` with `LargeChunkBucketSizes` geometry -- read every `LargeChunkBufferPool` reference below as `new ChunkBufferPool(ChunkBufferPool.LargeChunkBucketSizes, maxCachedBytes)`; the contract, cap policy, and DO-NOTs below are unchanged.)**

25. **Chunk-size-gated Azure upload transfer options (B53).** The pre-B53 `AzureBlobService.UploadEncryptedChunkAsync` applied a single `DefaultTransferOptions` (`MaximumConcurrency=8`, `MaximumTransferSize=8 MB`) to every encrypted chunk regardless of size, so the SDK staged ~64 MB per in-flight upload (`MaximumConcurrency * MaximumTransferSize`) even for chunks small enough to fit in a single PUT. Under the 16-way file × 6-way chunk concurrency ceiling that staging residency stacks across every concurrent upload and lives entirely outside the `MemoryBudget` accounting -- the dominant Azure-SDK contribution to the W3 over-budget reservoir. B53 introduces the new internal static helper `AzureBlobService.ComputeUploadTransferOptions(int encryptedLength)` which scales `MaximumConcurrency × MaximumTransferSize` linearly with the encrypted payload length: ≤ 8 MB → 1 × actualLength (single PUT, no parallel staging), ≤ 16 MB → 2 × 8 MB, ≤ 32 MB → 4 × 8 MB, > 32 MB → 8 × 8 MB (the pre-B53 `DefaultTransferOptions`, unchanged so the 64 MB media-library shape that drives the 350 Mbps observed upload ceiling keeps its full fan-out). `UploadEncryptedChunkAsync` now calls the helper instead of using `DefaultTransferOptions` directly; both `UploadChunkAsync` and `UploadChunkDirectAsync` flow through that single shared helper so both the dedup-aware and direct-upload paths get the gating. The download side (`AzureBlobService.Download.cs`) still uses `DefaultTransferOptions` because there is no caller that benefits from gating on the download side -- restore stages are already governed by `RestoreService.ComputeAdaptiveChunkConcurrency`. The 8 MB block size and 8-way ceiling are preserved verbatim for the upper band so the per-chunk throughput on chunks that actually need parallel staging is unchanged; only chunks small enough to fit in one or two blocks see reduced staging. Nine new tests in `AzureBlobServiceTransferOptionsTests` (parameterised across the four bands plus zero/negative input plus a `StagingResidencyBound_TracksEncryptedLength` test that pins `MaximumConcurrency × MaximumTransferSize` at every band) lock in the contract; the staging-residency test specifically pins the 1 MB / 8 MB / 16 MB / 32 MB / 64 MB residency values so a future regression that re-broadens the helper (e.g. removing the small-chunk single-PUT path) fails loudly. **DO NOT change the upper-band fan-out** -- the `> 32 MB → 8 × 8 MB` shape is the throughput-optimal config for the 64 MB media-library chunks and the only band the user-reported 350 Mbps ceiling was measured against; lowering it risks regressing the dominant production workload. **DO NOT apply `ComputeUploadTransferOptions` to the download side** -- restore concurrency is governed by a separate per-file adaptive helper and the staging-residency problem is upload-side only. **DO NOT widen the threshold ladder without a fresh `BackupThroughputBenchmark` run** -- the 8 / 16 / 32 MB step sizes were chosen so the staging-residency band always stays at or below the encrypted payload size; a narrower threshold could make the small-chunk PUT lose pipeline parallelism on the read-encrypt path. **DO NOT bypass the helper by passing `DefaultTransferOptions` directly to `BlobUploadOptions`** -- the helper is the single point that enforces the per-band staging ceiling, and a direct `DefaultTransferOptions` reference re-introduces the pre-B53 64 MB per-upload staging on whatever code path skipped it.

26. **Budget-derived effective file concurrency (B54).** Pre-B54 `BackupOrchestrator` used `EffectiveMaxParallelFileBackups` (default 16) as the file-level fan-out for every backup operation, regardless of the active `MemoryBudget`. On a small `MemoryLimitMB` setting that constant could admit more files than the budget can sustain, causing every chunk producer to stall against the budget the moment it tried to acquire a chunk buffer; the orchestrator would still SPIN UP all 16 file workers, each contributing Azure SDK staging residency outside the budget. B54 derives the effective file concurrency from the active budget via the new `internal static int BackupOrchestrator.ComputeEffectiveFileConcurrency(MemoryBudget budget, int configuredCeiling)` helper, which returns `clamp(budget.TotalBytes / EstimatedPerFileResidencyBytes, 1, configuredCeiling)` for finite budgets and `configuredCeiling` for unlimited budgets. The estimate `EstimatedPerFileResidencyBytes = 512 MB` matches the worst-case per-file producer charge (`MaxParallelChunkUploads (6) × MaxChunkSize (64 MB) = 384 MB` plus per-operation overhead) and also matches the historical B27 hand-picked ratio (`MemoryLimitMB=8192 ÷ MaxParallelFileBackups=16 = 512 MB per file`); the formal scaling rule therefore agrees with the historical default at the production setting and only changes behaviour at smaller budgets. The floor of 1 preserves the invariant that backups always make some forward progress even on a tiny budget; the budget itself still throttles admission inside the per-file pipeline. Three call sites are wired: `BackupFilesAsync` snapshots the value once and passes it through the new `BackupFilesCoreAsync` parameter `int effectiveFileConcurrency`; `MirrorSyncToAzureAsync` does the same so the mirror path's log line, metrics (`OperationMetrics.FileConcurrency`), and `ParallelOptions.MaxDegreeOfParallelism` all agree on the same value (the variable was hoisted out of the `if (filesToBackup.Count > 0)` block so the post-loop metrics record the value the loop actually used); `RunBackupLoopAsync` computes a fresh `batchFileConcurrency` per loop iteration so a slider change between batches takes effect immediately. The `Metrics?.RecordDecision("backup-concurrency", ...)` payload now carries both `maxParallelFileBackups` (the hard ceiling) and `effectiveFileConcurrency` (the budget-clamped value) so a post-hoc analysis can attribute throughput deltas to either the constant or the budget. Fourteen new regression tests in `tests/AzureBackup.Tests/Unit/BackupOrchestratorFileConcurrencyTests.cs` (`UnlimitedBudget_ReturnsConfiguredCeiling`, `BudgetWellAboveCeiling_ReturnsConfiguredCeiling`, `BudgetEqualsCeilingTimesPerFile_ReturnsCeilingExactly`, parameterised `BudgetBelowOrAtCeiling_ScalesDown` across 8 GB / 4 GB / 2 GB / 1 GB / 512 MB / 256 MB tiers, `TinyBudget_ReturnsFloorOfOne`, `NullBudget_Throws`, `ZeroCeiling_Throws`, `NegativeCeiling_Throws`, `CeilingOfOne_AlwaysReturnsOne`) pin the contract. **DO NOT compute `ComputeEffectiveFileConcurrency` from `_databaseService.GetConfiguration()` directly** -- the `MemoryBudget` instance is the single source of truth within an operation scope, and reading the config separately would let a slider change mid-operation produce a fan-out that disagrees with the budget the same operation is throttling against. **DO NOT lower `EstimatedPerFileResidencyBytes` below 512 MB** -- the value matches both the worst-case per-file producer charge and the production-tested B27 ratio, and a lower estimate would over-admit files at the production budget and re-introduce the W3 over-budget condition. **DO NOT remove the floor of 1** -- a tiny-budget user must still be able to back up; the budget itself will throttle admission inside the per-file pipeline. **DO NOT change the helper to read `Environment.ProcessorCount`** -- file-level fan-out is a memory-residency decision, not a CPU decision, and CPU is not the production bottleneck (architectural fact #4); blending CPU into this calculation would re-introduce the pre-B27 throttling pattern that the hardware run already disproved. **DO NOT bypass the helper by passing `EffectiveMaxParallelFileBackups` directly to `ParallelOptions.MaxDegreeOfParallelism`** in any new operation entry point -- the helper is the single point that enforces the budget-vs-fan-out relationship, and a direct reference re-introduces the pre-B54 admission gap.

27. **Producer-side Azure SDK staging charge (B55).** The B53 chunk-size-gated upload helper made the SDK staging residency *predictable* per chunk, but pre-B55 the staging bytes were still entirely invisible to the active `MemoryBudget`: `ChunkingService.AcquireChunkBufferAsync` charged only the chunk payload (B30) plus the encrypt-side ArrayPool rental (B38), so a 16-way file × 6-way chunk pipeline could push process working set well above `MemoryLimitMB` even when the budget read near-empty. B55 closes the gap by adding the new `internal static long AzureBlobService.EstimateUploadStagingBytes(int encryptedLength)` helper and folding its return value into `AcquireChunkBufferAsync`'s atomic `MemoryBudget.AcquireAsync`. The helper is implemented as `MaximumConcurrency × MaximumTransferSize` from the same `ComputeUploadTransferOptions(encryptedLength)` that the upload site itself hands to the Azure SDK -- the two numbers move in lockstep by construction, so a future change to the per-band staging shape automatically updates both the upload site's residency and the budget's accounting. Per-chunk staging charges therefore range from 1 MB (≤ 8 MB chunks, single PUT) up to 64 MB (> 32 MB chunks, full 8×8 MB fan-out). The producer-side charge is a conservative upper bound (the SDK does not necessarily hold the full N×M block at every instant) but matches the same upper-bound discipline B30/B38 already use for the chunk and encrypt buffers. The deadlock-avoidance branch in `MemoryBudget.AcquireAsync` (B34) still admits an at-least-one oversized request, so a tiny budget cannot wedge the pipeline. The per-file residency estimate `EstimatedPerFileResidencyBytes = 512 MB` (architectural fact #26 / B54) was deliberately NOT widened to match the new larger per-chunk charge -- raising it would over-throttle file-level fan-out and re-introduce the pre-B27 admission stalls; instead, the budget itself admits fewer concurrent chunks per file when staging is charged, which converges to the same in-flight residency without an orchestrator-side over-correction. Nine new tests in `AzureBlobServiceTransferOptionsTests` parameterised across 1 MB / 8 MB / 16 MB / 32 MB / 64 MB / 128 MB / 256 MB pin `EstimateUploadStagingBytes` to the same residency values the staging-bound test pins for `ComputeUploadTransferOptions`, plus zero/negative input validation; this guarantees the budget charge cannot drift away from the actual SDK staging shape used at the upload site. **DO NOT compute the staging charge from a separate constant** -- the helper deliberately re-uses `ComputeUploadTransferOptions` so that any future change to the per-band staging shape stays in sync between the upload site and the budget; a duplicated constant would silently over- or under-charge on the next B53 ladder change. **DO NOT widen `EstimatedPerFileResidencyBytes` to compensate for the larger per-chunk charge** -- the budget itself absorbs the extra charge by admitting fewer chunks per file; raising the per-file estimate would over-throttle file fan-out and undo the historical B27 production-default sizing. **DO NOT push the staging charge to the consumer side** -- B38 already proved that any consumer-side acquire on the same budget can deadlock against a producer that holds its charge until the consumer finishes; the producer-side charge is the only deadlock-free architecture for pipeline residency. **DO NOT skip the staging charge for chunks below the pool-skip threshold** -- the SDK stages the same per-band shape regardless of whether the chunk buffer came from the ArrayPool or a fresh allocation, so the staging charge is independent of the pool-routing decision and must apply to both code paths.

28. **Peak-aware `[mem]` log telemetry (B56).**

29. **`QuarantineFileLocator` codifies pair-by-stamp invariants for the rebuild form (B61).** New `public static class QuarantineFileLocator` in `src/AzureBackup.Core/Services/QuarantineFileLocator.cs` is the single source of truth for "which quarantined file pair should the Settings rebuild form auto-fill?". Three public members: `(string? DatabasePath, string? SaltPath) FindMostRecentQuarantinePair(string? directory)`, `bool IsQuarantineFileName(string?)`, `DateTime? TryParseQuarantineStamp(string?)`. The locator pairs files **by identical timestamp suffix** rather than by separate newest-db and newest-salt because two unrelated quarantine events would otherwise produce a mismatched pair that fails downstream validation (the salt-sidecar size check in `LoadOrCreateSalt`, or worse, the validate-key probe garbage-matching with a salt from a different event). It walks past lonely halves (db without salt, or salt without db) so the caller never sees a half-populated tuple. It explicitly skips `-wal` / `-shm` / `-journal` companions even though those files share the `backup.db` prefix. The timestamp is parsed from the filename by regex (`\.quarantine-(\d{8}-\d{6})$`, case-insensitive) rather than read from `File.GetCreationTimeUtc` so a copy/restore that resets ctime cannot reorder the result; the producer side (`LocalDatabaseService.QuarantineCorruptDatabase`, fact #22) emits exactly that suffix shape with a single UTC stamp per quarantine event. The locator is the ONLY consumer of the on-disk filename format; the rebuild form's `RequestRebuildFromQuarantine` calls it once at form-open and pre-fills empty path fields. **DO NOT change `FindMostRecentQuarantinePair` to fall back to "newest db + newest salt" if no identical-stamp pair exists** -- the rebuild orchestrator's `ReadPasswordSaltFromQuarantinedCatalog` (fact #23) absolutely requires the salt sidecar that pairs with the database, and a heuristic cross-pairing would silently route a wrong salt into Argon2id key derivation. The user can still manually pick a non-matching pair via the Browse buttons; that is a deliberate user override and surfaces as `Invalid password` if the pair is actually wrong. **DO NOT broaden the regex past `\d{8}-\d{6}`** -- the producer side emits that exact shape via `DateTime.UtcNow.ToString("yyyyMMdd-HHmmss")` and any drift between producer and consumer would cause silent skipping. **DO NOT extend the locator to scan recursively** -- the data directory is the only producer location and recursive scanning would risk picking up backup snapshots, sync-tool copies, or user-side archives whose pairing semantics the locator cannot validate. **DO NOT make the locator throw on a missing or unreadable directory** -- the form-open code path runs every time the user expands the rebuild form and a missing directory just means "no auto-fill suggestion to offer", not a programming error. **DO NOT introduce a new on-disk filename pattern for quarantined files without updating `QuarantineFileLocator` and adding a regression test** -- the locator and the producer in `LocalDatabaseService.Migration.cs` are a coupled pair, and the contract fact in #22 names the exact stamp format; changing one without the other breaks the auto-fill silently.

**DO NOT pass a `BandwidthScheduler` instance into a single-file restore from the UI** -- the controller exists only to coordinate concurrency across multiple files in a batch, and a single-file UI restore has no batch context to share state across.

31. **B63 polish on the B62 restore pipeline (variable-step AIMD, peak telemetry, watchdog deduplication, cleanup correctness).** Follow-up to architectural fact #30 after a code review of the just-landed B62 pipeline. Five coordinated changes, all in the restore code path. (a) **Variable-step additive increase.** `BandwidthScheduler.ApplyAdditiveIncrease(double delta)` now steps `+2` when `delta > PlateauBand * 4` (a strong-improvement signal) and `+1` otherwise, instead of always `+1`. AIMD's "additive" half is conventionally +1 per evaluation tick, but on a fast link a +1 climb from `initialConnections=4` to a 16-32 ceiling takes 24-56 s of sustained sample windows; on a household 100 Mbps link the user-perceived ramp under the pre-B63 +1 step was visibly slow at the start of a large restore. The +2 step is bounded by the existing `_maxConnections` clamp and only fires when the EWMA improvement is at least 4x the plateau band so a noisy single sample cannot trigger overshoot. The increase remains "additive" in character (linear, not multiplicative) -- the change is the linear coefficient, not the policy. (b) **`PeakActiveSlots` lifetime high-water mark.** New `BandwidthScheduler.PeakActiveSlots` `int` property tracks the maximum `_activeSlots` value observed over the scheduler's lifetime, updated lock-free via a CAS loop inside `AcquireSlotAsync`. Surfaced so `RestoreFilesBatchCoreAsync` can record the actual peak file-level concurrency in `OperationMetrics.FileConcurrency` rather than the static `MaxParallelFileRestores` ceiling, which post-B62 no longer reflects what the AIMD controller actually used. New `RestoreResult.PeakFileConcurrency` `int` carries the peak through `RestoreFilesWithRemappingAsync` and `MirrorSyncToLocalAsync`; both metric-emit sites use the peak when non-zero (scheduler engaged at least once for a large file) and fall back to `MaxParallelFileRestores` when zero (small-file-only batch never engaged the scheduler). (c) **Wakeup semaphore deduplication.** `BandwidthScheduler.ReleaseSlot()` now suppresses the wakeup when the post-decrement active count is still at or above the allowed ceiling. Pre-B63 every `ReleaseSlot` woke one waiter, which then re-parked because `active >= allowed`; under multiplicative-decrease pressure with many parked waiters this produced quadratic wakeup churn. Eventual additive-increase steps still wake waiters via their own `_acquireWakeups.Release(N)`, so skipping the redundant wakeup here is safe. The internal field was renamed from `_slotReleased` to `_acquireWakeups` because the semaphore is now released both when a slot is given back AND when an additive-increase step grants new capacity, and the old name implied only the first cause. The semaphore release for additive-increase wakeups is now also computed under `_evaluationLock` and released AFTER the lock exit -- pre-B63 the release fired under the lock, which was fragile against any future continuation that might re-enter scheduler code. (d) **Watchdog snapshot under the writer's lock.** `RestoreWithBoundedParallelDownloadsAsync` now hoists the writer's `pendingChunks` `Dictionary<int, (byte[] data, int length)>` to the outer scope, paired with a new `pendingChunksLock` `Lock`, and the stall watchdog snapshots it directly under that lock. Pre-B63 the writer maintained a parallel `ConcurrentDictionary<int, byte> bufferedIndicesSnapshot` purely so the watchdog could read indices without taking the writer's lock; that doubled per-chunk bookkeeping cost on the hot path and could drift from `pendingChunks` if the writer ever forgot to mirror an add/remove. The watchdog poll cadence is `WatchdogPollMs = 5_000`, so briefly blocking the writer for the snapshot has no measurable throughput impact and removes a whole class of "the parallel dictionary drifted" bugs. (e) **Producer cleanup correctness on cancel/error.** The producer's `catch (OperationCanceledException)` and `catch (Exception ex)` branches in the per-chunk download `Task.Run` lambda now release ONLY the budget bytes still owed (`owedBudget`, decremented to zero after the channel write transfers ownership to the writer) and return the rented buffer ONLY when it is still held (`rentedBuffer != null`, niled out after the channel write succeeds). Pre-B63 the catches blindly released `chunkMemoryCost` and never returned the rented buffer, which both over-released budget when the exception fired between Phase B (post-`memoryBudget.Release(chunk.Length)`) and the channel write, and leaked ArrayPool buffers on every cancelled or failed download that had already produced a chunk. The watchdog also stamps `lastDownloadCompletedTicks` BEFORE the bounded-channel `WriteAsync` so the "downloads idle" line distinguishes a download-stage stall from a writer-stage stall (a slow writer parking the producer in `WriteAsync` is a writer stall, not a download stall, and conflating the two would mis-classify the watchdog snapshot). The writer's `WriteChunkAndReleaseAsync` now wraps the body in a try/finally with a `windowSlotReleased` flag so a transient `FileStream.WriteAsync` exception cannot leak a window slot and stall the next chunk; the inline release on the success path stays for clarity, the finally is belt-and-braces. New tests in `BandwidthSchedulerTests` cover the new step policy and `PeakActiveSlots`. Tests: 907 of 907 pass. Build: 0 warnings, 0 errors. `docs/USER_GUIDE.md` reviewed per the verified-current-docs maintenance protocol -- restore scheduling is internal and the user-visible memory-limit contract is unchanged. `docs/SETUP.md` reviewed -- no new build flag, runtime identifier, package, schema column, encryption-envelope change, or `AppMode.DataDirectory` resolution change. **DO NOT raise the `+2` step above `+2` or remove the `delta > PlateauBand * 4` gate** -- a larger step or a wider gate erodes the "additive" half of AIMD and can overshoot the bandwidth-fitting ceiling, defeating the whole controller. **DO NOT reset `PeakActiveSlots` between batches** -- the value is the lifetime high-water mark and is consumed at scheduler-instance disposal; resetting it would make `OperationMetrics.FileConcurrency` under-report on long-running batches. **DO NOT remove the wakeup-suppression branch in `ReleaseSlot()`** -- it is the structural defence against the quadratic wakeup churn under multiplicative-decrease pressure; the additive-increase wakeup path is independent and still fires its own `_acquireWakeups.Release(N)`. **DO NOT release `_acquireWakeups` from inside `_evaluationLock`** -- a future continuation that re-enters scheduler code could deadlock; compute the wakeup count under the lock and release after exit. **DO NOT reintroduce a parallel `ConcurrentDictionary` mirror of `pendingChunks` for watchdog purposes** -- the locked snapshot under `pendingChunksLock` is the canonical read path, and any mirror is an opportunity for drift. **DO NOT blindly release `chunkMemoryCost` in producer-side catches** -- track exactly how much budget is still owed via the `owedBudget` local and release that, otherwise the budget over-releases when the exception fires after Phase B; pair the same discipline with the `rentedBuffer` local so the ArrayPool buffer is returned exactly once across all paths.

**DO NOT delete `Program.BuildSyntheticRunnableWrapperType` or the call to it inside `VerifyBenchmarkNameResolutionContract`** -- the dynamic-assembly wrapper it emits is the ONLY thing pinning the wrapper-walk contract against a refactor that quietly switches back to `GetType().Name`. **(B68 supersedes the original B67 nested-class form: do NOT re-add the `private sealed class Runnable_999_SelfCheckOnly : MemoryBudgetBenchmark { }` shim under `Program.cs` -- BenchmarkSwitcher.FromAssembly enumerates every type in the entry assembly including non-public nested ones, will rediscover it, and will fail validation again.)**

**DO NOT carry a non-null `BufferPool` on a `ChunkPayload` whose `ReturnToPool` is also `true`** -- the producer branches are mutually exclusive (pool-rent vs ArrayPool-rent vs exact-allocation) and the consumer's dispatch order in `BackupOrchestrator.BackupFileAsync` would route to the wrong sink and silently corrupt the pool's residency accounting.

**DO NOT leak the pool reference past the batch's `using` scope** -- pools are batch-scoped on purpose so the cap policy can be derived from the active `MemoryBudget`; a process-wide pool would re-introduce the same per-core retention problem the recycler exists to solve.

**DO NOT remove the `Interlocked.Exchange` drain in `Dispose`** -- a concurrent `ReleaseRetentionCharge` from a still-running `Rent` could otherwise double-release the budget after the pool is torn down.

**DO NOT make `AzureBlobService.UploadEncryptedChunkAsync` retry path re-rent the encrypted buffer from a different pool** -- the existing `reEncrypt` lambda re-uses the same `rentedBuffer` reference, which is what guarantees the finally-block return matches the original rent source.

**B74 SUPERSEDES the large-pool half of B73's encrypted-buffer routing.** As of B74 (commit `b0402ae`, 2026-05-27) the `BackupOrchestrator` and `RestoreService` call sites pass `null`

36. **Three-fix cluster for B72/B73 anomaly investigation (B74 / W5 Phase 4 Commit 3).** Post-B73 benchmark (2026-05-27) showed bidirectional `MaxUnacc` deltas with no clear improvement and meaningful regressions on `adver-large-chunks/16384` (+3.2 GB) and `adversarial-mixed/8192-16384` (+3.3 GB / +2.8 GB). Source inspection revealed three real defects, all addressed in one commit cluster:
   * **Fix A (`BackupMemoryReporter.cs`).** The pre-B74 unaccounted formula was `workingSet - used - poolCachedNow - smallPoolCachedNow`, which assumed pool cached bytes were NOT in `_budget.UsedBytes`. After B72's retention charging, `used` ALREADY contains the pool's cached retention via the `ChargeRetention` seam, so the pre-B74 formula was double-subtracting the pool cache and producing a systematically too-low `unaccounted` reading. The corrected formula computes `usedExcludingPoolRetention = used - sum(pool.BudgetChargedBytes)` first, then subtracts the pool caches exactly once. Extracted to `internal static long ComputeUnaccountedBytes(long workingSet, long budgetUsedBytes, long poolBudgetChargedBytes, long largePoolCachedBytes, long smallPoolCachedBytes)` so the invariant can be pinned without depending on `Process.WorkingSet64`'s jitter. When no pool is wired the formula reduces to the pre-B72 shape `workingSet - used`, so non-pool callers are completely unaffected.
   * **Fix B (`ChunkingService.AcquireChunkBufferAsync`).** The producer-side encrypt-buffer pre-charge was always `NextPowerOfTwoOrSelf(encryptedPayloadLen)`, which matches `ArrayPool<byte>.Shared`'s power-of-two tier shape but UNDER-charges by up to 2x against the small `ChunkBufferPool`'s `[64K, 256K, 1M, 4M, 16M]` bucket geometry that B73 introduced. Concrete case: a 4 MB plaintext chunk encrypts to ~4 MB + 37 bytes, which the small pool rents at 16 MB but the pre-B74 charge computed as 8 MB -- an 8 MB per-chunk under-charge that could accumulate to multi-GB at 96-way in-flight worst case. The corrected branch picks `BucketCeiling(encryptedPayloadLen, smallChunkPool)` when the encrypt rent routes through the small pool, and `NextPowerOfTwoOrSelf(encryptedPayloadLen)` everywhere else (large-pool path post-Fix-C2 stays on ArrayPool, and the no-pool fallback also stays on ArrayPool). The helpers `BucketCeiling` and `NextPowerOfTwoOrSelf` were promoted from `private static` to `internal static` purely so the contract test can pin the rent-vs-charge equality at every boundary the production workloads exercise.
   * **Fix C2 (`BackupOrchestrator` + `RestoreService`).** The B73 `encryptedBufferPool` dispatch routed BOTH small and large encrypted buffers through `ChunkBufferPool`. Fix C2 reverts the LARGE-pool branch -- both callers now pass `null` for `encryptedBufferPool` whenever `encryptedSize >= ChunkingService.PoolSkipThresholdBytes`, restoring the pre-B73 `ArrayPool<byte>.Shared` routing for the large-chunk hot path so the BCL's `Gen2GcCallback`-driven trim continues to decay steady-state residency on the production media-library / large-file workloads the user prioritised. The small-pool branch is unchanged from B73 because its retention is bounded by the 12.5%-of-budget / 64 MB-floor cap (so it cannot dominate residency) AND the B72 retention charge already attributes its cached bytes to the budget, making it strictly an improvement over ArrayPool's un-attributed per-core caches for the small-chunk regime.

   **DO NOT swap the small-pool branch in `BackupOrchestrator` / `RestoreService` to ArrayPool the way C2 did for the large branch** -- the small pool's bounded cap + B72 retention charge are exactly what make its routing honest; ArrayPool's per-core caches in the small-buffer band would be un-attributed and silently fragment LOH on the 4 KB-1 MB chunks the default config produces.

   37. **Committable per-benchmark markdown summary (B75).** Adds `benchmarks/AzureBackup.Benchmarks/BenchmarkSummaryWriter.cs`, a pure helper that takes the `IEnumerable<Summary>` returned by `BenchmarkSwitcher.Run(args)` and writes one `{BenchmarkType}-summary.md` per summary into `BenchmarkDotNet.Artifacts/results/` so the relevant portions of a run are diff-friendly and overwritable across runs the same way the BDN-generated `-report.csv` already is. Motivation: the timestamped `benchmarks/*.log` file the user manually tees from `dotnet run` output is huge (130 KB+ on the memory benchmark), gitignored (the repo's `*.log` rule), and awkward to compare across runs; the most useful portions live in the `Summary` object in memory at the end of the run (per-case Mean/StdErr/StdDev, total wall-clock estimate from the aggregated Means, and the memory-fidelity columns the `MemoryFidelityColumnProvider` also feeds into the CSV). The writer re-renders those into a single markdown table per benchmark, plus a "Noisy iterations (StdErr/Mean > 5%)" callout under the table so a noisy run is impossible to miss. The hook is wired immediately after `BenchmarkSwitcher.Run(args)` returns in `Program.Main`. The writer NEVER throws out of `WriteAll` -- a top-level try/catch surfaces failures on `Console.Error` and returns an empty list rather than tearing down a multi-hour benchmark with no surviving artifacts. The format helpers (`FormatNanos`, `FormatBytes`, `FormatDuration`) were promoted from `private static` to `internal static` and the benchmarks csproj exposes them to `AzureBackup.Tests` via `InternalsVisibleTo` so 25 new tests pin every format-shape invariant + the no-throw + argument-validation contracts without depending on a real BDN `Summary` (which is awkward to construct in a unit test because the ctor takes BenchmarkReports requiring full BenchmarkCase + Descriptor + Job). The tests project now has a metadata-only `ProjectReference` to the benchmarks project; nothing in the test assembly invokes a benchmark or pulls the BDN runtime past load time. 1003/1003 tests pass (+25 vs B74). Build: 0 warnings, 0 errors. **DO NOT change `SummaryFileSuffix` from `-summary.md` without updating `docs/SETUP.md`'s artifact-naming guidance** -- the `-summary.md` suffix is load-bearing because it sorts the summary next to `-report.csv` / `-report.html` / `-report-github.md` in a directory listing and renders natively in GitHub's PR view. **DO NOT let `WriteAll` throw out of the catch** -- the whole point of the helper is that the post-run hook can never tear down a multi-hour benchmark; a future contributor tempted to "fix the swallowed exception" needs to add structured logging instead. **DO NOT add per-iteration timing or BDN validation messages to the summary** -- the file is intentionally narrow so a committed diff focuses on the metrics that actually moved; the verbose `.log` is still available for the operator who needs raw data. **DO NOT promote `BuildMarkdown` / `WriteOne` / the format helpers to public** -- the API surface is intentionally minimal (`WriteAll` + `SummaryFileSuffix`) so future shape changes don't break external callers we don't know about.

## Active workstreams (as of 2026-04-27)

**W5 (in progress, started 2026-04-28 on the `memory` branch).** Memory-residency fidelity (measurement-first redesign). The user explicitly asked for a way to benchmark how accurately the program adheres to its `MemoryLimitMB` budget BEFORE and AFTER any redesign, with an explicit invitation to consider full redesigns provided encryption, security, and bandwidth utilisation are preserved. Recommended path is four numbered phases, each independently measurable against the previous phase's baseline:

1. **Measurement infrastructure (Phase 1).** **DONE in B64 (scaffold) + B65 (first cross-process attempt, retracted) + B66 (compile-time-path cross-process fix) + B67 (benchmark-name resolution fix) + B68 (self-check assembly-isolation fix to keep the synthetic `Runnable_999_SelfCheckOnly` wrapper out of BDN's entry-assembly discovery scan).**
2. **Baseline capture (Phase 2).** USER-DRIVEN. **DONE 2026-05-22.** User ran the new fidelity benchmarks on a separate host and pushed `BenchmarkDotNet.Artifacts/results/AzureBackup.Benchmarks.MemoryBudgetBenchmark-report.csv` into the repo. Numbers consumed by B69; the result table in `MemoryBudgetBenchmark` xmldoc is the canonical "before" against which Phase 3 will be re-compared.
3. **Redesign D -- ChunkBufferPool (Phase 3).** Replace `ArrayPool<byte>.Shared` at every backup/restore call site with operation-scoped `ChunkBufferPool` instances whose retention IS the budget's residency. Closes the rent-oversize gap that produces the bulk of `unaccounted` today. Acceptance: re-run Phase 1 benchmarks; expect `MaxUnaccounted_MB` to drop materially with throughput within noise. **Phase 3 Commit 1 (B69, 2026-05-22) DONE on the backup-producer path** with the original `BudgetedMemoryPool` (small-chunk recycler for the 64 KB to 16 MB range), wired through `ChunkPayload.SmallChunkPool`, `ChunkingService.AcquireChunkBufferAsync`, `BackupOrchestrator.BackupFileAsync` overloads, `BackupOrchestrator.Operations.cs` operation-scope ownership, and `BackupMemoryReporter` telemetry. **Phase 3 Commit 2 (B70, 2026-05-22) DONE as a structural cleanup**: merged `BudgetedMemoryPool` and `LargeChunkBufferPool` into one unified `ChunkBufferPool` with parameterized bucket geometry (`SmallChunkBucketSizes` / `LargeChunkBucketSizes`), collapsed `ChunkPayload` to one `BufferPool` field, replaced both pool-specific test suites with one parameterized `ChunkBufferPoolTests.cs`. Production residency behavior is unchanged; the diff is duplication removal so future cap / peak tuning happens once. **Phase 3 Commit 3 (B71, 2026-05-22) DONE on the restore-side plaintext-buffer path**: `IBlobStorageService.DownloadChunkStreamingAsync` gained a `ChunkBufferPool?` plaintext-buffer overload; both blob-service implementations route plaintext rents through the supplied pool when present; `RestoreService` selects per-chunk pool by `chunk.Length >= ChunkingService.PoolSkipThresholdBytes`, threads it through the channel/writer/drain paths via a new per-chunk tuple slot, and uses one centralised `ReturnPlaintextBuffer` helper at every return site; `RestoreService.Batch.cs` owns the two restore-scope pools at the batch level using the same cap policy as the backup side (helpers exposed as `internal static` on `BackupOrchestrator`). 944/944 tests pass (+3 new restore-pool tests). `IntegrityCheckService` scratch buffer kept on `ArrayPool<byte>.Shared` intentionally (out of scope; see fact #33). See architectural fact #33 for full scope and DO-NOTs.

   Phase 3 is now COMPLETE; the next blocking event is the user re-running Phase 1's benchmarks (`MemoryBudgetBenchmark` plus the three adversarial workloads) so the post-B71 fidelity numbers can be compared against the Phase 2 baseline before any Phase 4 work begins.
**Phase 4 Commit 2 (B73, 2026-05-26) DONE -- encrypted-buffer pool routing.** `IBlobStorageService.UploadChunkAsync` / `UploadChunkDirectAsync` / `DownloadChunkStreamingAsync` each gained a `ChunkBufferPool? encryptedBufferPool` overload; backup-side and restore-side callers select the pool by encrypted size (same `PoolSkipThresholdBytes` partition as the plaintext path) and forward it. 957/957 tests pass (+6 vs B72). **Phase 4 Commit 3 (B74, 2026-05-27) DONE -- three-fix cluster addressing the post-B73 anomalies.** Post-B73 measurement (2026-05-27) showed bidirectional MaxUnacc deltas and regressions on `adver-large-chunks/16384` (+3.2 GB) and `adversarial-mixed/8192-16384` (+3.3 GB / +2.8 GB). Source-level investigation found three real defects, all addressed in B74: (A) `BackupMemoryReporter` was double-subtracting pool cache after B72 added it to `_budget.UsedBytes` via `ChargeRetention`; corrected formula extracted to `internal static ComputeUnaccountedBytes` so it can be pinned by unit test independent of `Process.WorkingSet64` jitter. (B) `ChunkingService.AcquireChunkBufferAsync`'s encrypt-buffer pre-charge used `NextPowerOfTwoOrSelf` which under-charged by up to 2x against the small `ChunkBufferPool`'s [64K, 256K, 1M, 4M, 16M] bucket geometry B73 introduced; corrected to use `BucketCeiling` on the small-pool branch. (C2) `BackupOrchestrator` + `RestoreService` reverted the LARGE-pool half of B73's encrypted-buffer routing (encrypted size >= 16 MB now goes back through `ArrayPool<byte>.Shared`) because `ChunkBufferPool` never evicts under GC pressure while ArrayPool's per-core caches do trim via the BCL's `Gen2GcCallback` -- pinning large encrypted buffers in the never-evict pool was the cause of the steady-state residency regression on large-chunk workloads (which is the production hot path the user prioritised: backup + download of files over 500 MB). The small-pool branch is unchanged from B73 because its bounded cap + B72 retention charge make it strictly better than ArrayPool there. 11 new tests; 978/978 pass (+21 vs B73). See architectural fact #36 for the full contract. Future native-buffer / `NativeMemory.Alloc` work (the original Phase 4 scope) is now only worth pursuing if the post-B74 measurements still show a material MaxUnacc gap; the residual bytes after B74 are SDK-internal staging buffers and OS-level FileStream pages, both outside our control.
**Phases 5 and 6 CANCELLED (2026-05-28).** The original W5 plan scoped a Phase 5 (Windows Job Object hard-cap launcher around the benchmark host to convert overshoot >10% into a build-gate failure) and a Phase 6 (conditional, larger redesigns C / E / F gated on Phase 5 still rejecting). After the post-B74 measurement showed the production large-file hot path is honest within margin and the remaining tight-cell overshoots are reported accurately rather than hidden, the user reviewed the Phase 5 scope and cancelled it: dev-only insurance was not worth maintaining a Windows-only launcher binary on top of a benchmark that already produces a diffable summary, and Phase 6 was gated entirely on Phase 5 rejecting. W5 closes out on the B74 measurement; any future memory-residency work begins a new workstream.

Per-phase landing rules: each phase is a separate B-numbered commit with its own AGENT_CONTEXT entry per the pre-commit checklist; phases 3-4 are gated by the user explicitly running phase 2's baseline first and confirming the resulting numbers.

**DO NOT** start phase 3 or 4 without first capturing the previous phase's measurement results -- the whole point of W5 is staged measurement-driven decisions.
**DO NOT** weaken AES-GCM, the encryption envelope, AIMD bandwidth fitting, or the existing producer-side memory-budget accounting (W3 facts #24-#28) in pursuit of a fidelity number; the user's "preserve encryption / security / bandwidth" constraint is non-negotiable.
**DO NOT** raise the per-bucket cap or remove the global cap on `LargeChunkBufferPool` (B52, fact #24) when implementing the new `BudgetedMemoryPool` -- the new pool replaces `ArrayPool<byte>.Shared` usage but the LOH-recycler ceiling stays as defence in depth.

**W3 (RESOLVED 2026-04-27 by B52-B56)

Follow-ups deferred from B27 (each can become its own future workstream when prioritized):

- **Workload-aware LPT scheduler.** The B27 hardware run found LPT regresses mid-sized realistic workloads (+19% on `mixed-realistic-1000`, +24% on `realistic-large-200`) but is roughly flat on big-scale realistic workloads. A scheduler that applies LPT only when size variance exceeds a threshold could in principle recover the large-skew win without the mid-sized regression, but the right shape of "variance threshold" is not obvious from the current data. Would need many more synthetic profiles between `mixed-realistic-1000` (LPT loses by 19%) and `huge-outlier-mixed` (LPT flat) to find the inflection point. Out of scope for B27 because the simpler answer (do nothing) is defensible.
- **Per-file adaptive chunk concurrency in backup.** Restore already does this (`RestoreService.ComputeAdaptiveChunkConcurrency`). For backup, the pre-B27 i7-9700K data was directionally suggestive but compromised; the post-B27 fresh data on this hardware (`AdaptiveChunkConcurrencyBenchmark` and the partial `*BigScaleBenchmark` numbers in the 2026-04-24 log) showed no monotone benefit across 4 / 6 / 12 / 24 chunk concurrency, so there is no signal for the production change to track. Re-evaluate if a future workload-shape change (e.g. dramatically larger file sizes, much higher dedup) makes the per-file chunk pipeline a measurable bottleneck.
- **`LargestFirstSchedulingBigScaleBenchmark.media-library-500` re-run.** The B27 row has a 280-minute Mean with StdDev 389 m on N=2 (one normal ~5 m run, one pathological ~9 h run during the 42-hour overnight). Almost certainly host-OS pressure rather than orchestrator behaviour; the LPT iteration on the same workload was a stable ~5 m. If a defensible number is ever needed for that cell, re-run it alone with `iterationCount>=5` in a clean session. The xmldoc result table flags this row with an asterisk and notes the cause.
- **Disk-backed test blob service.** Currently `InMemoryBlobService.retainPayloads=false` is the only escape from the 64 GB-RAM ceiling on big-scale benchmarks. A disk-backed `IBlobStorageService` would let us measure restore throughput and integrity-check throughput against a populated store at production scale, but is out of scope for B27 because backup throughput (the W1/W2 question) does not need it. See discussion in the AGENT_CONTEXT maintenance log entry below if the trade-offs ever come up again.

---

## Completed workstreams (recent)

- **B75 (commit `e119d1e`, 2026-05-27)**: committable per-benchmark markdown summary.

- **B74 (commit `b0402ae`, 2026-05-27)**: W5 Phase 4 Commit 3 -- three-fix cluster (A + B + C2)

- **B73 (commit `3a9e6a0`, 2026-05-26)**: W5 Phase 4 Commit 2 -- encrypted-buffer pool routing.

- **B72 (commit `6334deb`, 2026-05-25)**: W5 Phase 4 Commit 1 -- pool-retention budget charging.

- **B71 (commit `7dd2948`, 2026-05-22)**: W5 Phase 3 Commit 3 -- restore-side `ChunkBufferPool` plaintext-buffer routing.

- **B70 (commit `cd9c394`, 2026-05-22)**: W5 Phase 3 Commit 2 -- structural cleanup that merged `BudgetedMemoryPool` and `LargeChunkBufferPool`

- **B69 (commit `<pending>`, 2026-05-22)**:

- **B68 (commit `8b7e50b`, 2026-05-21)**: W5 Phase 1 self-check assembly-isolation fix -- moved the synthetic `Runnable_999_SelfCheckOnly` wrapper out of the entry assembly via `Reflection.Emit` so BDN's discovery scan stops rejecting every real benchmark. Added `Program.VerifyNoStaticRunnableShimInEntryAssembly()` to fail-fast at host startup. See fact #32 / fact #33 closing clause.

- **B67 (commit `b6d31cc`, 2026-05-19)**: W5 Phase 1 benchmark-name resolution fix -- `BackupBenchmarkBase.ResolveBenchmarkTypeName(Type)` walks past BDN's autogenerated `Runnable_N` / `BenchmarkDotNet.Autogenerated.*` wrappers so persisted fidelity records join on the user benchmark class. See fact #32.

- **B66 (commit `0df97e0`, 2026-05-18)**:

- **B65 (commit `00c6357`, 2026-04-28, RETRACTED BY B66 2026-05-18)**: W5 Phase 1 first cross-process fix attempt for the memory-fidelity benchmark scaffolding. The first B64 multi-hour benchmark run produced `-` in every fidelity cell of the CSV report because BDN's default toolchain runs each benchmark method in a child process; the in-memory `MemoryFidelityCollector` singleton was populated in the child but `IColumnProvider.GetValue` runs in the host AFTER the child exits, so the host saw an empty result list (only `MemoryDiagnoser` survived because BDN gives it explicit cross-process marshalling). Fix attempted: file-backed persistence via an env var.Build clean, 907/907 tests pass. Architectural fact #32 amended at the time with persistence DO-NOT clauses. **Why retracted:** the user's first benchmark run AFTER B65 produced no `azbk-bench-fidelity-*` directory under `%TEMP%` at all. Either `Program.Main` was bypassed by the launcher (so the env-var publication never ran) or `Environment.SetEnvironmentVariable` after process start did not propagate to BDN's child. Either way the seam was defeated. The corresponding lesson is recorded in the B66 entry above: a runtime mechanism that depends on launcher cooperation is structurally fragile compared to a compile-time IL constant. The original B65 lesson (a custom BDN telemetry path needs cross-process state transfer) is correct and survives in B66's design; only the *mechanism* B65 chose for that transfer was wrong. Author: Copilot agent session.

- **B64 (commit `ee4a8aa`, 2026-04-28)**: W5 Phase 1 scaffold -- adversarial workloads, `MemoryFidelityCollector` / `MemoryFidelityColumnProvider`, fidelity columns on the BDN CSV. First multi-hour run produced `-` in every fidelity column because BDN runs each benchmark in a child process; B65/B66/B67/B68 closed that gap. See fact #32 and the W5 phase plan above.

- **B63 (commit `<pending>`, 2026-04-27)**: Code-review polish on the B62 restore pipeline -- variable-step AIMD, `PeakActiveSlots`, watchdog deduplication, producer cleanup correctness. See architectural fact #31.

- **B62 (commit `e730c30`, 2026-04-27)**: Bandwidth-adaptive multi-file restore scheduling (Phase B) plus writer-released in-order chunk window (Phase C). Fixed a 45-minute restore stall. See architectural fact #30.

- **B61 (commit `80b0da2`, 2026-04-27)**: UX polish on the B51 rebuild-from-quarantined-catalog Settings form (auto-fill via `QuarantineFileLocator`, fact #29).

- **B57-B60 (commits `ca5d628`, `04e55e6`, `93aea60`, `e245594`, 2026-04-27)**: W4 retired -- LiteDB compatibility surface fully removed across four coordinated commits (production migration bridges, orphaned helpers, selection seam, residual surface + package reference). See architectural fact #19 for the full scope and DO-NOTs.

- **B52-B56 (commits `68ff4b7`, `acb64b4`, `2d6542d`, `11b29a7`, `3535574`, 2026-04-27)**: W3 resolved -- memory-residency tightening across five phases (A: bounded `LargeChunkBufferPool` cap; B: chunk-size-gated upload transfer options; C: budget-derived effective file concurrency; D: producer-side Azure SDK staging charge; F: peak-aware `[mem]` log telemetry). See architectural facts #24-#28 for full per-phase scope and DO-NOTs.- **B51 (commit `<pending>`, 2026-04-27)**:- **B50-B51 (commits `8940431`, `8227dd2`, 2026-04-27)**: Non-destructive catalog quarantine + rebuild-from-quarantined-catalog recovery path. See architectural facts #22, #23, #29.

- **B44-B49 (commits `935fc02`, `6b5ab7b`, `7c8fcf4`, `18bc7fd`, `e170a87`, `d1f90ee`, 2026-04-27)**: Storage Health diagnostic stack -- catalog-file `cipher_integrity_check` + `integrity_check` diagnostic (B44, fact #16); narrow REINDEX-based repair (B45, fact #17); Rebuild from Azure repopulates files + file_chunks (B46, fact #18); LiteDB unlock probe removed (B47); SQLCipher wrong-password classifier widened to error 11 (B48, fact #20); async-dispose-on-shutdown + lock-protected `Checkpoint` (B49, fact #21).

- **B27 (commit `fee6622`, 2026-04-27)**: Re-baselined every backup benchmark on AMD EPYC 7763 hardware; production changes shipped were `MaxParallelFileBackups` 8->16 and `MemoryLimitEnabled=true` / `MemoryLimitMB=8192` defaults; B27b (per-file adaptive chunk concurrency in backup) and B27c (blanket LPT) retired by the data. See architectural fact #10 for the benchmark seam, fact #11 for `InMemoryBlobService` discard mode.

- **B26 (commit `b9d5744`, 2026-04-24)**: Unified the small-file threshold across UI/Core boundary via `public const long RestoreService.SmallFileThresholdBytes`. See architectural fact #9.

- **B25-bench-2 (commit `171f07e`, 2026-04-24)** + **B25-bench (commit `614825a`)**: First backup throughput benchmarks + `BackupBenchmarkBase` + strictly-additive override seams on `BackupOrchestrator`. See architectural fact #8.

---

## Conventions / patterns observed in this codebase

- **`[Conditional("DIAGNOSTICLOG")]` logging**: services use `Log(string)` methods that are completely no-ops in Release without the `DIAGNOSTICLOG` constant set. The B16 commit also adds a title-bar marker showing whether the build has DEBUG/DIAG on.
- **Diagnostic events forwarded by name**: services raise `EventHandler<string>? DiagnosticLog` events; `MainWindowViewModel.OnDiagnosticLog` aggregates them all.
- **Non-destructive catalog quarantine for crash recovery**: an unreadable on-disk catalog is moved aside under a timestamped `.quarantine-yyyyMMdd-HHmmss` suffix (the bytes are NEVER securely deleted) so a fresh catalog can be initialized at the same path. See `LocalDatabaseService.Migration.cs` (`QuarantineCorruptDatabase`, `QuarantineAndClose`) and `BackupOrchestrator.RebuildFromQuarantinedCatalogAsync`. Pre-B58 the same partial also held a `.upgrade-pending` sentinel + four-step rename dance for the LiteDB-to-SQLite migration; that surface was retired with the W4 cleanup and is no longer a reference for new file-rename code.
- **`PaddedLong` for hot atomic counters**: cache-line-padded structs prevent false sharing. See `BackupOrchestrator` line ~592 for example. If adding new hot counters under parallel workers, use `PaddedLong`.
- **Pool-routing discipline (post-B70)**: producer/consumer code transfers ownership of payload buffers via the channel. The `ChunkPayload` record carries `BufferPool` (unified `ChunkBufferPool?` covering both small and large geometries) and `ReturnToPool` (legacy ArrayPool flag). The consumer's dispatch order is fixed: if `BufferPool != null` return there; else if `ReturnToPool` return to `ArrayPool<byte>.Shared`; else drop. Never set both `BufferPool != null` and `ReturnToPool=true` on the same payload, and never double-return.
- **No emoji** anywhere in the commit messages.
- **xmldoc style is heavy and discursive** — multi-paragraph `<remarks>` blocks explaining WHY a design decision was made, with B-numbers cross-referencing commits. Match this style when adding new public surface.

---

## Running tests and benchmarks

### Tests

`dotnet test tests/AzureBackup.Tests/AzureBackup.Tests.csproj -c Debug` from the repo root. All 759 tests pass deterministically.

If running from inside Visual Studio (which is the typical user environment), the `run_tests` / `get_tests` MCP tools work directly against Test Explorer. Use `Project=AzureBackup.Tests` as the filter.


---

## Recent commit history (for fast catch-up)

| Hash | Message |
|---|---|
| `e119d1e` | B75: committable per-benchmark markdown summary.
| `b0402ae` | B74: W5 Phase 4 Commit 3 -- three-fix cluster (A+B+C2)
| `3a9e6a0` | B73: W5 Phase 4 Commit 2 -- encrypted-buffer pool routing.
| `6334deb` | B72: W5 Phase 4 Commit 1 -- pool-retention budget charging.
| `7dd2948` | B71: W5 Phase 3 Commit 3 -- restore-side plaintext-buffer routing. IBlobStorageService.DownloadChunkStreamingAsync overload accepts ChunkBufferPool?; AzureBlobService.Download.cs + InMemoryBlobService.cs implement it; RestoreService.RestoreFileAsync threads per-chunk pool selection through the channel via a new tuple slot and a centralised ReturnPlaintextBuffer helper; RestoreService.Batch.cs constructs and owns the two restore-scope pools at the batch level via BackupOrchestrator.ComputePoolCapBytes / ComputeSmallPoolCapBytes (now internal static); 3 new restore-pool tests; 944/944 tests pass |
| `cd9c394` | B70: merge BudgetedMemoryPool and LargeChunkBufferPool into one parameterized ChunkBufferPool with SmallChunkBucketSizes / LargeChunkBucketSizes; collapse ChunkPayload's two pool owners into one BufferPool field; migrate ChunkingService, BackupOrchestrator, BackupOrchestrator.Operations.cs, BackupMemoryReporter; replace LargeChunkBufferPoolTests + BudgetedMemoryPoolTests with one parameterized ChunkBufferPoolTests; 941/941 tests pass |
| `ebd7e3f` | B69: BudgetedMemoryPool for the backup-producer small-chunk path; ChunkPayload.SmallChunkPool; ChunkingService routes small chunks through it; BackupOrchestrator.Operations.cs creates one per operation with ComputeSmallPoolCapBytes; BackupMemoryReporter subtracts small pool from unaccounted and adds smPool= suffix; 935/935 tests pass |
| `8b7e50b` | B68: stop B67's nested 'private sealed class Runnable_999_SelfCheckOnly : MemoryBudgetBenchmark' from leaking into BenchmarkSwitcher.FromAssembly's discovery scan,
| `6b9d875` | B67: fix AGENT_CONTEXT self-reference of B67 commit hash. Mirrors the B27/B28 pattern: amending the original B67 commit to backfill its own hash rewrites the hash, so the follow-up is a separate small commit. |
| `b6d31cc` | B67: fix the last-mile join-key bug
| `0df97e0` | B66: retract B65's env-var seam (it never produced a samples dir; some launchers bypass Program.Main and env vars do not always propagate to BDN children). Replace with a compile-time `[CallerFilePath]`-baked path resolved identically in host and every BDN child. Per-PID `samples-pid<n>.jsonl` files so concurrent processes do not contend. Visible `[B66 fidelity persist]` / `[B66 fidelity load]` Console.Error diagnostics with PID + path + record key on every persist and load. `.fidelity-samples/` .gitignore'd. End-to-end verified: dotnet exec-launched process bypasses Program.Main yet still resolves the same IL path and writes a JSON line the host loads back. Build clean, 907/907 tests pass |
| `00c6357` | B65: (RETRACTED BY B66) W5 Phase 1 first cross-process fix attempt. Added env-var seam AZBK_BENCH_FIDELITY_DIR + JSON-lines persistence; host published a per-run randomised temp dir via Environment.SetEnvironmentVariable before BenchmarkSwitcher.Run. The next benchmark run produced no samples dir at all, proving the seam was defeated by launcher behaviour or env-var inheritance. Kept here for forensic continuity; B66 replaces every line of the B65 cross-process code. |
| `ee4a8aa` | B64: W5 Phase 1 -- memory-budget fidelity benchmark scaffolding; new BackupOrchestrator.MemoryReporterIntervalOverride seam; new MemoryFidelityCollector / MemoryFidelityColumnProvider / MemoryFidelityConfig benchmark files exposing PeakWS_MB, MaxUnacc_MB, PeakBudget_MB, Overshoot, Stalls/GB, Oversized columns; three adversarial workloads (large-chunks, pool-churn, mixed); MemoryBudgetBenchmark widened to 5x4 sweep with fidelity tracking; Program.cs parser self-check; 907/907 tests pass |
| `<pending>` | B63: variable-step AIMD additive increase (+2 on strong improvement); BandwidthScheduler.PeakActiveSlots + RestoreResult.PeakFileConcurrency surfaced through OperationMetrics; ReleaseSlot wakeup-suppression + _slotReleased->_acquireWakeups rename; watchdog snapshots pendingChunks under pendingChunksLock (parallel ConcurrentDictionary mirror removed); producer cleanup correctness with owedBudget/rentedBuffer locals; writer windowSlot release in try/finally; 907/907 tests pass |
| `e730c30` | B62: bandwidth-adaptive restore scheduling (Phase B) plus writer-released in-order chunk window (Phase C); new BandwidthScheduler with AIMD+EWMA and AcquireSlotAsync/ReleaseSlot; channelCapacity reduced from effectiveConcurrency*4 to effectiveConcurrency; writer's WriteChunkAndReleaseAsync owns the slot-release point; 22 new tests in BandwidthSchedulerTests; 902/902 tests pass |
| `80b0da2` | B61: Browse buttons + auto-populated paths in the Settings rebuild-from-quarantined-catalog form;
| `e245594` | B60: W4 Phase 3 Commit 2 -- retire residual LiteDB surface;
| `93aea60` | B59: W4 Phase 3 Commit 1 -- collapse SQLite-vs-LiteDB selection seam;
| `04e55e6` | B58: W4 Phase 2 -- delete orphaned LiteDB migration helpers and tests;
| `ca5d628` | B57: W4 Phase 1 -- remove legacy LiteDB migration branches from auth/startup viewmodels (MainWindowViewModel.Commands.Auth.cs, MainWindowViewModel.cs, MainWindowViewModel.Commands.Navigation.cs); 3 files, +33/-276; 921/921 tests pass |
| `3535574` | B56: peak-aware [mem] log telemetry;
| `11b29a7` | B55: charge Azure SDK upload staging residency into producer-side MemoryBudget;
| `2d6542d` | B54: budget-derived effective file concurrency in BackupOrchestrator; new ComputeEffectiveFileConcurrency(budget, ceiling) helper clamps fan-out to budget/512 MB with floor 1 and ceiling cap; BackupFilesCoreAsync now takes an explicit int effectiveFileConcurrency; mirror-sync hoists the value so log/metrics agree; RunBackupLoopAsync re-derives per batch; 14 new tests in BackupOrchestratorFileConcurrencyTests; W3 Phase C |
| `acb64b4` | B53: chunk-size-gated Azure upload transfer options on AzureBlobService.UploadEncryptedChunkAsync; new ComputeUploadTransferOptions helper scales staging linearly (1xN <=8MB, 2x8 <=16MB, 4x8 <=32MB, 8x8 >32MB); upper-band 8x8 MB fan-out unchanged so 64 MB media chunks keep the 350 Mbps regime; download path unchanged; nine new tests in AzureBlobServiceTransferOptionsTests; W3 Phase B |
| `68ff4b7` | B52: bound LargeChunkBufferPool global cache residency at 25% of MemoryBudget (256 MB floor); new LargeChunkBufferPool(long maxCachedBytes) ctor + TotalReturnsDroppedForCap counter; BackupOrchestrator.ComputePoolCapBytes derives the cap from the active budget; pre-B52 callers preserved via parameterless ctor forwarding to long.MaxValue; seven new tests in LargeChunkBufferPoolTests; W3 Phase A |
| `8227dd2` | B51: rebuild from quarantined catalog; new SqliteBackend.ReadPasswordSaltFromQuarantinedCatalog (read-only) and BackupOrchestrator.RebuildFromQuarantinedCatalogAsync orchestration; Settings/Danger Zone form (quarantined DB + salt sidecar + password + connection string + container); recovers in-DB PasswordSalt, reseeds fresh catalog, repopulates from Azure metadata; six new regression tests; USER_GUIDE updated |
| `8940431` | B50: non-destructive catalog quarantine for unreadable local DB; new LocalDatabaseService.QuarantineCorruptDatabase + QuarantineAndClose, BackupOrchestrator.QuarantineCorruptCatalogAsync, Settings/Danger Zone UI; encrypted connection string treated as unrecoverable, user re-enters Azure details; bytes preserved on disk under .quarantine-yyyyMMdd-HHmmss suffix; three regression tests in SqliteBackendSmokeTests; USER_GUIDE updated |
| `d1f90ee` | B49: dispose MainWindowViewModel on shutdown via App.axaml.cs ShutdownRequested two-pass dance; lock-protect SqliteBackend.Checkpoint via InWriteLock so the hourly checkpoint timer no longer races writers on the shared connection; new regression test pins the contract |
| `e170a87` | B48: classify SqliteException(11) "database disk image is malformed" as InvalidPasswordException in SqliteBackend.OpenAndUnlockCore wrong-password validation probe; new regression test runs 50 distinct wrong passwords against a SQLCipher DB to cover every garbage-decrypt surface |
| `18bc7fd` | B47: remove unlock-time LiteDB migration probe from MainWindowViewModel.Commands.Auth.cs; unlock now goes directly to LocalDatabaseService.Initialize and the SQLite backend; Core migration helpers retained for tests |

Earlier history is summarized in the Completed workstreams section above. Run `git log --oneline` for full chronology. The most recent commits in the table above are local-only unless explicitly pushed -- the user pushes manually, the agent does not.

---

## Maintenance log for THIS file

- **2026-04-24** — Initial creation. Captures B25-bench-2 in-progress state, all four benchmarks measured, two production seams added, working-tree dirty pending commit. Author: Copilot agent session (Claude Sonnet 4.5).
- **2026-04-24** — Added two permanent rules: (1) never run `git push` under any circumstances, pushing is the user's manual job; (2) treat every Markdown doc in the repo as stale until verified against code, and update affected docs in the same commit as the code change that invalidates them. Added a new "Documentation trust policy" subsection under "Hard rules" capturing the second rule explicitly. Reworded the previous (also added today) `docs/SETUP.md` and `docs/USER_GUIDE.md` pointers in the project-at-a-glance section so they no longer treat those files as authoritative; instead they tell the agent to run a real `dotnet build` for setup and read the actual view code for UX questions. Reworded the corresponding `MemoryLimitMB` open-question entry the same way. Same updates applied to `.github/copilot-instructions.md` so a fresh agent reads them before even reaching this file. Author: Copilot agent session.
- **2026-04-24** — Hardened the maintenance rule itself in response to user audit. Root cause of the B26 miss: the previous "UPDATE this file when ANY of the following happens" wording described triggers as outcomes evaluated AFTER the commit, with no pre-commit checkpoint and no explicit "introduces a new architectural fact" trigger to balance the existing "discovers" trigger. Three changes: (1) Replaced the soft trigger list with a numbered nine-item **Pre-commit checklist** at the top of the file, framed as "before running git commit, walk through these questions" so the audit happens BEFORE the commit lands rather than after. Each item is a concrete yes/no question; question 1 explicitly names the categories the agent has previously rationalized away (new public API on a Core type, new cross-project constant, new env var, new build constant, new partial-class seam, etc). Added a "Hidden failure mode" subsection naming the specific bad reasoning ("this commit only changes code, not docs") that caused the B26 miss, citing commit `df0df53` as the catch-up. (2) Mirrored the rule into `.github/copilot-instructions.md` so a fresh session sees it before even reaching this file: top-of-file UPDATE summary now includes "introduced" alongside "discovered" and explicitly mentions B-numbered commits / new public APIs as triggers; Git commit policy section now starts with an "audit AGENT_CONTEXT before committing" bullet that references the pre-commit checklist by name and warns about the exact bad reasoning that caused the B26 miss. (3) Renamed the file's old "Edit policy" section to "Edit policy (style)" to make clear it covers formatting only, while the load-bearing rule lives in the Pre-commit checklist. No change to the substance of any other section. Author: Copilot agent session.
- **2026-04-24 to 2026-04-27 (archived)** -- Maintenance entries for B26-B63 (W1 follow-ups, B27 benchmark re-baseline, B28-B29 startup-pane and hardware-aware memory defaults, B30-B38 memory-budget accounting, B41-B48 build hygiene and integrity-check stack, B50-B51 quarantine + rebuild-from-quarantined-catalog, B52-B56 W3 memory-residency tightening phases A-F, B59 W4 LiteDB-seam collapse, B62-B63 restore-stall fixes) were collapsed to keep this file readable for fresh-session agents. The load-bearing lessons and DO-NOT clauses from those commits live in architectural facts #9-#31 above; the per-commit narratives are recoverable via `git log` / `git show` for any specific B-number. Author: Copilot agent session.
Added a new W5 active-workstream entry with four numbered phases (Phase 1 measurement infrastructure, Phase 2 user-driven baseline capture, Phase 3 BudgetedMemoryPool, Phase 4 native chunk buffers), and shipped Phase 1 in this commit.
- **2026-04-28** -- W5 Phase 1 cross-process fix attempt (B65, RETRACTED 2026-05-18). User ran the first B64 multi-hour benchmark and produced `AzureBackup.Benchmarks.MemoryBudgetBenchmark-report.csv` with all 20 cells filled for `Allocated` (174.66 GB on the large-chunks workload, etc.) but every one of the six new fidelity columns (`PeakWS_MB`, `MaxUnacc_MB`, `PeakBudget_MB`, `Overshoot`, `Stalls/GB`, `Oversized`) rendered as `-`. Root cause: BDN's default toolchain forks a child process per benchmark method to isolate measurement; the `[mem]` line stream that `MemoryFidelityCollector` parses arrived in the CHILD process but the `IColumnProvider.GetValue` projection runs in the HOST process AFTER the child exits. `MemoryDiagnoser`'s `Allocated` column survives because BDN gives it explicit cross-process marshalling; arbitrary in-memory singletons do not. **Fix attempted:** env-var seam (`AZBK_BENCH_FIDELITY_DIR`) plus JSON-lines persistence path published from `Program.Main`. **Retracted by B66 2026-05-18 after the user's next benchmark run produced NO samples directory at all under `%TEMP%`, proving the seam was defeated.** Either `Program.Main` was bypassed by the launcher (Visual Studio 2026 benchmark integration, BDN re-launch, `dotnet test`) or `Environment.SetEnvironmentVariable` after process start did not propagate to BDN's child. The earlier "env-var propagation smoke test" was tautological -- it ran `--list flat`, which does not spawn a benchmark child, so the `[B64]` line in the output came from the host's own stdout, not from a child confirming inheritance. Lesson captured at the time was correct (a custom BDN telemetry path needs cross-process state transfer), but the chosen mechanism (env-var + Program.Main publication) was structurally fragile. See the B66 entry below for the actual fix. Author: Copilot agent session.
- **2026-05-18** -- W5 Phase 1 cross-process fix (B66). After B65 shipped, the user ran `MemoryBudgetBenchmark` again and got the same `-`-every-fidelity-column CSV. Forensic inspection of `%TEMP%` showed two `azbk-bench-fidelity-*` dirs from 10:51 AM smoke tests (both empty -- they were `--list flat` runs that never spawn a benchmark child) and ZERO dirs from the 6:10 PM benchmark. The Release binary timestamp (10:51 AM) ruled out a stale-build hypothesis -- the B65 code was definitely in the executing DLL. The only remaining explanations were (a) `Program.Main` was bypassed by the launcher so the env-var publication never ran, or (b) `Environment.SetEnvironmentVariable` after process start did not propagate to BDN's child. Either way the env-var seam is structurally unsuited for cross-process state transfer in a benchmark process. **B66 fix:** retract the env-var entirely. New `MemoryFidelityCollector.SamplesDirectory` static property is the absolute path of a `.fidelity-samples/` folder next to `MemoryFidelityCollector.cs`, baked into IL via the `[CallerFilePath]` parameter of a private `ResolveSamplesDirectory` helper. Both the BDN host process and every BDN child process resolve to the SAME absolute path with zero runtime state, env-var inheritance, or `Program.Main` cooperation. `EndIteration` writes one JSON line per iteration to `<SamplesDirectory>/samples-pid<ProcessId>.jsonl` so concurrent host + children write without `File.AppendAllText` lock contention or partial-line interleaving; `LoadPersistedResults` enumerates every `samples-pid*.jsonl` and aggregates. Every persist and every load emits a visible `[B66 fidelity persist]` / `[B66 fidelity load]` line to `Console.Error` with PID, path, and the record key (or the exception type and message on failure); the silent `catch {}` was the worst part of B65 and is gone. `Program.Main` now runs `CleanFidelitySamplesDirectory()` before the self-checks, runs the persistence round-trip self-check against the new path, and prints `[B66] Memory-fidelity samples directory: <path>` so the user can locate the JSONL after a run. `.gitignore` patched for `benchmarks/AzureBackup.Benchmarks/.fidelity-samples/`. End-to-end proof captured at fix time: launching `dotnet exec AzureBackup.Benchmarks.dll --list flat` (a launcher path that bypasses `dotnet run` and therefore bypasses our `Main`) still resolved the exact same IL-baked absolute path, wrote a JSON line, and the host loaded it back -- the scenario B65 silently failed on now works. Build clean, 907/907 tests pass. Architectural fact #32 retitled `(B64, B65, B66)` with the B65-retracted-by-B66 explanation and the new compile-time-path DO-NOT clauses (do not rename or move `MemoryFidelityCollector.cs`, do not collapse per-PID files into one, do not silently swallow persistence exceptions, do not skip the host-side cleanup). W5 Phase 1 active-workstream status line rewritten to credit B64 with the scaffold, B65 with the retracted attempt, and B66 with the structural fix. The B65 row stays in the recent-commit-history table flagged `(RETRACTED BY B66)` so the forensic chain stays visible to future agents. `docs/USER_GUIDE.md` and `docs/SETUP.md` reviewed per the verified-current-docs maintenance protocol; neither requires update because the change is benchmark-only and the user-visible memory-limit contract is unchanged. Lessons: (1) when a runtime mechanism depends on launcher cooperation (entry-point execution, env-var inheritance, process-start hooks), a compile-time IL constant is strictly more robust than any runtime publication scheme. `[CallerFilePath]` is purpose-built for this: it gives every loaded copy of the assembly the same path with no cooperation from anyone. (2) A smoke test that does not exercise the actual failure surface is worse than no smoke test, because it converts "unknown" into a false "confirmed working". A real cross-process smoke test must spawn an independent process via the same launcher BDN uses, write something from the child, and read it back from the host. The B65 smoke test ran `--list flat`, which never spawns a benchmark child; the agent should have caught this at design time instead of declaring B65 verified. (3) Silently swallowing exceptions inside a write-from-event-handler path makes future regressions undebuggable; surface failures to `Console.Error` (or any sink the user will actually see during a benchmark run) even at the cost of a noisier log. Author: Copilot agent session.
- **2026-05-22** -- W5 Phase 3 Commit 2 (B70) merged `BudgetedMemoryPool` (B69) and `LargeChunkBufferPool` (B37) into one parameterized `ChunkBufferPool` after the user asked for a frank assessment of why two near-identical recycler classes existed and whether merging them was wise. Source inspection confirmed the two classes were byte-for-byte equivalent except for the bucket-size array; every B52 / B56 / B69 tuning change had to be made twice. Merge shape: one type with a public `int[] bucketSizes` constructor parameter, two well-known static `int[]` geometries (`SmallChunkBucketSizes`, `LargeChunkBucketSizes`), production cap / peak / zero-on-return contracts inherited verbatim. `ChunkPayload` collapsed `LargeChunkPool` + `SmallChunkPool` into one `ChunkBufferPool? BufferPool`; `ChunkingService`, `BackupOrchestrator`, `BackupOrchestrator.Operations.cs`, and `BackupMemoryReporter` migrated; the user-facing `lohPool=...` / `smPool=...` `[mem]` log suffixes preserved so existing log-scraping is unaffected. Tests: two pre-B70 pool-specific files (`LargeChunkBufferPoolTests.cs`, `BudgetedMemoryPoolTests.cs`) deleted and replaced by one parameterized `ChunkBufferPoolTests.cs` whose every behavioural assertion runs as a `[Theory]` over both geometries plus four B70-specific constructor-validation tests; 941 / 941 pass (+6 vs B69 from the parameterized fan-out). Architectural fact #32 rewritten to describe the unified pool (the original B69 fact #32 is replaced; the same load-bearing contracts are preserved). Fact #24 prefixed with a B70 rename note so its B52 cap-policy reasoning still applies. W5 Phase 3 Commit 2 status updated; Phase 3 Commit 3 (restore-side small-buffer call sites) noted as the remaining open work for the backup/restore memory-honesty redesign. The unified `ChunkBufferPool` is now the single source of truth for chunk-buffer recycling at every operation-scope call site; all future cap / peak tuning lands once and applies to both geometries automatically. `docs/USER_GUIDE.md` and `docs/SETUP.md` reviewed per the verified-current-docs maintenance protocol; neither requires update because the change is internal refactor and the user-visible memory-limit contract is unchanged. Lessons: (1) two classes with identical behavior but different config are usually a single class with parameterized geometry, and the duplication tax compounds quickly as every cap-policy change has to land in both; (2) collapsing the ownership-return contract from two type-specific fields to one abstract pool reference simplifies the consumer's dispatch ladder without losing any safety; (3) when retiring a class, deleting its dedicated test file in favor of theory-based tests over the unified replacement preserves coverage AND avoids the awkward intermediate state where two test files assert the same contract against two near-identical types. Author: Copilot agent session.
- **2026-05-19** -- W5 Phase 1 benchmark-name resolution fix (B67).
- **2026-05-21** -- W5 Phase 1 self-check assembly-isolation fix (B68). The user ran `dotnet run -c Release --project benchmarks\AzureBackup.Benchmarks -- --filter *MemoryBudget*` on the remote PC right after B67 landed and got `* Benchmarked method `Backup` is within a sealed class, Declaring type must be unsealed.` AND `* Benchmarked method `Backup` is within a non-visible class, all declaring types must be public.` against `Runnable_999_SelfCheckOnly.Backup`, which blocked every matched real benchmark. Diagnosis: the B67-era synthetic wrapper was a `private sealed class Runnable_999_SelfCheckOnly : MemoryBudgetBenchmark { }` nested in `Program.cs`. `BenchmarkSwitcher.FromAssembly(typeof(Program).Assembly).Run(args)` enumerates EVERY type in the entry assembly -- public AND non-public, nested AND top-level -- looking for `[Benchmark]`-decorated methods, and the nested wrapper inherited `Backup` from its base. BDN's validator then correctly rejected the candidate. The original B67 source comment claiming BDN ignored non-public types was wrong; the original B67 validation only ran `--filter NoSuchBenchmarkExistsXYZ`, which exits BDN before the validator runs on any candidate, so the regression was masked at validation time. B68 fix: move the synthetic wrapper out of the entry assembly entirely via `Reflection.Emit`. New `Program.BuildSyntheticRunnableWrapperType(Type parent)` defines a transient `AssemblyBuilder("AzureBackup.Benchmarks.SelfCheck.Dynamic", AssemblyBuilderAccess.Run)`, defines a module, defines a `NotPublic | Sealed` type named `Runnable_999_SelfCheckOnly` whose `BaseType` is `parent`, and returns the created `Type`; the wrapper now lives in a separate `AssemblyBuilder` instance that BDN's discovery scan never visits. New `Program.VerifyNoStaticRunnableShimInEntryAssembly()` runs at host startup, walks every type in the entry assembly via `Assembly.GetTypes()`, and throws `InvalidOperationException` on any `Name.StartsWith("Runnable_")` so a future contributor who tries to re-add the static wrapper hard-fails in sub-second host startup rather than during a multi-hour benchmark run. Validated by switching from the masking filter to `--list flat --filter *MemoryBudget*`, which actually invokes BDN's validator on every matched case before listing them; post-fix the output is exactly one line, `AzureBackup.Benchmarks.MemoryBudgetBenchmark.Backup`, with no `Runnable_*` leak. Re-ran `--filter NoSuchBenchmarkExistsXYZ` to confirm all four startup self-checks (parser contract, name-resolution contract via dynamic-assembly wrapper, no-static-Runnable scan, persistence round-trip) still execute cleanly. Architectural fact #32 retitled `(B64, B65, B66, B67, B68)` and amended with three new DO-NOT clauses; W5 Phase 1 active-workstream status line rewritten to credit B68; recent-commit-history table updated with the B68 row; completed-workstreams section gained the B68 entry above B67. `docs/USER_GUIDE.md` and `docs/SETUP.md` reviewed per the verified-current-docs maintenance protocol; neither requires update because the change is benchmark-only and the user-visible memory-limit contract is unchanged. Lessons: (1) `BenchmarkSwitcher.FromAssembly` discovers non-public and nested types, so visibility is not a safe way to keep test fixtures out of the benchmark discovery surface; any candidate with an inherited `[Benchmark]` method must satisfy BDN's validator (public, not sealed) regardless of its declared visibility. (2) A no-match filter is the wrong tool for validating discovery-time changes because BDN exits before the validator runs; `--list flat` (or a real benchmark filter) is the cheapest validation that actually invokes the validator on every matched candidate. (3) Architectural guardrails that depend on "do not write code shaped like X" are strictly weaker than guardrails that prove "code shaped like X does not exist" at startup; when a discovery scan is the failure surface, the matching defence is a startup-time scan that enumerates the exact same surface. Author: Copilot agent session.
- **2026-05-22** -- W5 Phase 2 baseline import (user-driven, no B-number) followed by W5 Phase 3 Commit 1 (B69). The user ran `MemoryBudgetBenchmark` on a separate host and `git pull`ed `BenchmarkDotNet.Artifacts/results/AzureBackup.Benchmarks.MemoryBudgetBenchmark-report.csv` into this clone; that file is now the canonical Phase 2 baseline against which Phase 3 will be re-compared. The user then directed the agent to start Phase 3 and to prune AGENT_CONTEXT clutter before recording the new entry. B26-B63 maintenance entries were collapsed into a single archived line above; the load-bearing facts from those commits live in architectural facts #9-#31 and are recoverable per-commit via `git log` / `git show`. B69 itself: added `BudgetedMemoryPool` (small-chunk 64 KB-16 MB recycler mirroring `LargeChunkBufferPool` contract), extended `ChunkPayload.SmallChunkPool`, rewired `ChunkingService.AcquireChunkBufferAsync` / `ReleaseChunkBuffer` to route through it on the small-chunk path with a pool-aware bucket-ceiling budget charge, added `BackupOrchestrator.BackupFileAsync` overloads + `ComputeSmallPoolCapBytes` (12.5 percent of budget, 64 MB floor), wired operation-scope ownership through `BackupOrchestrator.Operations.cs`, and extended `BackupMemoryReporter` to subtract the new pool from `unaccounted` and emit a `smPool=...` suffix. `replace_string_in_file` did not cleanly prune the maintenance log on the first attempt -- on user instruction the agent used a multi-line PowerShell script to rewrite the file (one-shot exception to the single-line PowerShell rule for AGENT_CONTEXT.md edits only). Tests: 935 of 935 pass (+28 new `BudgetedMemoryPoolTests`). Build: 0 warnings, 0 errors. `docs/USER_GUIDE.md` reviewed -- the small-chunk pool is purely internal and the user-visible memory-limit contract is unchanged. `docs/SETUP.md` reviewed -- no new build flag, runtime identifier, package, schema column, encryption-envelope change, or `AppMode.DataDirectory` resolution change. Restore-side small-buffer call sites are NOT in scope and remain on `ArrayPool<byte>.Shared` for W5 Phase 3 Commit 2. Author: Copilot agent session.
W5 Phase 3 status updated to mark Commit 3 done; the workstream is now gated on the user re-running Phase 1's MemoryBudgetBenchmark and adversarial workloads so the post-B71 fidelity numbers can be compared against the imported Phase 2 baseline before any Phase 4 work begins.
Workstream is now gated on the user re-running MemoryBudgetBenchmark so the post-B72 fidelity columns can be compared against the post-B71 baseline before any further Phase 4 (native chunk buffers) work begins.
Workstream is now gated on the user re-running MemoryBudgetBenchmark so the post-B73 fidelity columns can be compared against the post-B72 baseline.
- **2026-05-27** -- W5 Phase 4 Commit 3 (B74). User reviewed the post-B73 benchmark and asked the agent to dig into the specific anomalies discovered. Investigation surfaced three real defects: (A) BackupMemoryReporter's unaccounted formula was double-subtracting pool cache because B72 added the pool retention to MemoryBudget.UsedBytes via ChargeRetention, but the pre-B74 formula still subtracted pool.TotalBytesCached on top of that; (B) ChunkingService.AcquireChunkBufferAsync's encrypt-buffer pre-charge used NextPowerOfTwoOrSelf which matches ArrayPool's power-of-two tier shape but under-charges by up to 2x against the small ChunkBufferPool's [64K, 256K, 1M, 4M, 16M] bucket geometry B73 introduced (worst case: 4 MB plaintext encrypts to ~4 MB + 37, small pool rents at 16 MB, pre-B74 charge computed 8 MB, an 8 MB per-chunk under-charge); (C) B73's encrypted-buffer routing through ChunkBufferPool swapped GC-evictable ArrayPool for never-evicting pool, which pinned large encrypted buffers at the high-water mark instead of letting Gen2GcCallback trim them between gen-2 collections, causing the +3.2 GB MaxUnacc regression on adver-large-chunks/16384 and the +3.3/+2.8 GB regressions on adversarial-mixed/8-16 GB. User chose A + B + C2 in one commit cluster, prioritised around C2 because the real-world workload is backup + download of files over 500 MB which routes through LargeFileConfig. Implementation: Fix A extracted the unaccounted formula into BackupMemoryReporter.ComputeUnaccountedBytes(workingSet, budgetUsedBytes, poolBudgetChargedBytes, largePoolCachedBytes, smallPoolCachedBytes) returning long; the helper computes usedExcludingPoolRetention = max(0, budgetUsedBytes - poolBudgetChargedBytes) then max(0, workingSet - usedExcludingPoolRetention - large - small), so when no pool is wired both pool inputs are 0 and the formula reduces to the pre-B72 shape (workingSet - used). Fix B changed the encryptCharge math from NextPowerOfTwoOrSelf(encryptedPayloadLen) to a conditional that picks BucketCeiling(encryptedPayloadLen, smallChunkPool) when encryptedPayloadLen < PoolSkipThresholdBytes AND smallChunkPool != null, NextPowerOfTwoOrSelf otherwise; the conditional is correct because Fix C2 ensures the consumer-side rent for encryptedPayloadLen >= PoolSkipThresholdBytes always goes through ArrayPool (large pool encrypted routing is gone), and the no-pool fallback already goes through ArrayPool. Fix C2 changed both BackupOrchestrator.BackupFileAsync's upload dispatch and RestoreService.RestoreFileAsync's download dispatch from ncryptedSize >= PoolSkipThresholdBytes ? largeChunkPool : smallChunkPool to ncryptedSize >= PoolSkipThresholdBytes ? null : smallChunkPool. The B73 IBlobStorageService overloads + AzureBlobService implementation overloads + RentEncryptedBuffer/ReturnEncryptedBuffer helpers are kept verbatim -- the change is purely in the caller's pool selection; passing null routes the helpers back to ArrayPool<byte>.Shared with clearArray:true on Return exactly as pre-B73. To enable the Fix B contract test, BucketCeiling and NextPowerOfTwoOrSelf were promoted from private static to internal static with xmldoc remarks explaining the visibility bump is purely for testability and the method bodies are unchanged. Tests: 8 new EncryptChargeBucketCeilingTests (one BucketCeilingMatchesActualSmallPoolRent theory with 8 boundary sizes the production workloads exercise from 64K+1 through 16M-overhead-1, plus NextPow2 contract tests, plus a SmallPoolBucketCeilingExceedsNextPow2InGapZones theory that directly quantifies the pre-B74 bug shape with expected bucket vs expected NextPow2 values so a future regression fails with a clear delta), 3 new BackupMemoryReporterTests (UnaccountedDoesNotDoubleSubtractPoolRetentionAfterB72 pins the corrected formula against a constructed workingSet=100 MB / used=30 MB / poolCharge=20 MB / largeCache=20 MB scenario expecting 70 MB unaccounted vs the pre-B74 50 MB; UnaccountedFormulaIsBackwardsCompatibleWhenNoPoolWired pins the no-pool reduction; UnaccountedFormulaClampsToZero pins the negative-clamp; UnaccountedFormulaHandlesPoolBudgetChargeGreaterThanUsed pins the defensive race-clamp). Full suite: 978 of 978 pass (+21 vs B73). Build: 0 warnings, 0 errors. Architectural fact #36 added with four DO-NOT clauses including the critical NOT-re-introducing-the-large-pool-routing-without-Gen2GcCallback-wiring guard and the no-swapping-small-pool-to-ArrayPool guard. Fact #35 received a B74-SUPERSEDES preamble pointing readers at fact #36 for the current contract. W5 Phase 4 status updated to mark Commit 3 done with all three fixes and the expected post-B74 benchmark shift described. docs/USER_GUIDE.md reviewed -- the three fixes are internal residency wiring; the user-visible memory-limit contract is unchanged. docs/SETUP.md reviewed -- no new build flag, runtime identifier, package, schema column, encryption-envelope change, or AppMode.DataDirectory resolution change. Lessons: (1) when a behaviour change inserts itself BEFORE a downstream subtraction in a formula, audit every formula that subtracts the same quantity -- B72 added pool cache to budget.UsedBytes but I missed that BackupMemoryReporter's unaccounted line was still subtracting it explicitly; (2) when two systems use the same nominal partition (encrypted size vs PoolSkipThresholdBytes) they had better also use the same bucket geometry -- ArrayPool's power-of-two tiers and ChunkBufferPool's [64K, 256K, 1M, 4M, 16M] geometry are NOT interchangeable for budget accounting, and a future change that adjusts either side needs to re-audit the other; (3) GC-pressure-driven eviction is a load-bearing implicit contract for any pool whose buffers are part of the steady-state working set -- swapping a gen-2-trimmed pool for a never-evict pool will move bytes from 'decays naturally' to 'pinned at high-water mark' even if both pools have the same nominal cap; (4) extracting a formula into a pure helper is a recurring pattern for testability when the original lives next to noisy OS APIs (Process.WorkingSet64, Stopwatch) -- the BackupMemoryReporter.ComputeUnaccountedBytes seam is the third instance of this pattern in this codebase (after EstimateUploadStagingBytes and BucketCeiling). Author: Copilot agent session.
- **2026-05-27** -- B75 (post-B74 same-session add-on). User asked, BEFORE re-running the benchmark, whether the benchmark could be updated to output the relevant portions of the log file to the same folder as the benchmark results so the data could be overwritten and checked into the repo after each run the same way the CSV file is. Investigation: the timestamped benchmarks/*.log file (130 KB on the memory benchmark) is produced manually by the user piping dotnet run output to a file (nothing in the project generates it), is gitignored by the repo's *.log rule, and is awkward to diff because it contains BDN validation messages + toolchain restore output + every periodic [mem] sample. The actually-useful per-run data lives in the IEnumerable<Summary> that BenchmarkSwitcher.Run(args) returns at end-of-run: BenchmarkCases for parameter shape, Reports[i].ResultStatistics for Mean/StdErr/StdDev/N, and BenchmarkCase.Config.GetColumnProviders() which already includes the MemoryFidelityColumnProvider that drives the CSV's PeakWS_MB/MaxUnacc_MB/PeakBudget_MB/Overshoot/StallsGB/Oversized columns. Implementation: new benchmarks/AzureBackup.Benchmarks/BenchmarkSummaryWriter.cs exposes a tiny static API (public static IReadOnlyList<string> WriteAll(IEnumerable<Summary>, string outputDirectory), public static string BuildMarkdown(Summary), public const string SummaryFileSuffix = '-summary.md'). The writer emits one {BenchmarkType}-summary.md per Summary into the supplied directory (Program.Main passes BenchmarkDotNet.Artifacts/results so the summary lands next to the BDN-generated -report.csv/-report.html/-report-github.md). Markdown content: H1 title, generated-at timestamp, case count, aggregated wall-clock estimate (sum of per-case Mean times N from ResultStatistics), summary title, host info (RuntimeVersion + Os.Value), per-case table with parameter columns + Mean + StdErr + StdDev + Allocated + every Custom column the configured column providers contribute (automatically picks up the fidelity columns for the memory benchmark and adds nothing for benchmarks without a custom provider), and a Noisy iterations section that lists every case with StdErr/Mean > 5%. The writer NEVER throws out of WriteAll: a top-level try/catch surfaces failures on Console.Error and returns an empty list rather than tearing down a multi-hour benchmark with no surviving artifacts; each per-summary write is also wrapped so a single bad summary cannot prevent the rest from landing. Wiring: Program.Main captured the return value of BenchmarkSwitcher.Run, computed resultsDir = Path.Combine(GetCurrentDirectory, BenchmarkDotNet.Artifacts, results), and forwarded the summaries to WriteAll, logging each written path with a [B75] prefix. To enable the format-shape unit tests without depending on a real BDN Summary (the ctor takes BenchmarkReports requiring full BenchmarkCase + Descriptor + Job which would be enormous to mock), the FormatNanos / FormatBytes / FormatDuration helpers were promoted from private static to internal static, the benchmarks csproj gained InternalsVisibleTo Include='AzureBackup.Tests', and the tests csproj gained a metadata-only ProjectReference to the benchmarks project (nothing in the test assembly invokes a benchmark or pulls the BDN runtime past load time). Hit one MSBuild gotcha during the csproj edit: an XML comment cannot contain '--' or end with '-' (MSB4025), so the comment had to be rewritten without the em-dash style this repo uses elsewhere. Tests: 25 new BenchmarkSummaryWriterTests pin every format-shape branch including the null/zero/negative/NaN sentinels for FormatNanos (returns '-'), the byte-size unit thresholds (B/KB/MB/GB) for FormatBytes, the second/minute/hour formats for FormatDuration, the empty-input + invalid-directory + null-input + empty-directory + whitespace-directory branches of WriteAll, and a pin on the SummaryFileSuffix constant (load-bearing because '-summary.md' sorts the summary next to the BDN-generated '-report.csv' / '-report.html' / '-report-github.md' in a directory listing and renders natively in GitHub PR views). Full suite: 1003 of 1003 pass (+25 vs B74). Build: 0 warnings, 0 errors. Architectural fact #37 added with four DO-NOT clauses; W4 ('## Completed workstreams (recent)') updated; commit-history table updated. docs/USER_GUIDE.md reviewed -- B75 is dev-only tooling, no user-visible surface. docs/SETUP.md reviewed -- the suffix change is documented in fact #37's first DO-NOT but does not need to land in SETUP.md until a future change actually touches build flags / package versions / artifact layout. .gitignore reviewed -- the new file at BenchmarkDotNet.Artifacts/results/*-summary.md is NOT covered by the existing *.log rule, so the user can git add it normally; if the entire BenchmarkDotNet.Artifacts directory is ever gitignored in the future, the rule will need a !*-summary.md exception. Lessons: (1) the right shape for 'capture relevant portions of a verbose log' is almost always 're-derive from the in-memory model that produced the log' rather than 'parse the log file' -- the Summary object had everything I needed and 1000x less ambiguity than regexes against the BDN text format; (2) when adding a post-run hook to a benchmark driver, the swallow-and-log-to-stderr pattern is mandatory -- the alternative ('let it propagate so users see the bug') silently loses every artifact from a multi-hour run, which is the worst possible failure mode; (3) BDN's Summary surface uses ImmutableArray<T> for BenchmarksCases and Reports, NOT a nullable List, so the conditional-access operator (?.) does not compile -- a future contributor adding more Summary access needs to remember Length > 0 / [0] / FirstOrDefault() instead. Author: Copilot agent session.
