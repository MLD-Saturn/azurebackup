# AGENT_CONTEXT.md — Handoff for Copilot Sessions

This file is the **persistent memory** for Copilot agent sessions on the AzureBackup repo. It is checked into source control so the agent can read it on a fresh chat (on **any machine, any OS-supported environment**) and immediately understand the project state, conventions, recent decisions, and open questions.

## How to use this file

**At the start of every fresh Copilot chat:** read this file before doing anything else. Treat the "Active workstreams" section as a continuation of the previous session.

### Pre-commit checklist (run this immediately before every `git commit`)

This section is the load-bearing rule of the file. **The agent has demonstrably skipped it before.** When that happens the file silently drifts and the next session inherits a lie. Treat it as non-optional.

Before running `git add .; git commit -m "..."`, walk through these questions in order. If the answer to any is "yes", the commit MUST also include an AGENT_CONTEXT.md edit (and frequently a `docs/USER_GUIDE.md` or `docs/SETUP.md` edit per the Documentation trust policy).

1. **Did the change introduce a new architectural fact?** New `public` API on a `Core` type, new cross-project constant, new env var read at startup, new build-time `DefineConstants`, new file-format magic number / version byte, new partial-class seam, new singleton, new background task, new on-disk file location, new lock or shared-state contract. → Add an entry to the "Architectural facts to load into working memory" list. Do not assume "it's just a refactor" — a public API / public constant / cross-boundary contract IS a fact.
2. **Did the change introduce or remove a hard-coded duplicate of a value used in more than one project?** → Add a fact recording the canonical location and a "do not introduce a new copy" warning. (See fact #9 for the template.)
3. **Did the change land a commit with a B-number?** → Add a row to the "Recent commit history" table. Add an entry to "Completed workstreams". If the commit consumed a B-number that was pre-allocated to a different planned task in "Active workstreams", renumber the planned task and add a one-line note explaining the renumbering.
4. **Did the change resolve, block, or pivot any workstream?** → Update the corresponding W-numbered section. If a workstream is now done, move it to "Completed workstreams".
5. **Did the change establish or change a user preference, coding convention, or workflow rule?** → Add or update the corresponding entry under "Hard rules".
6. **Did the change discover a non-obvious production behavior** (memory, concurrency, persistence, network, encryption)? → Add a fact.
7. **Did the change produce a benchmark number that affects a design decision?** → Update the relevant benchmark file's xmldoc table (the source of truth) AND add a one-line summary in the relevant W-numbered section. Do not paste the full numbers here.
8. **Did the change teach a "do not do this" lesson?** → Add a fact, with the word "NEVER" or "DO NOT" so a future session greps for it.
9. **Did this session touch AGENT_CONTEXT.md at all?** → Add a dated bullet to the "Maintenance log for THIS file" at the bottom summarizing what changed in the file. One bullet per session, not per edit.

If the answer to all nine is "no", commit without touching this file. That is the only acceptable reason to skip the update.

### Hidden failure mode that has happened before

The trap is reasoning "this commit only changes code, it doesn't change documentation, so AGENT_CONTEXT doesn't need an update." That logic is wrong because **AGENT_CONTEXT documents the code, not the documentation**. A code commit that adds a new public constant is exactly the trigger for question 1 above. Commit `b9d5744` (B26) hit this trap; the catch-up was commit `df0df53` and cost the user an extra prompt to ask "did you remember the file?". Do not repeat it.

### Implausibility-prompts-verification (added 2026-04-27 after B43)

When a user reports a measurement, log line, or behaviour whose magnitude looks *implausible* given the documented design (e.g. "throughput shows 6 Mbps on gigabit Azure", "memory used reads 41 GB on a 32 GB host", "auto-repair reset to zero after restart"), do NOT accept it at face value AND do NOT immediately blame the user's environment. Treat implausibility as a prompt to verify the LABEL against the FORMULA at every producer in the codebase before tuning anything. The B43 throughput rename was the textbook case: production logs showed values that looked like single-digit Mbps for a gigabit pipeline, but the on-disk number was actually MB/s -- a factor-of-8 label error that would have wasted a network-perf investigation. The check costs one `code_search` for the field name; the cost of skipping it is days of misdirected debugging.

### Edit policy (style)

- Keep entries dated (YYYY-MM-DD).
- Move completed workstreams to the "Completed workstreams" section near the bottom.
- Keep the file under ~600 lines so a fresh agent can read it in one tool call.
- **Never** record machine-specific values (free disk, CPU model, core count, terminal-tool timeouts, exact wall-clock times, OS-edition build numbers). Those mislead the next session if it is on a different PC. Record *relative* findings (deltas, percentages, comparisons) and *workload* identity (which benchmark / which workload name produced the number) instead.

**Anti-pattern:** do not duplicate code or full xmldoc into this file. Reference file paths, commit hashes, and benchmark file xmldoc tables instead.

---

## Project at a glance

- **Name:** AzureBackup — a cross-platform desktop application that backs up local files to Azure Blob Storage with client-side encryption and content-defined chunking for deduplication. Runs on Windows, macOS, and Linux.
- **Language/runtime:** C# on .NET 10 (`net10.0` TFM).
- **Solution:** `azurebackup.sln` at repo root.
- **Project layout:**
  - `src/AzureBackup.Core/` — all backup/restore/encryption/database logic. Cross-platform-clean C# library; no UI dependencies. **All non-trivial logic lives here.**
  - `src/AzureBackup/` — **Avalonia 11.3.12** desktop UI shell (`MainWindowViewModel` + views). Targets `net10.0` (no `-windows` suffix), so it builds and runs on Windows, macOS, and Linux. Verify with `Select-String Avalonia src/AzureBackup/AzureBackup.csproj` if in doubt.
  - `tests/AzureBackup.Tests/` — xUnit. As of B25-bench-2: 759 tests, all passing.
  - `benchmarks/AzureBackup.Benchmarks/` — BenchmarkDotNet 0.15.8. Release-only, never run in CI. See "Running tests and benchmarks" below.
- **Setup, build, publish, file locations:** `docs/SETUP.md` is current and authoritative as of the most recent commit that touched it. If you change build flags, target frameworks, runtime identifiers, the `portable.marker` mechanism, the `AppMode.DataDirectory` resolution, or the encryption/chunking parameters listed in its "Technical specifications" table, you MUST update `docs/SETUP.md` in the same commit.
- **End-user UX (views, settings, menus, dialogs):** `docs/USER_GUIDE.md` is current and authoritative as of the most recent commit that touched it. If you add or rename a view (`*.axaml` under `src/AzureBackup/Views/`), add or rename a button/toggle/setting, change a default value visible to the user, or change a user-visible workflow, you MUST update `docs/USER_GUIDE.md` in the same commit.
- **Database backend:** SQLCipher-encrypted SQLite (production default since commit `63103b1` = `C-5`). Legacy LiteDB code path still present for migration only.
- **Authentication:** Argon2id KDF (64 MB, 8 lanes, 3 iterations) for both database key derivation and the `EncryptionService` content key.
- **Encryption envelope:** AES-256-GCM with `[magic(4) | version(1) | nonce(12) | ciphertext(N) | tag(16) | crc32(4)]`. `EncryptionService.EncryptionOverhead = 37`.
- **Chunking:** content-defined (Rabin-style rolling hash, window=48, prime=31). Per-extension chunk-size config (`.mkv` uses 1-64 MB, `.txt` uses 16-128 KB, etc.). Files >500 MB use 16-128 MB chunks regardless of extension.

---

## Hard rules (USER PREFERENCES — never violate)

These come from `.github/copilot-instructions.md` and from explicit user instruction during session work. Reproduced here because they apply to every session.

### Documentation trust policy

**Treat every Markdown documentation file in this repo as stale until proven current** — with exceptions noted below.

**Verified-current docs (treat as authoritative, but maintain rigorously):**

- `docs/SETUP.md` — verified accurate against source on 2026-04-24. Covers Azure provisioning, build-from-source, single-file publish, portable mode (`portable.marker`), encryption envelope, per-extension chunking config, storage tiers, `AppMode.DataDirectory` paths, and a verified "Technical specifications" table.
- `docs/USER_GUIDE.md` — verified accurate against source on 2026-04-24. Covers every view (`SyncView`, `SettingsView`, `StorageHealthView`, `TierMigrationView`, `DataIntegrityView`, `LogsView`), the diagnostic-bundle export flow, the memory-limit slider, and all four storage tiers (Hot/Cool/Cold/Archive).
- `AGENT_CONTEXT.md` - verified accurate against source on 2026-04-27. Covers project layout, architectural facts, workstream state, and the conventions for commits, terminal commands, and tool usage that every future session MUST follow.

**Mandatory maintenance protocol for the verified-current docs:** every commit that touches one of the things they describe MUST update them in the same commit. Specifically:

- Adding/renaming a view file (`src/AzureBackup/Views/*.axaml`) → update `docs/USER_GUIDE.md` table of contents and the corresponding section.
- Adding/renaming a user-visible button, toggle, or setting → update `docs/USER_GUIDE.md`.
- Changing a default value the user can see (default storage tier, default container name, default exclusion patterns, default memory-limit state) → update `docs/USER_GUIDE.md`.
- Changing the encryption envelope, Argon2id parameters, chunk-size config, storage-tier set, or `AppMode.DataDirectory` resolution → update `docs/SETUP.md`.
- Changing build flags, target framework, runtime identifiers, the publish profile, or NuGet package versions called out in the technical-specifications table → update `docs/SETUP.md`.
- Adding or renaming a top-level menu, tab, or workflow step → update both docs as appropriate.
- **Renaming a serialized field, schema column, JSON key, log key, or any other on-disk / on-wire identifier** → update every doc that quotes the old name in the same commit. The grep target is the literal old token, not the prose around it; doc table cells that reference the old key are silent landmines until found.

**Label/formula consistency rule (added 2026-04-27 after B43).** Any persisted numeric field whose NAME implies a unit (e.g. `*_mbps`, `*_kib`, `*_us`, `*_per_sec`) MUST match the formula at every producer. The agent shipped `ThroughputMbps` for months while every producer divided by `1024 * 1024` (i.e. produced megabytes per second, MB/s) -- the label was wrong by a factor of 8 and silently corrupted every downstream throughput comparison. Do not trust the existing field name as evidence of the unit; trace the formula at the producer and the consumer separately and confirm they agree. When renaming the field, pin the JSON key explicitly via `[JsonPropertyName(...)]` so the configured naming policy cannot silently regress the key on a future rename.

When the doc and the code drift, the doc is wrong by definition; fix the doc, do not weaken the code to match. If a commit cannot reasonably update the doc in the same change (e.g. an emergency hotfix), record an observation in this file's maintenance log and open a follow-up.

For any other Markdown doc not on the verified-current list:

- Verify it against the actual code, project files, configuration, build output, or a fresh test run. Do not paraphrase a doc claim into a response without that verification.
- If the statement is wrong or out of date, update the doc file in the same commit as the code change that reveals the discrepancy. Do not leave a known-wrong doc file in place.

This file (`AGENT_CONTEXT.md`) is the one doc that future sessions are *required* to read first, so the bar for keeping it current is correspondingly higher: every commit that invalidates anything in it MUST update it in the same commit, and the maintenance log at the bottom must record the change.

### Terminal commands

- **Single physical line, always.** Chain with `;`. No here-strings, no backticks, no multi-line.
- The user's shell is **PowerShell 7+ (`pwsh.exe`)**. Other shells will be flagged immediately.
- Length is not a concern — correctness and single-line execution are.
- Occasionally a tooling display artifact mangles the leading characters of the command in the echoed output (e.g. `Set-Location` shown as `t-Location`). This indicates the command was not run through the terminal tool as intended. If you see this, stop and fix the command rather than proceeding with a broken one.

### Git commits

- Commit immediately at logical stopping points.
- Use `git add .; git commit -m "..."` chained on one line, with multiple `-m` flags (one per paragraph). **Never** write the message to a file. **Never** use `git commit -F`.
- **No emoji, no non-ASCII** in commit messages.
- Escape `"` and `\` properly so the command parses cleanly.
- **Never run `git push` under any circumstances.** Pushing is a manual step the user performs.
- **One logical change, one commit.** Do not split a single change across multiple commits to work around tool limitations (e.g. the placeholder-hash trailer pattern previously used for `35536b0`, `4364faf`, etc. is retired -- see hash policy below).

#### Commit message hygiene

Commit messages exist to explain the substance of the change to a reader who is browsing history months later. They are NOT a session-narration log, a process audit, or a checklist re-statement. Write them accordingly:

- **Describe what changed and why.** Code changes, the bug being fixed, the design decision being made, the new feature being introduced, performance numbers if relevant, the test count delta if it moved.
- **Do NOT mention things that did not change.** "docs/SETUP.md was checked, no update needed" is noise -- the absence of a docs/SETUP.md edit in the commit already conveys that. Same for any "I checked X and X was fine" line.
- **Do NOT restate rules that live in this file.** "Commit is local; per the never-push rule it will not be pushed" is implicit. "Pre-commit checklist items 1, 2, 4 did not fire" is process narration. "Per the verified-current-docs maintenance protocol" is a process reference. The reader already knows the rules; the commit message describes the change, not the process that produced it.
- **Do NOT refer to yourself as the agent.** Write in the impersonal third person about the change. "Added the guard" not "the agent added the guard". "Restructured the conditional allocation" not "I restructured". The author of the commit is recorded in the git metadata; it does not belong in the message body.
- **The same hygiene applies to AGENT_CONTEXT maintenance-log entries** -- they describe what changed in this file and the load-bearing reason, not the dialogue that led to the change. "User asked the agent to look at X" is the wrong shape; "X was changed because Y" is the right shape.

#### Commit hashes in `AGENT_CONTEXT.md`

The recent-commit-history table and Completed-workstreams entries reference commit hashes as a navigation aid. Because a commit cannot embed its own hash, follow this rule for new B-numbered commits:

- Add the new row to the table with a `<pending>` placeholder where the hash would go. Do the same for the matching Completed-workstreams entry.
- The next session that touches `AGENT_CONTEXT.md` for any other reason replaces any `<pending>` placeholders it finds with the actual hashes from `git log --oneline | Select-String 'B<n>:'`. This is a low-priority janitorial step, not a separate commit.
- **Never** commit a placeholder hash and then immediately follow up with a trailer commit whose only purpose is to fill in the hash. That pattern (used by `35536b0`, `4364faf`, `75ceaa9`, `d5fbc38`, `d3e60d4`) inflated the commit history with no substance and is retired.

### Tool usage

- Prefer `replace_string_in_file` / `multi_replace_string_in_file` over re-creating files. Re-create only when the file is small or being rewritten end-to-end.
- Read files before editing them.
- The `nuget_get-nuget-solver*` and `start_modernization` tools are NEVER appropriate for this project's typical work — do not invoke them unless the user explicitly asks for a package upgrade or .NET version change.
- Do not call `code_search` in parallel; do not call `run_command_in_terminal` in parallel.
- Long-running operations (benchmarks, large test sweeps) may exceed the terminal tool's timeout (whatever it is in the current environment). BenchmarkDotNet writes its result artifacts on each child-process completion, so even when the orchestrating call appears to time out you can usually find the artifacts under `BenchmarkDotNet.Artifacts/results/`. Always check there before assuming a benchmark didn't run.

---

## Architectural facts to load into working memory

These are non-obvious and have caused real bugs when forgotten.

1. **`SqliteBackend` is single-connection, single-threaded.** Post-B23 every read AND write goes through one `SQLiteConnection` serialized by a single `_writeLock`. Adding more file-level parallelism cannot help SqliteBackend throughput; it just adds contention. This is a known constraint of the chosen backend, not a bug.

2. **`FileOperationDiagnostics` (`.diag` files)** are written by 4 producers: `BackupOrchestrator.BackupFileAsync`, `RestoreService.RestoreFileAsync`, `RestoreService.Batch.cs` (CorruptedRecovery), `IntegrityCheckService.CheckOneFileAsync`. Post-B23 + B24, **all four only emit on real errors**. The `FlushAllLive` shutdown hook in `FileOperationDiagnostics` snapshots any diag still in the live `_live` registry at process exit — so the success/cancel paths in producers MUST call `Discard()` to deregister.

3. **`MemoryBudget` is a soft cap, configured via the WPF app's memory-limit slider** (`MemoryLimitEnabled` + `MemoryLimitMB` in the Configuration table). When `MemoryLimitEnabled=false` the budget is unlimited and the orchestrator can hold many GB of in-flight chunk buffers — confirmed by production telemetry showing multi-GB process working sets on large workloads with `memoryBudget=unlimited`. The dominant memory cost is `EffectiveMaxParallelChunkUploads (default 6) × per-chunk size (up to 64 MB for media) × EffectiveMaxParallelFileBackups (default 8)`.

4. **CPU is NOT the production bottleneck.** Production telemetry on a 7,313-file backup showed CPU usage well below 5%. The actual bottleneck is somewhere downstream: disk read throughput, the SqliteBackend write lock, or HttpClient/TLS state. Any "make this more parallel" hypothesis must be measured rather than reasoned about — the benchmarks under `benchmarks/AzureBackup.Benchmarks/` exist precisely for this.

5. **`InMemoryBlobService`** (`src/AzureBackup.Core/Services/InMemoryBlobService.cs`) is a production-quality fake of `IBlobStorageService`. Used by ~10 test files and by all 4 backup benchmarks. Constructor takes optional `simulatedLatencyMs` and `failureRate`. Honors deduplication. Tracks `TotalBytesUploaded` / `TotalOperations`. **This is the single biggest enabler of network-free testing in the project.** Use it instead of inventing a new mock.

6. **`AZBK_USE_SQLITE=1` env var** forces the SQLite backend. Set by benchmark `[GlobalSetup]`. Read once at `LocalDatabaseService.Initialize`; flipping mid-process is a no-op.

7. **`BackupOrchestrator` constructor** signature: `(LocalDatabaseService, EncryptionService, ChunkingService, IBlobStorageService, FileWatcherService)`. The `FileWatcherService` is required even when no folders are watched — it can be constructed without ever being started.

8. **`BackupOrchestrator` parallelism overrides (B25-bench-2 seam):** two optional `int?` properties — `MaxParallelChunkUploadsOverride` (default null → 6) and `MaxParallelFileBackupsOverride` (default null → 8). Read once per backup operation via internal `EffectiveMax*` getters so they cannot change mid-pipeline. Production behavior is unchanged when overrides are unset; the seam exists for the parallelism benchmarks. If you want to change the production *default*, change the constants in `BackupOrchestrator.cs`, not the override.

9. **`RestoreService.SmallFileThresholdBytes` (B26) is the single source of truth for what "small" means.** This `public const long` (currently `16L * 1024 * 1024`, i.e. 16 MB) lives on the `RestoreService` partial in `src/AzureBackup.Core/Services/RestoreService.Batch.cs` and is referenced by **both** the production restore pipeline (which routes files at or below the threshold through a 32-way parallel small-file lane) **and** the desktop UI (which groups the same set of files into the aggregate "Small files" progress row in `ProgressTabViewModel.SmallFileGroupText`, plus three call sites in `MainWindowViewModel.Commands.Backup.cs` / `MainWindowViewModel.Commands.TreeView.cs`). Before B26 the UI hard-coded 100 MB in three places while production used 16 MB, so the progress row mis-grouped a strictly larger set of files than the pipeline actually optimized for. **Do not introduce a new hard-coded copy of this value anywhere.** If you need the threshold in a new file, add `using AzureBackup.Core.Services;` and reference `RestoreService.SmallFileThresholdBytes` directly. The displayed user-facing label interpolates the constant so it tracks any future change automatically.

10. **B27 benchmark seam (`BackupBenchmarkBase`) — three new public surfaces.** `BackupBenchmarkBase.MemoryLimitMBOverride` is a `protected virtual int?` returning `null` by default; when non-null, `BaseIterationSetup` writes `MemoryLimitEnabled=true` + `MemoryLimitMB=<value>` via `LocalDatabaseService.SaveConfiguration` BEFORE the orchestrator runs, and `BackupOrchestrator.BackupFilesCoreAsync` picks it up via its `GetConfiguration()` call at the top of every backup. This is how the big-scale benchmarks simulate the recommended 16 GB production setting without touching production code. `BackupBenchmarkBase.BigWorkloads` is the public set of disk-heavy workloads (`media-library-500` ~260 GB, `production-scale-3000` ~70 GB, `huge-outlier-mixed` ~32 GB) gated by a new disk-space preflight at the top of `BaseGlobalSetup` that throws a clear error if free disk < 1.5x estimated workload size. The preflight reads `EstimateWorkloadBytes(Workload)` which returns 0 for the pre-B27 small workloads (no preflight) and a conservative overestimate for big ones. **Pre-B27 small workloads are unchanged** so the historical i7-9700K result tables in the four pre-B27 benchmark xmldocs remain directly comparable to a fresh small-workload run on the same hardware. Do not duplicate the `MemoryLimitMB=16384` value — if the recommended production setting changes, change it once in the relevant `*BigScaleBenchmark.cs` files (and `ProductionScaleBackupBenchmark.cs` and `MemoryBudgetBenchmark.cs`) and update this fact. **2026-04-27 correction (post-B27 hardware run):** the seam works as designed; the OOM observed at `[ChunkConcurrency=24, Workload=media-library-500]` on the first B27 hardware run was NOT caused by `MemoryLimitMB=16384` failing to bind. Root cause was the destination side -- see fact #11.

11. **`InMemoryBlobService` discard mode (B27).** New constructor parameter `bool retainPayloads = true` (default preserves all pre-B27 behaviour). When `false`, `UploadChunkAsync` / `UploadChunkDirectAsync` still record the blob name and tier in `_blobs` / `_blobTiers` (so dedup, listing, and `BlobExistsAsync` work) but the encrypted payload is replaced with `Array.Empty<byte>()`; the would-be storage cost is recorded in `_discardModeStorageBytes` so `TotalStorageUsed` stays accurate. Discard mode short-circuits the dedup-collision verification path because the original bytes are no longer present to compare against -- it trusts the hash, matching real Azure's optimistic dedup. `DownloadChunkAsync` and `VerifyChunkIntegrityAsync` are not supported against a discard-mode store, so restore-style benchmarks must NOT opt in. Production code never sets the flag; production tests never set the flag. The flag exists exclusively to remove the RAM ceiling on big-scale backup-throughput benchmarks: without it, `media-library-500` (~260 GB) cannot fit its encrypted ciphertext in process memory regardless of `MemoryBudget`, and the previously observed 58 GB peak working set was the `_blobs` dictionary growing unboundedly, not the orchestrator chunk-buffer pool. The benchmark opt-in is the new `protected virtual bool BackupBenchmarkBase.RetainBlobPayloads => true`; all five big-scale benchmark classes (`AdaptiveChunkConcurrencyBigScaleBenchmark`, `TwoTierFileSplitBigScaleBenchmark`, `LargestFirstSchedulingBigScaleBenchmark`, `ProductionScaleBackupBenchmark`, `MemoryBudgetBenchmark`) override it to `false`. **DO NOT add a new big-scale benchmark against `media-library-500` or larger without setting `RetainBlobPayloads => false`** -- it will OOM the host. **DO NOT use discard mode for any test that calls `DownloadChunkAsync` / `VerifyChunkIntegrityAsync` against the same store** -- they will fail because the bytes are gone.

12. **`BackupOrchestrator.BackupFilesAsync` reports unchanged-file bytes as completed (B31).** The orchestrator computes its own `totalFiles` and `totalBytes` from the input list it receives (`BackupFilesCoreAsync` lines 681-696). When a file is detected as unchanged via the metadata-skip path (`BackupOrchestrator.cs` ~line 648, `progress?.Report((fileInfo.Length, fileInfo.Length))`), the unchanged file's full size flows into the running `bytesProcessed` essentially instantly. This is INTENTIONAL and is the behaviour the file-watcher path and `MirrorSyncToAzureAsync` rely on. Consequence for UI callers that derive their progress denominators from a preview's `TotalBytesToTransfer` (which excludes unchanged files): if the UI passes the unfiltered selection list to `BackupFilesAsync`, the UI's preview-derived denominator (e.g. 1 TB of changed bytes) will be exceeded by the orchestrator's reported numerator (e.g. 2 TB including unchanged), producing 200%-style progress and a wildly wrong upload-speed estimate that briefly shows the unchanged files' bytes as transferred instantaneously. **DO NOT call `BackupOrchestrator.BackupFilesAsync` from a UI command path with the unfiltered user selection.** The B31 fix makes `MainWindowViewModel.ConfirmAndFilterBackupFilesAsync` rebuild the file list as `preview.FilesToCreate.Where(IsIncluded) UNION preview.FilesToOverwrite.Where(IsIncluded)` so unchanged files are stripped before the orchestrator ever sees them. Two regression tests in `BackupOrchestratorTests` (`BackupFilesAsync_UnchangedFileInInputList_OrchestratorReportsItsBytesAsCompleted` and `BackupFilesAsync_MixedChangedAndUnchanged_BytesProcessedSumsAllInputs`) pin the orchestrator-side behaviour the UI fix relies on. The Mirror Sync path on `MainWindowViewModel.Commands.TreeView.cs` is structured differently (it derives its denominator from the same unfiltered list and adds unchanged files instantly via an explicit "Unchanged" handler) and is not affected by this fix.

13. **Force-reupload + integrity auto-repair (B42).** `BackupOrchestrator.BackupFileAsync` gained a new optional parameter `bool forceReupload = false` on its deepest overload (the three shorter overloads default to `false` and behave exactly as before). When `true`, the metadata-skip fast path is bypassed (~line 654), `existingHashes` is treated as empty so the chunker emits every chunk as new (~line 681), and `isNewFile` is forced so the upload routes through `UploadChunkDirectAsync` instead of the dedup-aware `UploadChunkAsync` path (~line 686). `BackupOrchestrator.BackupFilesAsync` and `BackupFilesCoreAsync` both accept and propagate the same flag. **`PerformFullScanAsync` (in `BackupOrchestrator.Operations.cs`) was rewired in the same commit to actually re-upload** -- pre-B42 it only enqueued `FileChangeEvent`s which still hit the metadata-skip path for unchanged files, despite the UI button being labelled as a force-reupload action. It now invokes `BackupFilesAsync(allFiles, ..., forceReupload: true, ct)` directly. `IntegrityCheckService` gained a public `Func<string, CancellationToken, Task<bool>>? RepairCallback` and a private `RepairableReasons` whitelist (`missing-blob`, `wrong-size`, `md5-mismatch`, `crc-mismatch`, `decrypt-failed`, `byte-differ`, `download-error`); `IntegrityCheckOptions` gained `bool AutoRepairOnFailure { get; init; } = true`; `IntegrityCheckRun` gained `int FilesAutoRepaired` (B43: now persisted to the SQL `integrity_check_runs.files_auto_repaired` column; pre-B43 rows backfill to 0). When the first per-file pass produces at least one repairable failure and a callback is wired, the engine `Discard()`s the first-pass diag, invokes the callback (which is wired in `MainWindowViewModel` to `_orchestrator.BackupFileAsync(..., forceReupload: true, ...)`), re-reads the file's metadata via `_databaseService.GetBackedUpFile(path)` so the second pass uses the freshly-uploaded chunk hashes, and re-runs the chunk-loop. If the second pass is clean: no `.diag` is written, no row is persisted in `integrity_check_failures`, and `FilesAutoRepaired` is incremented. If the callback returns `false` or the second pass still fails: the original (or post-repair) failures are reported normally with a `.diag` flushed via the new `ReplayFailuresToDiag` / `FlushPassDiag` helpers. The retry is one-shot -- no recursion, no escalating budget. Three regression tests in `IntegrityCheckServiceTests` pin the contract (`AutoRepair_MissingBlob_ReuploadFixesIt_NoFailureRecordedNoDiag`, `AutoRepair_Disabled_FailureStillReported`, `AutoRepair_CallbackReturnsFalse_OriginalFailureReported`). **DO NOT call `BackupFileAsync(path, ct)` (the two-arg overload) from a repair callback** -- it hits the metadata-skip path and the missing/corrupt blob stays missing/corrupt; you MUST use the deepest overload with `forceReupload: true`. **DO NOT add a new failure reason that re-uploading cannot fix to `RepairableReasons`** (e.g. `local-file-missing`, `engine-error`) -- the callback would loop pointlessly and the user would see no failure.

14. **Throughput JSONL field is megabytes-per-second, key pinned (B43).** `ThroughputMetrics.FileMetrics` and `OperationMetrics` expose the C# property `ThroughputMBps` (note the capitalization: B-M-B-p-s, not B-Mbps). The on-disk JSON key is pinned via `[JsonPropertyName("throughput_mbytes_per_sec")]` so the configured `JsonNamingPolicy.SnakeCaseLower` cannot regress the key on a future rename. The value is computed at every producer as `bytes / elapsedSeconds / 1048576`, i.e. **megabytes per second (MB/s)**. Pre-B43 the property was named `ThroughputMbps` and the key was `throughput_mbps`, advertising megabits per second -- a factor-of-8 label error against the actual MB/s formula. Pre-B43 JSONL files contain the old key but the same MB/s values; consumers do a key-rename only, no value conversion. **DO NOT introduce a new throughput field with a `_mbps` suffix unless the producer literally multiplies by 8 to convert to megabits.** **DO NOT remove the `[JsonPropertyName]` attribute** -- the snake_case policy would silently emit `throughput_m_bps` and break every grep tool that expects the canonical key. The implausibility lesson is recorded in the new "Implausibility-prompts-verification" subsection under Hard Rules: when a user reports a number that looks off by a small-integer factor, suspect a label/formula mismatch BEFORE suspecting the environment.

15. **`integrity_check_runs.files_auto_repaired` schema column (B43).** Added in `SqliteBackend.Schema.cs` as `INTEGER NOT NULL DEFAULT 0`, with an idempotent `pragma_table_info` probe + `ALTER TABLE` backfill so existing user databases pick up the column on first open without a destructive migration. `InsertIntegrityCheckRun` and `UpdateIntegrityCheckRun` write the value, `GetRecentIntegrityCheckRuns` selects it, `ReadRun` maps it onto `IntegrityCheckRun.FilesAutoRepaired`. Pre-B43 historical rows backfill to 0 (the value was not recorded at the time, so 0 is the historical truth). **DO NOT change the column default to anything other than 0** -- the History expander would misrepresent legacy runs. **DO NOT remove the `pragma_table_info` probe** -- a fresh install does not need it but every existing user DB does, and a missing column would surface as a parameter-binding error on the next run insert.

16. **Catalog-file integrity diagnostic distinct from Data Integrity feature (B44).** `SqliteBackend.IntegrityFile.cs` adds `CheckDatabaseFileIntegrity()` which runs `PRAGMA cipher_integrity_check` followed by `PRAGMA integrity_check` under the same write lock the rest of the backend uses, and returns a `DatabaseFileIntegrityResult` record with `CipherIntegrityMessages`, `SqliteIntegrityMessages`, `CipherOk`, `SqliteOk`, and `IsHealthy`. **The two pragmas have OPPOSITE success shapes**: SQLCipher's `cipher_integrity_check` returns ZERO rows on success and one row per failing page; stock SQLite's `integrity_check` returns a single literal `"ok"` row on success and one row per problem. The result record encodes that asymmetry (`CipherOk => Count==0`, `SqliteOk => Count==1 && rows[0]=="ok"`); the StorageHealthViewModel report formatter spells `ok (no failing pages)` so the empty-list shape is not misread as "the pragma did not run". `LocalDatabaseService.CheckDatabaseFileIntegrity()` forwards on SQLite and throws `NotSupportedException` on the LiteDB legacy backend. `IntegrityCheckService.RunAsync` wraps the very first `InsertIntegrityCheckRun` call in a typed catch for SQLite error 11 (`SQLITE_CORRUPT`) and 26 (`SQLITE_NOTADB`) and rethrows as the new `DatabaseFileCorruptException` (carries `SqliteErrorCode`); `DataIntegrityViewModel.CheckNowAsync` and `ReCheckFailuresAsync` catch that type specifically and surface `Check could not start: …` / `Re-check could not start: …` with a pointer to the new Storage Health diagnostic. The Storage Health view exposes a `Verify Database File` button + scrolling `SelectableTextBlock` report bound to `VerifyDatabaseFileCommand` / `DatabaseFileReport`. **DO NOT collapse the cipher and SQLite pragmas into one bool** -- the two failure modes have different remediations (re-derive key vs restore from backup vs rebuild from Azure metadata) and the user needs to see which one fired. **DO NOT use `cipher_integrity_check` to distinguish "healthy" from "pragma did not run"** -- the empty-list shape is the success signal; check the connection state before calling instead. **DO NOT route `SQLITE_CORRUPT` errors from the integrity-check engine through the generic Data Integrity "check failed" path** -- a corrupt catalog cannot be repaired by re-running the Data Integrity check, and the auto-repair callback would loop pointlessly.

---

## Active workstreams (as of 2026-04-27)

None. W1 and W2 were resolved by the B27 commit (see Completed workstreams below). The next session should pick up from a clean slate.

Follow-ups deferred from B27 (each can become its own future workstream when prioritized):

- **Workload-aware LPT scheduler.** The B27 hardware run found LPT regresses mid-sized realistic workloads (+19% on `mixed-realistic-1000`, +24% on `realistic-large-200`) but is roughly flat on big-scale realistic workloads. A scheduler that applies LPT only when size variance exceeds a threshold could in principle recover the large-skew win without the mid-sized regression, but the right shape of "variance threshold" is not obvious from the current data. Would need many more synthetic profiles between `mixed-realistic-1000` (LPT loses by 19%) and `huge-outlier-mixed` (LPT flat) to find the inflection point. Out of scope for B27 because the simpler answer (do nothing) is defensible.
- **Per-file adaptive chunk concurrency in backup.** Restore already does this (`RestoreService.ComputeAdaptiveChunkConcurrency`). For backup, the pre-B27 i7-9700K data was directionally suggestive but compromised; the post-B27 fresh data on this hardware (`AdaptiveChunkConcurrencyBenchmark` and the partial `*BigScaleBenchmark` numbers in the 2026-04-24 log) showed no monotone benefit across 4 / 6 / 12 / 24 chunk concurrency, so there is no signal for the production change to track. Re-evaluate if a future workload-shape change (e.g. dramatically larger file sizes, much higher dedup) makes the per-file chunk pipeline a measurable bottleneck.
- **`LargestFirstSchedulingBigScaleBenchmark.media-library-500` re-run.** The B27 row has a 280-minute Mean with StdDev 389 m on N=2 (one normal ~5 m run, one pathological ~9 h run during the 42-hour overnight). Almost certainly host-OS pressure rather than orchestrator behaviour; the LPT iteration on the same workload was a stable ~5 m. If a defensible number is ever needed for that cell, re-run it alone with `iterationCount>=5` in a clean session. The xmldoc result table flags this row with an asterisk and notes the cause.
- **Disk-backed test blob service.** Currently `InMemoryBlobService.retainPayloads=false` is the only escape from the 64 GB-RAM ceiling on big-scale benchmarks. A disk-backed `IBlobStorageService` would let us measure restore throughput and integrity-check throughput against a populated store at production scale, but is out of scope for B27 because backup throughput (the W1/W2 question) does not need it. See discussion in the AGENT_CONTEXT maintenance log entry below if the trade-offs ever come up again.

---

## Completed workstreams (recent)

- **B44 (commit `<pending>`, 2026-04-27)**: Added a Storage Health diagnostic that verifies the local SQLCipher catalog file itself, distinct from the user-visible Data Integrity feature that verifies backed-up files against Azure storage. Triggered by a production repro: the Data Integrity tab failed with `Check failed: SQLite Error 11: 'database disk image is malformed'` and the only log line was `InsertIntegrityCheckRun: enter`, proving the catalog file was the failure point rather than any backed-up file. Three layers landed in one commit. (a) Backend: `SqliteBackend.CheckDatabaseFileIntegrity()` runs `PRAGMA cipher_integrity_check` then `PRAGMA integrity_check` under the existing write lock and returns a `DatabaseFileIntegrityResult` record. (b) Service plumbing: `LocalDatabaseService.CheckDatabaseFileIntegrity()` forwards (LiteDB throws `NotSupportedException`); `IntegrityCheckService.RunAsync` translates SQLite error 11/26 from the very first `InsertIntegrityCheckRun` call into the new `DatabaseFileCorruptException` so the UI can present an actionable typed message instead of a raw error code. (c) UI: `DataIntegrityViewModel` shows `Check could not start: …` for the typed corruption case; `StorageHealthViewModel` exposes `VerifyDatabaseFileCommand` + `DatabaseFileReport`, and `StorageHealthView.axaml` adds a Catalog Database File card with a `Verify Database File` button and a scrolling `SelectableTextBlock` that the user can copy into a support request. The result-record contract bakes in the asymmetry between the two pragmas (cipher: zero-rows-on-success, SQLite: single-`"ok"`-row-on-success); a regression test (`CheckDatabaseFileIntegrity_OnHealthyDatabase_ReportsBothPragmasOk`) pins both shapes against a freshly initialised database. `docs/USER_GUIDE.md` gained a new "Catalog database file" subsection under Storage Health describing when to use the diagnostic and how to read the report. Tests: 831 of 831 pass (was 830 at B43; +1 net). Build: 0 warnings, 0 errors. Lesson captured in fact #16: SQLCipher and stock SQLite report "healthy" with opposite row counts, so a naive `Count==1 && rows[0]=="ok"` predicate that works for one pragma silently flags the other as broken on every healthy database. Author: Copilot agent session.
- **B41 (commit `<pending>`, 2026-04-27)**:
- **B27 (commit `fee6622`, 2026-04-27)**: Re-baselined every backup benchmark on this AMD EPYC 7763 hardware after the 2026-04-25 overnight run completed cleanly, then shipped two production changes the data justified and explicitly retired two more that the data refuted. Production changes: (1) `BackupOrchestrator.MaxParallelFileBackups` raised from 8 to 16 -- `TwoTierFileSplitBigScaleBenchmark` showed -22% on `production-scale-3000` and -27% on `media-library-500` against the pre-B27 default, with the 32-way row flat against 16-way confirming 16 is the crossover. (2) `Configuration.MemoryLimitEnabled` default flipped from `false` to `true`, and `Configuration.MemoryLimitMB` default raised from 2048 to 8192 -- `MemoryBudgetBenchmark` showed all four budget values within 4.5% of each other on `media-library-500` (the worst-case workload), so constraining is free; 8 GB is the smallest stepped value that fits the new 16-way ceiling without forcing stalls. Decisions retired: (3) **B27b (per-file adaptive chunk concurrency in backup) NOT shipped** -- `AdaptiveChunkConcurrencyBenchmark` and the partial big-scale data showed no monotone benefit across 4 / 6 / 12 / 24 chunk concurrency, so there's no signal for the production change to track. (4) **B27c (blanket largest-first sort) NOT shipped** -- `LargestFirstSchedulingBenchmark` reproduced the pre-B27 regressions of +19% on `mixed-realistic-1000` and +24% on `realistic-large-200` on this hardware, and the `BigScale` companion was flat at production scale, so blanket LPT remains DO-NOT-DO. Infrastructure: a new architectural seam (`InMemoryBlobService(retainPayloads:)` constructor parameter + `BackupBenchmarkBase.RetainBlobPayloads` virtual hook) was needed to remove the RAM ceiling that was causing the first B27 hardware run to OOM at the 260 GB `media-library-500` workload. All five big-scale benchmark classes opt into `RetainBlobPayloads => false`; production tests are unchanged. See architectural fact #11 for the discard-mode contract. Result tables filled in all 7 affected benchmark xmldocs; `docs/SETUP.md` updated with the new defaults and concurrency constants; `docs/USER_GUIDE.md` updated with the new memory-limit default text. 766 of 766 tests pass (was 759 in pre-B27 AGENT_CONTEXT, +6 new discard-mode tests on `BlobServiceTests`, +1 from a separate small change already in the working tree at the time of the B27 baseline).
- **B26 (commit `b9d5744`, 2026-04-24)**: Unified the small-file threshold across the UI/Core boundary. Promoted the previously private `RestoreService.Batch.cs` constant `SmallFileThreshold = 16L * 1024 * 1024` to a `public const long RestoreService.SmallFileThresholdBytes`. Replaced three independent UI-side hard-codes of `100L * 1024 * 1024` (one in `MainWindowViewModel.Commands.Backup.cs`, two in `MainWindowViewModel.Commands.TreeView.cs`) and the user-visible string `"Small files (≤100 MB)"` in `ProgressTabViewModel.SmallFileGroupText` with references to the public constant. ProgressView now correctly groups files ≤ 16 MB into the "Small files" aggregate row, matching what the production restore pipeline actually treats as small. All 759 tests still pass.
- **B25-bench-2 (commit `171f07e`, 2026-04-24)**: Added `BackupBenchmarkBase`, `LargestFirstSchedulingBenchmark`, `TwoTierFileSplitBenchmark`, `AdaptiveChunkConcurrencyBenchmark`. Added strictly-additive `MaxParallelChunkUploadsOverride` / `MaxParallelFileBackupsOverride` seams on `BackupOrchestrator`. All 759 tests still pass.
- **B25-bench (commit `614825a`)**: First end-to-end backup throughput benchmark. Established the `InMemoryBlobService`-based pipeline test pattern.

---

## Conventions / patterns observed in this codebase

- **`[Conditional("DIAGNOSTICLOG")]` logging**: services use `Log(string)` methods that are completely no-ops in Release without the `DIAGNOSTICLOG` constant set. The B16 commit also adds a title-bar marker showing whether the build has DEBUG/DIAG on.
- **Diagnostic events forwarded by name**: services raise `EventHandler<string>? DiagnosticLog` events; `MainWindowViewModel.OnDiagnosticLog` aggregates them all.
- **Crash-safe atomic operations**: file moves during DB migration use a sentinel `.upgrade-pending` JSON file plus 4-step rename dance. See `LocalDatabaseService.Migration.cs` and `.Migration.Sqlite.cs`. Do not invent new file-rename code without reading those.
- **`PaddedLong` for hot atomic counters**: cache-line-padded structs prevent false sharing. See `BackupOrchestrator` line ~592 for example. If adding new hot counters under parallel workers, use `PaddedLong`.
- **`ArrayPool<byte>` discipline**: producer/consumer code transfers ownership of pooled buffers via the channel. The `ChunkPayload` record carries the rented buffer and the consumer is responsible for `ArrayPool<byte>.Shared.Return(payload.Data)` after upload completes. Do not double-return.
- **No emoji** anywhere in the commit messages.
- **xmldoc style is heavy and discursive** — multi-paragraph `<remarks>` blocks explaining WHY a design decision was made, with B-numbers cross-referencing commits. Match this style when adding new public surface.

---

## Running tests and benchmarks

### Tests

`dotnet test tests/AzureBackup.Tests/AzureBackup.Tests.csproj -c Debug` from the repo root. All 759 tests pass deterministically.

If running from inside Visual Studio (which is the typical user environment), the `run_tests` / `get_tests` MCP tools work directly against Test Explorer. Use `Project=AzureBackup.Tests` as the filter.


---

## Recent commit history (for fast catch-up)

| Hash | Message |
|---|---|
| `<pending>` | B44: catalog-file integrity diagnostic (cipher_integrity_check + integrity_check) on Storage Health; typed DatabaseFileCorruptException on first integrity-run insert; clearer Data Integrity UX when catalog itself is malformed |
| `<pending>` | B43: throughput field rename (MB/s, pinned JSON key); persist FilesAutoRepaired; integrity auto-repair outcome telemetry; Auto-repair toggle in Data Integrity UI; Force Re-upload Selected in Sync menu |
| `<pending>` | B42: integrity-check auto-repair (force-reupload then re-check, suppress diag on heal); BackupOrchestrator.BackupFileAsync forceReupload; PerformFullScanAsync now actually re-uploads |
| `<pending>` | B41: clean build warnings (CS8602 x2, xUnit2009/2013/2031); production guard pattern restored on RebuildReverseChunkIndexCore |
| `fb852db` | B31: UI pre-filters unchanged files before BackupFilesAsync; fixes >100% progress and wrong ETA when selection mixes changed and unchanged files |
| `9cd7d47` | B37: LargeChunkBufferPool, bounded LOH recycler for large chunk buffers |
| `fc1897a` | B38: fold encrypt-buffer charge into producer-side acquire (prevents deadlock) |
| `0f11c59` | B30+B33+B34+B36: producer-side budget accounting, exact byte[] for large chunks, tightened oversized admission, periodic memory log |
| `d3e60d4` | B29: add B29 row to AGENT_CONTEXT recent-commit-history table |
| `aa6fec8` | B29: hardware-aware MemoryLimitMB default (25 percent of physical RAM, capped at 8 GB) |
| `35536b0` | B28: fix AGENT_CONTEXT self-reference of B28 commit hash |
| `e67951e` | B28: fix empty Local pane on startup; document Avalonia TreeView+Reset-event footgun |
| `4364faf` | B27: fix AGENT_CONTEXT self-reference of B27 commit hash |
| `fee6622` | B27: 16-way file concurrency, MemoryLimit defaults on/8 GB, InMemoryBlobService discard mode, big-scale benchmark result tables |
| `b9d5744` | B26: UI small-file threshold reads RestoreService constant; remove 100 MB hard-code |
| `fe620d6` | docs: rewrite SETUP and USER_GUIDE against verified source, fix AGENT_CONTEXT errors |
| `a36db75` | AGENT_CONTEXT and copilot-instructions: never push, treat docs as stale until verified |
| `68410b5` | AGENT_CONTEXT: remove PC-specific data and stale workstream status |
| `171f07e` | B25-bench-2: design-decision benchmarks for backup parallelism plus AGENT_CONTEXT handoff |
| `614825a` | B25-bench: add BackupThroughputBenchmark for end-to-end backup pipeline |
| `e7a1ab1` | B24: discard restore diag on success and on cancellation |
| `31f467b` | B23: serialize all SqliteBackend reads against the shared connection; fix diag noise from successful and cancelled file backups |
| `5dfa40a` | B22: split SqliteBackend.cs into 11 partial files and gate diag logging on DIAGNOSTICLOG |
| `c0c33fc` | B21: add permanent Copilot instructions for single-line PowerShell and git commit policy |
| `0ecd72f` | B20: fix Check Now / Cancel / Re-check / Backfill buttons stuck at initial enabled state |
| `3b4a64f` | B19: stable per-file fileIndex in parallel backup progress; fix Active Files panel + small-file completion counts |

Run `git log --oneline | Select-Object -First 20` for the latest. Earlier history (157 commits total as of this writing) includes a `C-`series and an unnumbered prefix. The four most recent commits in the table above are local-only as of this writing — the user pushes manually, the agent does not.

---

## Maintenance log for THIS file

- **2026-04-24** — Initial creation. Captures B25-bench-2 in-progress state, all four benchmarks measured, two production seams added, working-tree dirty pending commit. Author: Copilot agent session (Claude Sonnet 4.5).
- **2026-04-24** — User pointed out that PC-specific data (CPU model, free disk, terminal-tool timeout, hardware-tied benchmark numbers, workstream status that referred to uncommitted edits already long-since committed as `171f07e`) misleads sessions on other machines. Removed all such data. Removed the entire stale "what remains in W1" subsection. Added explicit "never record machine-specific values" rule to the edit policy at the top. Added pointers to `docs/SETUP.md` and `docs/USER_GUIDE.md` in the project-at-a-glance section. Replaced the hardware-tied "Local environment quirks" section with a hardware-neutral "Running tests and benchmarks" section so a fresh agent on a fresh clone knows the entry-point commands. Added an explicit Windows-only note about the WPF project. Added architectural fact #8 covering the new B25-bench-2 override seam on `BackupOrchestrator`. Promoted B25-bench / B25-bench-2 into a "Completed workstreams" section. Renumbered the active W2 (LPT investigation) since the old W2/W3 collapsed. Updated the recent-commit-history table to include `171f07e`. Author: Copilot agent session.
- **2026-04-24** — Added two permanent rules: (1) never run `git push` under any circumstances, pushing is the user's manual job; (2) treat every Markdown doc in the repo as stale until verified against code, and update affected docs in the same commit as the code change that invalidates them. Added a new "Documentation trust policy" subsection under "Hard rules" capturing the second rule explicitly. Reworded the previous (also added today) `docs/SETUP.md` and `docs/USER_GUIDE.md` pointers in the project-at-a-glance section so they no longer treat those files as authoritative; instead they tell the agent to run a real `dotnet build` for setup and read the actual view code for UX questions. Reworded the corresponding `MemoryLimitMB` open-question entry the same way. Same updates applied to `.github/copilot-instructions.md` so a fresh agent reads them before even reaching this file. Author: Copilot agent session.
- **2026-04-24** — Systematic rewrite of `docs/SETUP.md` and `docs/USER_GUIDE.md`. Verification against source revealed that AGENT_CONTEXT itself was wrong about the GUI framework: it claimed WPF / Windows-only, but the project is Avalonia 11.3.12 targeting bare `net10.0` (cross-platform: Windows, macOS, Linux). Fixed that error in the project-at-a-glance section and the open-questions list. Other corrections caught while verifying the docs: (a) default storage tier for new watched folders is **Hot**, not Cool as both old docs claimed; (b) the app supports **four** storage tiers (Hot/Cool/Cold/Archive), not three as both old docs claimed; (c) the app supports a **portable mode** triggered by a `portable.marker` file next to the EXE, completely undocumented before; (d) the **Data Integrity Check** and **Tier Migration** views existed but were not in the USER_GUIDE TOC; (e) the **Export Bundle** button in Logs and the **Auto-export bundle on failure** option in Data Integrity were undocumented; (f) the per-extension chunk-size config table was undocumented (old SETUP just said "64KB-1MB"); (g) the LiteDB-to-SQLCipher migration path was undocumented; (h) the diagnostic-logging story (runtime UI toggle gated on the `DIAGNOSTICLOG` build constant) was incorrectly described in USER_GUIDE as a generic runtime toggle. Both docs are now flagged in the Documentation trust policy as "verified-current" with an explicit per-commit maintenance protocol listing the categories of change that REQUIRE updating them in the same commit. Author: Copilot agent session.
- **2026-04-24** — Catch-up update missed by commit `b9d5744` (B26). The B26 commit unified the UI's small-file grouping threshold with production's 16 MB constant in `RestoreService.Batch.cs` but did not touch this file at the time, even though the file's own rules require it. This entry corrects three resulting drifts: (1) the B26 number had been pre-allocated to one of the W1 follow-up tasks in the active workstreams; renamed those tasks to B27a/B27b/B27c so the numbering stays unambiguous, and added an explanation of why the pre-allocated number was consumed by an unrelated fix. (2) Added B26 to the "Completed workstreams" section. (3) Added architectural fact #9 documenting `RestoreService.SmallFileThresholdBytes` as the single cross-boundary source of truth for the "small file" definition, with an explicit "do not introduce a new hard-coded copy" warning so future agents do not re-create the drift the original fix removed. Also refreshed the recent-commit-history table to include all four post-B25-bench-2 commits (`68410b5`, `a36db75`, `fe620d6`, `b9d5744`), updated the conventions-section line that said "current series is at B25" to point at B26, and bumped the cited total commit count from 153 to 157. Lesson recorded: the per-commit AGENT_CONTEXT-maintenance rule applies to every code commit that introduces a new architectural fact or a new architectural seam, not just to commits that change something already documented in the file. Author: Copilot agent session.
- **2026-04-24** — Hardened the maintenance rule itself in response to user audit. Root cause of the B26 miss: the previous "UPDATE this file when ANY of the following happens" wording described triggers as outcomes evaluated AFTER the commit, with no pre-commit checkpoint and no explicit "introduces a new architectural fact" trigger to balance the existing "discovers" trigger. Three changes: (1) Replaced the soft trigger list with a numbered nine-item **Pre-commit checklist** at the top of the file, framed as "before running git commit, walk through these questions" so the audit happens BEFORE the commit lands rather than after. Each item is a concrete yes/no question; question 1 explicitly names the categories the agent has previously rationalized away (new public API on a Core type, new cross-project constant, new env var, new build constant, new partial-class seam, etc). Added a "Hidden failure mode" subsection naming the specific bad reasoning ("this commit only changes code, not docs") that caused the B26 miss, citing commit `df0df53` as the catch-up. (2) Mirrored the rule into `.github/copilot-instructions.md` so a fresh session sees it before even reaching this file: top-of-file UPDATE summary now includes "introduced" alongside "discovered" and explicitly mentions B-numbered commits / new public APIs as triggers; Git commit policy section now starts with an "audit AGENT_CONTEXT before committing" bullet that references the pre-commit checklist by name and warns about the exact bad reasoning that caused the B26 miss. (3) Renamed the file's old "Edit policy" section to "Edit policy (style)" to make clear it covers formatting only, while the load-bearing rule lives in the Pre-commit checklist. No change to the substance of any other section. Author: Copilot agent session.
- **2026-04-24** — B27 benchmark rewrite landed in the working tree (uncommitted) ahead of the real-hardware run. User flagged that the original B25-bench-2 benchmarks were compromised by a 28 GB disk cap on a different PC, and that any future numerical comparison must be same-hardware. Three new big-scale workloads (`media-library-500`, `production-scale-3000`, `huge-outlier-mixed`) added to `BackupBenchmarkBase` along with a `MemoryLimitMBOverride` virtual hook (so 16 GB MemoryBudget can be applied via `LocalDatabaseService.SaveConfiguration` without touching production code) and a disk-space preflight in `BaseGlobalSetup`. Five new benchmark classes added: `ProductionScaleBackupBenchmark`, `LargestFirstSchedulingBigScaleBenchmark`, `TwoTierFileSplitBigScaleBenchmark`, `AdaptiveChunkConcurrencyBigScaleBenchmark`, `MemoryBudgetBenchmark`. Pre-B27 small-workload benchmarks and their xmldoc result tables are deliberately untouched per user directive (keep historical numbers as-is for continuity), but they are now flagged as i7-9700K data and must not be quantitatively compared to numbers from any other PC. Three updates to this file: (1) added architectural fact #10 documenting the `BackupBenchmarkBase` seam (`MemoryLimitMBOverride`, `BigWorkloads`, disk preflight); (2) added a new "Same-hardware benchmark discipline" subsection under Hard Rules capturing the never-cross-PC-compare rule and noting the four pre-B27 benchmark classes whose embedded result tables are i7-9700K data; (3) renamed W1 from "act on B25-bench-2 findings" to "re-baseline at production scale on this hardware first, then act", added a new B27d follow-up gated on `MemoryBudgetBenchmark` results, and folded W2 (LPT regression investigation) into B27 since `huge-outlier-mixed` is the decisive test. No commit yet — the commit will land after the real-hardware run produces numbers, with the result tables filled into the new benchmarks' xmldocs in the same B27 commit. During this update I twice broke the numbering of the architectural-facts list while editing fact #10 in (had to restore fact #9's body verbatim from this file's earlier maintenance-log quote and re-append fact #10 at the end). Lesson: when adding to a numbered list near the end, append at the end with a verified surrounding-context match, do not try to insert into the middle. Author: Copilot agent session.
- **2026-04-27** — B27 commit landed (`fee6622`, then self-reference fix `4364faf`). Implementation details captured in the commit messages and in the recent-commit-history table; this maintenance-log slot was overwritten by a later edit during B28/B29 work and the body is preserved only via `git show fee6622`.
- **2026-04-27** — B28 commit landed (`e67951e`, then self-reference fix `35536b0`). Fixed empty Local pane on startup: `RefreshLocalFilesAsync` rebuilt `LocalFileTreeRoots` via `BulkObservableCollection.ReplaceAll` (single Reset event), but Avalonia's `TreeView` with a `HierarchicalDataTemplate` drops Reset events that arrive before the control's first layout pass -- which is exactly the startup-unlock window because `SyncView` is `IsVisible="False"` until `MainWindow.HandleStartupFlowAsync` flips `CurrentView` to `"Sync"` AFTER unlock returns. Fix: switched the tree-roots collection to Clear+per-item-Add (mirrors the working right-pane Azure tree); kept `LocalFilesFlatList.ReplaceAll` because the `ListBox` is unaffected. Added the missing `OnPropertyChanged(nameof(FilteredLocalFiles))` mirror of the right-pane invalidation. Documented the footgun in a new DO-NOT paragraph on `BulkObservableCollection`'s class summary.
- **2026-04-27** — B29 commit. User flagged that the B27 flat `MemoryLimitMB=8192` default misbehaves on low-RAM machines: on a 4 GB host the saved value of 8192 exceeds total physical RAM, the slider can only display up to 4096, the severity indicator goes red, and the budget never binds (effectively unlimited despite the toggle being on). Replaced with a hardware-aware rule: `min(round_down_to_step(0.25 * total_physical_RAM), 8192 MB)`. Sample outputs: 2 GB host -> 512 MB, 4 GB -> 1 GB, 8 GB -> 2 GB, 16 GB -> 4 GB, 32 GB or more -> 8 GB. The 25% rule lines up with the existing 25% OS reserve in `SystemMemoryHelper.GetEstimatedAvailableMemoryBytes`; the 8 GB cap matches the B27 worker-pool ceiling (`16 files x 6 chunks x 64 MB = 6 GB` plus ~2 GB of headroom) above which the budget pulls no extra weight. Implementation pattern: schema column flipped from `INTEGER NOT NULL DEFAULT 8192` to `INTEGER NULL DEFAULT NULL` (sentinel for "user has not yet expressed a preference"); `SqliteBackend.GetConfiguration` substitutes `SystemMemoryHelper.GetRecommendedDefaultLimitMB()` when the column reads NULL but does NOT auto-write the substituted value back to the DB, so a never-configured row stays NULL and is inspectable for diagnostics. Existing user databases that wrote 8192 in B27/B28 keep that exact value verbatim -- the rule only fires on rows where the column was NULL at read time, which can only be a fresh install. Helper has a pure-function sibling `GetRecommendedDefaultLimitMBForTotalBytes` so unit tests can exercise every RAM tier deterministically; +24 new tests cover 2/4/8/16/32/64/128 GB tiers plus three boundary cases (detection-failed/0, negative, and 768 MB tiny-machine). The `SqliteBackendConfigurationTests.GetConfiguration_FreshDatabase_ReturnsDefaults` assertion was updated to compare against `SystemMemoryHelper.GetRecommendedDefaultLimitMB()` instead of the C# model default of 8192, so the test passes on every CI hardware tier (on a 32 GB+ host the two values still match by happy accident, on a 16 GB host they intentionally do not). Both `docs/SETUP.md` (per-tier rule replaces the flat `8192` row in the technical-specs table) and `docs/USER_GUIDE.md` (memory-limit section now includes the per-tier default table) updated in the same commit per the verified-current-docs maintenance protocol. Also corrected the FAQ entry near the bottom of this file (which the B28 commit had updated to say "enabled by default at 8 GB" -- correct for B27/B28, but now superseded by the hardware-aware rule). 790 of 790 tests pass (was 766 in B28; +24 new helper tests). Lesson: appending a new dated entry to this file by replacing a prefix of an older entry -- the trick I have used in B27, B28, and now here -- destroys the older entry's body. Stop doing that. Use a literal append (find the last entry's full closing `Author: Copilot agent session.` line as oldString, replace with `<that line>\n- <new entry>`) so the previous entry survives intact. Author: Copilot agent session.
- **2026-04-27** -- B30+B33+B34+B36 commit. User reported a production-test OS crash: 8000-file / 10.5 TB media-heavy backup with `MemoryLimitMB=16384` on a 32 GB host saw the program drive working set well above 16 GB and the OS apparently ran out of memory; the existing logs gave no insight into where the budget was leaking. Diagnosis identified five concrete undercount bugs in the backup memory-budget enforcement: (1) the producer's `ChunkingService.ChunkAndStreamChangedAsync` rented `MaxChunkSize`-sized buffers via `ArrayPool<byte>.Shared.Rent` and pushed them through a bounded channel of size `MaxParallelChunkUploads=6` BEFORE the consumer charged anything to the budget, so up to 1+6+6=13 chunk buffers per file existed in physical RAM completely invisible to the budget; (2) `ArrayPool<byte>.Shared` rounds rents up to the next power-of-two tier and parks released arrays in per-core buckets that the runtime never returns to the OS, so the budget's `payload.Length * 2` charge undercharged actual residency by up to 2x; (3) the encrypt-side `ArrayPool` rental in `AzureBlobService.UploadChunkAsync` allocated a third full-size buffer that the budget never charged for; (4) the `MemoryBudget.AcquireAsync` deadlock-avoidance branch admitted any request when `_usedBytes==0` rather than only oversized requests, allowing transient drains during a 16-way parallel run to silently bypass the cap; (5) `CdcBufferOverhead=128 MB` was charged once even though 16 concurrent CDC producers each rent their own 128 MB buffer. **B30** moves budget acquisition into the producer (`ChunkingService.ChunkAndStreamChangedAsync` now takes an optional `MemoryBudget` and calls `AcquireAsync` BEFORE allocating each chunk buffer); the consumer no longer re-charges and only releases what the producer charged via the new `ChunkPayload.ChargedBytes` field; the dedup branch in `DispatchChunkAsync` releases the producer-side charge so a re-backup of an unchanged folder no longer leaks the budget. **B33** introduces `ChunkingService.PoolSkipThresholdBytes=16 MB`: chunks at or above the threshold bypass `ArrayPool<byte>.Shared` entirely and use exact `byte[]` allocations, surfaced through the new `ChunkPayload.ReturnToPool` flag so the consumer can mirror the producer's allocation decision (returning a non-pool array to the shared pool would silently corrupt its tier buckets, hence a load-bearing flag). **B34** tightens `MemoryBudget.AcquireAsync`'s deadlock-avoidance branch so it fires only when `bytes > totalBytes && _usedBytes == 0`; a request that COULD fit by waiting now waits even when the budget reads empty, and each oversized admission increments the new `MemoryBudget.OversizedAdmissions` counter so the breach is observable rather than silent. **B36** adds a new `BackupMemoryReporter` that samples the live memory state every 30 seconds (configurable) and emits a structured one-line report through `StatusChanged` (the always-visible log channel, not `DiagnosticLog`) carrying budget used vs total, stall delta + total, oversized-admission delta + total, GC heap, GC memory load, working set, private memory, the unaccounted-for delta (working-set minus budget-used -- the diagnostic that would have caught this regression earlier), and gen-0/1/2 collection counts. The reporter is wired into both backup operation entry points and emits an immediate `[mem-start]` sample at construction plus a final `[mem]` sample on dispose. The `CdcBufferOverhead` constant in `BackupOrchestrator` was lowered from 128 MB to 16 MB now that producer-side allocations are tracked exactly. Tests: 806 of 806 pass (was 790; +16 new tests covering B34's `OversizedAdmissions` counter, the new would-fit-but-must-wait behaviour, and the full `BackupMemoryReporter` lifecycle including its swallow-exceptions-from-timer-callback contract). Both `docs/SETUP.md` (new technical-specs row describing the memory-budget enforcement rules) and `docs/USER_GUIDE.md` (new `Memory log lines during a backup (B36)` subsection) updated in the same commit per the verified-current-docs maintenance protocol. Author: Copilot agent session.

- **2026-04-27** -- B38 commit. The B30+B33+B34+B36 commit (0f11c59) closed the largest pre-fix accounting gaps but the user re-ran the production test and the OS still OOM-killed the process. The B36 memory log captured the regression in real time: budget locked at 99.3% of 16 GB across hundreds of samples (perfect throttle), oversized=0 (B34 working), but `gcHeap` peaked at 38.9 GB on a 32 GB host and `privateMem` hit 41.2 GB -- well past physical RAM. Diagnosis from the live log identified two remaining undercount sites: (a) the encrypt-side ArrayPool rental in `AzureBlobService.UploadChunk*Async` (chunkData.Length + EncryptionService.EncryptionOverhead bytes per chunk, rounded up to next pool tier, alive for the duration of the upload) which the B30 producer charge did not cover; (b) LOH retention from the B33 exact `byte[]` allocations sitting on the LOH between gen-2 collections (one per minute under load, ~6 GB of allocations per minute). B38 fixes (a). B37 will fix (b) separately. Initial implementation tried the obvious shape -- a consumer-side Acquire on the encrypt buffer threaded through `IBlobStorageService.UploadChunk*Async` as a new optional `MemoryBudget?` parameter -- and the test suite hung. Root cause: producer-vs-consumer circular wait on the same budget. The producer holds its chunk-buffer charge until the consumer finishes upload; the consumer cannot start the upload because its encrypt-Acquire is blocked on the producer charge that already filled the budget. The orchestrator integration test `WhenExplicitBudgetProvidedThenConsumerRespectsIt` (budget=512 KB, file=256 KB) hit the deadlock immediately because the worst-case per-chunk pipeline residency (~3 MB) exceeds 512 KB and the at-least-one branch can only admit ONE oversized request -- the second stage's Acquire then blocks forever. Fix: drop the consumer-side Acquire entirely. Charge BOTH stages on the producer side in a single atomic `MemoryBudget.AcquireAsync` inside `ChunkingService.AcquireChunkBufferAsync` -- payload tier ceiling + encrypt tier ceiling = ~2 x payloadSize per chunk. This eliminates the cycle: the budget becomes a strict throttle on the producer alone, the consumer never Acquires, only Releases. The plumbing-only Phase 1 changes to `IBlobStorageService.UploadChunk*Async` (added optional `MemoryBudget?` parameter) were reverted -- the interface signature is back to its pre-B38 shape, and the blob services are unchanged. Two new tests lock in the contract: `B38_ProducerChargeCoversEncryptBuffer_NoDeadlockOnMultiChunkFile` runs an 8 MB multi-chunk file through a 512 KB budget with a 60 s wall-clock timeout, so any future regression that re-introduces the cycle fails fast rather than hanging. The pre-existing `WhenExplicitBudgetProvidedThenConsumerRespectsIt` test was preserved verbatim with its 512 KB budget so it doubles as a regression guard. Tests: 807 of 807 pass (was 806; +1 net). `CdcBufferOverhead` was not changed -- it was already 16 MB after B30 and the per-chunk producer charge now naturally absorbs the encrypt residency. `docs/SETUP.md` updated to describe the new combined-charge model and the rationale (the consumer never acquires from the same budget). The user-visible memory log format from B36 is unchanged. Lesson: when adding a budget Acquire on a downstream stage of a producer-consumer pipeline, ALWAYS check that the upstream stage releases before the downstream stage acquires from the same budget -- if both stages can hold a charge concurrently against the same budget, there exists a workload that deadlocks. Even if the deadlock-avoidance branch (B34) admits the first oversized request, subsequent requests still block. The safer architecture is to charge the entire pipeline cost in one Acquire at the producer side; the budget then quantifies in-flight work units rather than in-flight allocations. Author: Copilot agent session.
A bounded recycler converts that to `cap x bucket_size` regardless of allocation rate, at the cost of holding a fixed residency floor even when the workload is idle. The trade is correct because the floor is BOUNDED whereas the GC-managed residency was not. Author: Copilot agent session.
The Mirror Sync path on `MainWindowViewModel.Commands.TreeView.cs` is structured differently (denominator derives from the same unfiltered list and unchanged files are added instantly via an explicit "Unchanged" handler) and is not affected. Tests: 826 of 826 pass (was 824; +2 net). `docs/SETUP.md` and `docs/USER_GUIDE.md` checked per the verified-current-docs maintenance protocol; neither makes any specific claim about progress percentage or ETA accuracy in the mixed-selection case, so no doc update required by this change. Author: Copilot agent session.
- **2026-04-27** -- B41 commit. Build cleaned to zero warnings (was 5). `SqliteBackend.RebuildReverseChunkIndexCore` was a private helper called via `InWriteLock(() => Helper(...))` from a public method that already guarded `_connection != null`; flow analysis cannot follow the prior guard across the lambda boundary, so the first `_connection.CreateCommand()` inside the helper warned CS8602. Added the same guard at the top of the helper, matching the file-wide convention. `RestoreService.RestoreFileAsync` multi-chunk branch stored the null-check result in a separate `ownsMemoryBudget` flag and reassigned the `memoryBudget` parameter inside an `if`-block; CS8602 could not follow this indirection. Restructured to capture the conditional allocation into a non-null local via `??`. The `ownsMemoryBudget` flag is preserved and still drives the disposal decision in the `finally` block, so ownership semantics are unchanged. Three xUnit-analyzer warnings fixed by switching to the analyzer-recommended assertion shape (no behaviour change, only better failure messages). The Git commits section of this file gained a new commit-message-hygiene subsection covering four rules: describe the change not the process, do not mention what did not change, do not restate rules that live in this file, do not refer to yourself as the agent. The same hygiene applies to maintenance-log entries here. The placeholder-hash trailer-commit pattern (`35536b0`, `4364faf`, `75ceaa9`, `d5fbc38`, `d3e60d4`) is retired: new B-numbered commits add their row with a `<pending>` placeholder and the next session that touches this file for any other reason fills in the hash opportunistically. Reusable craft lessons recorded for future sessions: when a method takes a nullable parameter and conditionally allocates when null, capture into a non-null local via `??` rather than reassigning a flagged parameter -- flow analysis follows the former and not the latter; flow analysis does not propagate guards across `InWriteLock(() => Helper(...))` lambda boundaries, so private helpers reached this way must repeat the guard if the field is nullable.
- **2026-04-27** -- B42 commit. Diagnostic bundle from a production run showed integrity-check failures that were genuine corruption / missing-blob conditions but had no automated recovery path: the user had to manually find the failing file in the UI, force a re-upload, and re-run the integrity check. The root user request was three lines, the first of which was specified ("auto-repair on integrity failure, suppress diag on heal") and the second and third of which were blank ("2. implment this." / "3, Implement"). Implemented only item 1; left a clarification request for items 2 and 3 rather than guessing. Three layered changes. (a) `BackupOrchestrator.BackupFileAsync` deepest overload gained `bool forceReupload = false`; when `true`, the metadata-skip fast path (`existingFile.Status == Completed && size+timestamp match`) is bypassed, `existingHashes` is empty so the chunker emits every chunk as new, and `isNewFile` is forced so uploads route through `UploadChunkDirectAsync` (the new-file path) instead of the dedup-aware `UploadChunkAsync`. The three shorter overloads default to `false` and behave exactly as before, so existing call sites are unchanged. `BackupFilesAsync` and `BackupFilesCoreAsync` propagate the flag through. (b) `PerformFullScanAsync` was rewired in the same commit because the pre-B42 implementation only enqueued `FileChangeEvent`s, which still hit the metadata-skip path for unchanged files despite the UI button being labelled as a force-reupload action. It now invokes `BackupFilesAsync(allFiles, ..., forceReupload: true, ct)` directly so the button does what it promises. (c) `IntegrityCheckService` gained `RepairCallback` (a `Func<string, CancellationToken, Task<bool>>?`), a private `RepairableReasons` whitelist (`missing-blob`, `wrong-size`, `md5-mismatch`, `crc-mismatch`, `decrypt-failed`, `byte-differ`, `download-error`), an `AutoRepairOnFailure` option (default `true`), and a `FilesAutoRepaired` counter on `IntegrityCheckRun`. The per-file `CheckOneFileAsync` was refactored into `RunOnePassAsync` (one chunk-loop, lazy diag, returns the unflushed diag so the caller decides flush-vs-discard) plus a thin orchestration layer that runs pass 1, evaluates `canRepair = autoRepair && callback != null && firstPass.Failures.Any(f => RepairableReasons.Contains(f.FailureReason))`, on yes calls `firstPass.Diag?.Discard()` then the callback then re-reads the file via `_databaseService.GetBackedUpFile(path)` (because the re-upload replaced the chunk hashes) and runs pass 2; on second-pass clean increments `FilesAutoRepaired` and returns no failures (no `.diag`, no `integrity_check_failures` row); on second-pass still-failing flushes the second-pass diag with a `(post-auto-repair)` summary suffix and reports those failures. New helpers `FlushPassDiag` and `ReplayFailuresToDiag` keep the old "always-flush-on-failure" contract intact for the no-repair path and the callback-failed path. The retry is one-shot -- no recursion, no escalating budget. `MainWindowViewModel` wires the callback to `_orchestrator.BackupFileAsync(path, progress: null, memoryBudget: null, largeChunkPool: null, forceReupload: true, ct)`; the callback returns `false` on `OperationCanceledException` rethrown to the engine, on the orchestrator returning `false`, and on any other exception caught at the wire site (the engine then surfaces the original failures normally). The auto-repair count is surfaced in two UI places: the post-run toast in `MainWindowViewModel.OpenDataIntegrityTab`'s `CheckCompleted` handler appends ` (N auto-repaired)` to the summary line, and `IntegrityCheckRunViewModel.Header` appends ` [auto-repaired N]` after the `[ok]/[N failed]/[cancelled]` status when `Run.FilesAutoRepaired > 0`. `FilesAutoRepaired` is in-memory only on `IntegrityCheckRun` -- not persisted to the SQL `integrity_check_runs` table -- so historical rows always read 0 (deliberate; the value is only meaningful on the run that produced it). Three regression tests in `IntegrityCheckServiceTests` (`AutoRepair_MissingBlob_ReuploadFixesIt_NoFailureRecordedNoDiag`, `AutoRepair_Disabled_FailureStillReported`, `AutoRepair_CallbackReturnsFalse_OriginalFailureReported`) pin the contract; the first one explicitly uses `forceReupload: true` because an earlier draft used the two-arg `BackupFileAsync(path, ct)` overload and the metadata-skip path kept the deleted blob deleted -- preserved as a documented anti-pattern in the new architectural fact #13 (`DO NOT call BackupFileAsync(path, ct) from a repair callback`). Tests: 829 of 829 pass (was 826; +3 net). Lessons captured in fact #13: a successful self-heal must be silent on disk (no `.diag`) but visible in the UI summary so the user does not misread the run as a no-op; a force-reupload path must bypass BOTH file-level metadata-skip AND chunk-level dedup, otherwise unchanged-looking files with corrupt or missing remote bytes will silently stay broken; do not extend the `RepairableReasons` whitelist to reasons that re-uploading cannot fix (`local-file-missing`, `engine-error`) because the callback would loop pointlessly. `docs/USER_GUIDE.md` and `docs/SETUP.md` reviewed per the verified-current-docs maintenance protocol; the user-visible behaviour change ("the integrity check now silently re-uploads and re-checks repairable failures") needs a one-paragraph addition to the Data Integrity Check section in USER_GUIDE -- to land in the same commit. The "Force re-upload" / Full Scan button behaviour change (was: queue file-change events; now: actually re-upload) also needs a one-line USER_GUIDE update on the Sync view's button table.
- **2026-04-27** -- B44 commit. User repro: opened the program, switched to Storage Health, clicked Check Data Integrity, switched to Data Integrity, picked a custom selection (7314 files), kept Auto-export bundle on failure and Auto-repair on failure both checked, clicked Check Now, and saw `Check failed: SQLite Error 11: 'database disk image is malformed'` in the tab UX with a single log line `[SqliteBackend] InsertIntegrityCheckRun: enter (scope=(Custom selection) (7314 files), parentRunId=(none))`. Diagnosis: SQLite error 11 is `SQLITE_CORRUPT` -- the local catalog database file itself is malformed on disk. It is NOT a B43 schema issue (that would surface as `no such column files_auto_repaired`, not error 11) and the Data Integrity feature is the wrong tool to repair it (Data Integrity verifies the user's backed-up files against Azure storage, not the local catalog file). Implementation in three layers per architectural fact #16. (a) Backend: new partial file `SqliteBackend.IntegrityFile.cs` with `CheckDatabaseFileIntegrity()` that runs `PRAGMA cipher_integrity_check` then `PRAGMA integrity_check` under the existing write lock and a `DatabaseFileIntegrityResult` record encoding the asymmetric success shapes of the two pragmas (cipher: zero-rows-on-success; SQLite: single-`"ok"`-row-on-success). The pragma-name string is hard-coded at the call site so the runtime SQL is parameter-free and safe by construction. A `try/catch SqliteException` inside `RunPragmaIntegrityCheck` synthesises a per-pragma `"pragma X failed: SQLite Error N: …"` row when the corruption is bad enough that the pragma itself cannot read its own result page, so the caller sees a clear signal instead of a deceptive empty list. (b) Service plumbing: `LocalDatabaseService.CheckDatabaseFileIntegrity()` forwards on the SQLite backend and throws `NotSupportedException` on the LiteDB legacy backend (matches the existing integrity-persistence forwarders pattern); new `DatabaseFileCorruptException` in `Exceptions.cs` carries the underlying `SqliteErrorCode`; `IntegrityCheckService.RunAsync` wraps ONLY the first `InsertIntegrityCheckRun(run)` call in a typed catch for SQLite error 11 (`SQLITE_CORRUPT`) and 26 (`SQLITE_NOTADB`) and rethrows the typed exception with a multi-line user-facing message that points at the new Storage Health diagnostic and explains the difference between the two integrity features. (c) UI: `DataIntegrityViewModel.CheckNowAsync` and `ReCheckFailuresAsync` catch `DatabaseFileCorruptException` specifically and surface `Check could not start: …` / `Re-check could not start: …` ahead of the generic catch; `StorageHealthViewModel` gained `DatabaseFileReport`/`HasDatabaseFileReport` observables, `VerifyDatabaseFileAsync` `[RelayCommand]`, and a `FormatDatabaseFileReport` static helper that prints `ok (no failing pages)` for the empty-cipher-list success shape so the user is not misled into thinking the pragma was skipped. `StorageHealthView.axaml` adds a `Catalog Database File` card after `Index Management` with a `Verify Database File` button and a 240-px-tall `ScrollViewer`-wrapped `SelectableTextBlock` (Consolas, no wrap, horizontal scrollbar) so the report is copy-pasteable. Test: `CheckDatabaseFileIntegrity_OnHealthyDatabase_ReportsBothPragmasOk` initialises a fresh database, runs the new method, and asserts both `CipherOk` and `SqliteOk` are true on a healthy DB. The first draft of the result-record contract assumed both pragmas returned `"ok"` on success; the test caught the bug immediately because SQLCipher returns zero rows on success. Fixed in the same edit cycle: `CipherOk => Count==0`, with the asymmetry called out in the record's xmldoc and the report formatter. `docs/USER_GUIDE.md` updated with a new "Catalog database file" subsection under Storage Health describing when to use the diagnostic, what the cipher and SQLite pragmas check, and how to read the empty-vs-`ok` success shapes. `docs/SETUP.md` not updated -- B44 introduces no new build flag, runtime identifier, package, schema column, encryption-envelope change, or `AppMode.DataDirectory` resolution change, so per the verified-current-docs maintenance protocol it is not in scope. Tests: 831 of 831 pass (was 830 at B43; +1 net). Build: 0 warnings, 0 errors. Lessons captured in fact #16: (1) SQLCipher and stock SQLite report "healthy" with opposite row counts, so a naive single-`"ok"`-row predicate silently mislabels every healthy database when applied to the cipher pragma -- always verify the success shape of each pragma against its documentation rather than copy-pasting a check from a sibling pragma; (2) when an error category has a different remediation path from the surrounding feature (here: a corrupt catalog vs a corrupt backed-up file), translate it into a typed exception at the lowest layer that can detect the category, then surface a domain-appropriate message in the UI -- the auto-repair callback the rest of the integrity-check engine relies on cannot fix the catalog file and would loop pointlessly if reached; (3) `PRAGMA integrity_check` and `PRAGMA cipher_integrity_check` can themselves fail when the corruption is bad enough that the pragma cannot read its own result page, so the caller must turn an `ExecuteReader` `SqliteException` into a synthetic failure row rather than letting it surface as an empty result that looks like "healthy". Author: Copilot agent session.
- **2026-04-27** -- B43 commit. Five separate items rolled into one coherent batch after a follow-up to B42.
